/**
 * Parallel route page for history sheet slot.
 * This is rendered in the @sheet slot when navigating to /history.
 * The actual sheet wrapper is in the intercepting route (.)history/page.tsx.
 */
export default function HistorySheetPage() {
  // The sheet content is rendered by the intercepting route
  return null;
}
