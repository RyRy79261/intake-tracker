/**
 * Parallel route page for settings sheet slot.
 * This is rendered in the @sheet slot when navigating to /settings.
 * The actual sheet wrapper is in the intercepting route (.)settings/page.tsx.
 */
export default function SettingsSheetPage() {
  // The sheet content is rendered by the intercepting route
  return null;
}
