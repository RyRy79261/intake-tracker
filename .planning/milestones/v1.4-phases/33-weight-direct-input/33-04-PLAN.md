---
phase: 33-weight-direct-input
plan: 04
type: execute
wave: 1
depends_on: ["33-02"]
files_modified:
  - e2e/dashboard.spec.ts
autonomous: true
gap_closure: true
requirements: [WGT-01]

must_haves:
  truths:
    - "e2e/dashboard.spec.ts contains a test named 'should allow direct keyboard entry for weight'"
    - "The test focuses weight-direct-input, fills a value, blurs, verifies display, clicks Record Weight, and checks toast"
  artifacts:
    - path: "e2e/dashboard.spec.ts"
      provides: "E2E test for weight direct keyboard entry flow"
      contains: "direct keyboard entry for weight"
  key_links: []
---

<objective>
Restore the accidentally deleted E2E test for weight direct keyboard entry.

Purpose: Plan 03 commit 9e94070 accidentally deleted the test added by Plan 02 (commit 21e7326). The test body is recoverable from git history. No logic changes needed — just restore the test block.

Output: `e2e/dashboard.spec.ts` with the restored test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-weight-direct-input/33-VERIFICATION.md
@.planning/phases/33-weight-direct-input/33-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore deleted E2E test for weight direct keyboard entry</name>
  <files>e2e/dashboard.spec.ts</files>
  <read_first>e2e/dashboard.spec.ts</read_first>
  <action>
Add the following test block to `e2e/dashboard.spec.ts`, after the existing `should record a weight entry` test (after line 129, before the urination test):

```typescript
  test('should allow direct keyboard entry for weight', async ({ page }) => {
    await page.goto('/');
    const weightCard = page.locator('#section-weight');
    await weightCard.scrollIntoViewIfNeeded();
    await expect(weightCard).toBeVisible();

    // Wait for card to initialize
    const recordBtn = weightCard.locator('button:has-text("Record Weight")');
    await expect(recordBtn).toBeVisible();

    // Find the hidden input via data-testid
    const weightInput = weightCard.getByTestId('weight-direct-input');

    // Focus the hidden input (simulates tapping the weight display)
    await weightInput.focus();

    // Clear and type a new value
    await weightInput.fill('71.35');

    // Blur to trigger rounding (focus the record button)
    await recordBtn.focus();

    // Wait for display to update with the value
    // 71.35 is already aligned to 0.05 increments, so it stays 71.35
    await expect(weightCard.getByText('71.35')).toBeVisible();

    // Submit via existing Record Weight button
    await recordBtn.click();

    // Verify success toast
    await expect(page.getByText('Weight recorded', { exact: true })).toBeVisible();
  });
```

This is the exact test body from git commit 21e7326 that was accidentally deleted by Plan 03.
  </action>
  <acceptance_criteria>
    - `e2e/dashboard.spec.ts` contains `test('should allow direct keyboard entry for weight'`
    - The test calls `weightCard.getByTestId('weight-direct-input')`
    - The test fills '71.35' and verifies the value is visible
    - The test clicks 'Record Weight' and checks for 'Weight recorded' toast
    - `pnpm build` passes (no TypeScript errors)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - E2E test 'should allow direct keyboard entry for weight' restored to dashboard.spec.ts
    - Test exercises full tap-to-type-to-submit flow
  </done>
</task>

</tasks>

<threat_model>
No trust boundaries affected — test-only change.
</threat_model>

<verification>
1. `pnpm build` completes without errors
2. `grep "direct keyboard entry for weight" e2e/dashboard.spec.ts` returns a match
</verification>

<success_criteria>
- E2E test for weight direct keyboard entry exists in dashboard.spec.ts
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/33-weight-direct-input/33-04-SUMMARY.md`
</output>
