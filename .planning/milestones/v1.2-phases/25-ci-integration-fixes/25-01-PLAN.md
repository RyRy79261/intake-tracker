---
phase: 25-ci-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - .github/workflows/ci.yml
  - benchmarks/results.json
autonomous: true
requirements: [CIPL-01, CIPL-03, BNCH-01, SCHN-04]

must_haves:
  truths:
    - "pnpm typecheck exits 0 with zero errors"
    - "benchmarks/results.json contains main repo paths, not worktree paths"
    - "Supply chain drift check loop verifies all 4 pnpm security settings"
  artifacts:
    - path: "tsconfig.json"
      provides: "ES2020 target for type checking"
      contains: '"target": "ES2020"'
    - path: ".github/workflows/ci.yml"
      provides: "Complete 4-setting supply chain drift check"
      contains: "auditLevel"
    - path: "benchmarks/results.json"
      provides: "Clean benchmark baselines without worktree paths"
  key_links:
    - from: "tsconfig.json"
      to: "pnpm typecheck"
      via: "tsc --noEmit reads target from tsconfig"
      pattern: '"target":\\s*"ES2020"'
    - from: ".github/workflows/ci.yml"
      to: "pnpm-workspace.yaml"
      via: "drift check greps workspace config for each setting"
      pattern: "minimumReleaseAge trustPolicy blockExoticSubdeps auditLevel"
---

<objective>
Fix three CI integration defects identified in the v1.2 milestone audit so every CI job passes on a clean PR.

Purpose: DEFECT-01 (critical) breaks the typecheck CI job on every PR, blocking all merges. DEFECT-02 and DEFECT-03 are cleanliness and completeness issues that should ship with the milestone.
Output: Three modified files — tsconfig.json, ci.yml, benchmarks/results.json — closing all three defects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/25-ci-integration-fixes/25-CONTEXT.md
@.planning/phases/25-ci-integration-fixes/25-RESEARCH.md

@tsconfig.json
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix typecheck target and supply chain drift check</name>
  <files>tsconfig.json, .github/workflows/ci.yml</files>
  <read_first>
    - tsconfig.json (current config — no "target" field, defaults to ES3)
    - .github/workflows/ci.yml (line 164: supply-chain drift check loop missing auditLevel)
    - pnpm-workspace.yaml (verify all 4 security settings exist as grep targets)
  </read_first>
  <action>
**DEFECT-01 (per D-01, D-02 — addresses CIPL-01, CIPL-03):**

In `tsconfig.json`, add `"target": "ES2020"` as the FIRST entry in `compilerOptions`. The resulting compilerOptions block must start:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["dom", "dom.iterable", "esnext"],
    ...
  }
}
```

This resolves all 4 TypeScript errors:
- TS1501 in `parse-schema.ts` line 30 (regex `s` flag)
- TS1501 in `table-sync.test.ts` line 26 (regex `s` flag)
- TS2802 in `table-sync.test.ts` line 162 (Set spread)
- TS2802 in `table-sync.test.ts` line 170 (Set spread)

ES2020 is chosen over ES2022/ESNext as the minimum needed. Next.js uses SWC for transpilation independently — this only affects `tsc --noEmit`.

**DEFECT-03 (per D-05 — addresses SCHN-04):**

In `.github/workflows/ci.yml`, find the supply-chain job drift check loop (currently line 164):

```yaml
for setting in minimumReleaseAge trustPolicy blockExoticSubdeps; do
```

Change it to include `auditLevel` as the 4th setting:

```yaml
for setting in minimumReleaseAge trustPolicy blockExoticSubdeps auditLevel; do
```

The item must be added INSIDE the `for ... in` list, before the semicolon. This completes the 4-setting verification (matching the 4 security settings in pnpm-workspace.yaml).
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm typecheck && grep '"target": "ES2020"' tsconfig.json && grep "minimumReleaseAge trustPolicy blockExoticSubdeps auditLevel" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json contains `"target": "ES2020"` in compilerOptions
    - `pnpm typecheck` exits 0 with zero errors (confirms DEFECT-01 resolved)
    - .github/workflows/ci.yml contains `for setting in minimumReleaseAge trustPolicy blockExoticSubdeps auditLevel; do` (confirms DEFECT-03 resolved)
    - No other changes to tsconfig.json or ci.yml beyond these two targeted edits
  </acceptance_criteria>
  <done>pnpm typecheck exits 0. ci.yml drift check loop lists all 4 security settings.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate benchmark baselines with clean paths</name>
  <files>benchmarks/results.json</files>
  <read_first>
    - benchmarks/results.json (first 10 lines — confirm worktree paths like `.claude/worktrees/agent-a4dfb4bb/`)
    - package.json (verify bench:ci script definition: `vitest bench --outputJson benchmarks/results.json`)
  </read_first>
  <action>
**DEFECT-02 (per D-03, D-04 — addresses BNCH-01):**

Regenerate benchmark baselines by running `pnpm bench:ci` from the main repo root `/home/ryan/repos/Personal/intake-tracker` (NOT from any worktree directory). This runs `vitest bench --outputJson benchmarks/results.json` which overwrites the existing file.

Command:
```bash
cd /home/ryan/repos/Personal/intake-tracker && pnpm bench:ci
```

This will take 30-60 seconds (vitest bench runs real performance measurements with configured warmup). The benchmark files import `fake-indexeddb/auto` directly (bench files skip vitest setupFiles).

After regeneration, verify the `filepath` fields in the output do NOT contain `.claude/worktrees/`. They should contain the main repo path `/home/ryan/repos/Personal/intake-tracker/src/__tests__/bench/...`.

No CI guard or path-stripping script is needed (per D-04). The vitest comparison mechanism uses ID-based matching (derived from relative paths), so absolute paths in the JSON are cosmetic. The goal is simply to eliminate the confusing worktree paths.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && grep -c "worktrees" benchmarks/results.json | grep -q "^0$" && echo "PASS: no worktree paths" && grep -c '"filepath"' benchmarks/results.json | grep -q "[1-9]" && echo "PASS: filepath fields present" && pnpm bench --run --compare benchmarks/results.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - benchmarks/results.json does NOT contain the string "worktrees" (grep -c returns 0)
    - benchmarks/results.json contains at least one "filepath" field (file was regenerated, not emptied)
    - `pnpm bench --run --compare benchmarks/results.json` runs without errors (baseline comparison works)
    - filepath values start with `/home/ryan/repos/Personal/intake-tracker/` (main repo root, not worktree)
  </acceptance_criteria>
  <done>benchmarks/results.json contains clean paths from main repo root. Benchmark comparison runs successfully against the regenerated baseline.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification suite:

1. `pnpm typecheck` — exits 0 (DEFECT-01 closed)
2. `grep '"target": "ES2020"' tsconfig.json` — confirms tsconfig fix
3. `grep "minimumReleaseAge trustPolicy blockExoticSubdeps auditLevel" .github/workflows/ci.yml` — confirms all 4 settings in drift check (DEFECT-03 closed)
4. `grep -c "worktrees" benchmarks/results.json` returns 0 — confirms clean paths (DEFECT-02 closed)
5. `pnpm bench --run --compare benchmarks/results.json` — confirms benchmark comparison works
</verification>

<success_criteria>
- pnpm typecheck exits 0 with zero errors
- benchmarks/results.json has no worktree paths and benchmark comparison succeeds
- ci.yml supply chain drift check verifies all 4 pnpm security settings
- All 3 milestone audit defects (DEFECT-01, DEFECT-02, DEFECT-03) are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/25-ci-integration-fixes/25-01-SUMMARY.md`
</output>
