---
phase: 23-supply-chain-hardening
plan: 03
type: execute
wave: 1
depends_on: ["23-01", "23-02"]
files_modified: [".github/workflows/ci.yml", "pnpm-lock.yaml"]
autonomous: true
gap_closure: true
requirements: [SCHN-04]

must_haves:
  truths:
    - "pnpm audit --audit-level high exits 0 locally (all high findings either resolved or ignored with documented rationale)"
    - "CI supply-chain audit step passes on clean PRs (not permanently broken)"
    - "CI --ignore flags match exactly the set of unfixable/false-positive GHSAs documented in pnpm-workspace.yaml auditConfig.ignoreCves"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "supply-chain audit step with --ignore flags for known false-positives"
      contains: "--ignore GHSA-h25m-26qc-wcjf"
    - path: "pnpm-lock.yaml"
      provides: "lockfile with overrides fully applied"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "pnpm-lock.yaml"
      via: "overrides force safe transitive versions"
      pattern: "overrides"
    - from: "pnpm-workspace.yaml auditConfig.ignoreCves"
      to: ".github/workflows/ci.yml --ignore flags"
      via: "manual sync (pnpm 10.30 bug)"
      pattern: "--ignore GHSA-"
---

<objective>
Close two verification gaps from Phase 23: (1) force the lockfile to honor existing overrides for minimatch and picomatch, and (2) add --ignore flags to the CI audit step for documented false-positives and unfixable findings so the supply-chain job actually passes on clean PRs.

Purpose: The supply-chain CI job currently fails on EVERY PR (exit code 1) due to 6 high findings. This makes SCHN-04 non-functional — it cannot distinguish clean PRs from vulnerable ones.
Output: A working CI audit gate that exits 0 on the current codebase and will correctly exit 1 only when NEW high/critical vulnerabilities are introduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-supply-chain-hardening/23-01-SUMMARY.md
@.planning/phases/23-supply-chain-hardening/23-02-SUMMARY.md
@.planning/phases/23-supply-chain-hardening/23-VERIFICATION.md
@pnpm-workspace.yaml
@.github/workflows/ci.yml

<interfaces>
From pnpm-workspace.yaml (current state):
- auditConfig.ignoreCves contains: GHSA-h25m-26qc-wcjf, FAKE-0000-0000, GHSA-FAKE-0000, GHSA-mw96-cpmx-2vgc
- overrides section has 26 entries including:
  - `minimatch@>=10.0.0 <10.2.3: '>=10.2.3'` (not honored in lockfile)
  - `picomatch@>=4.0.0 <4.0.4: '>=4.0.4'` (not honored in lockfile)
- auditLevel: high

From .github/workflows/ci.yml line 134:
- Current audit step: `pnpm audit --audit-level high` (no --ignore flags)

Current high findings (6 total from `pnpm audit --audit-level high`):
- GHSA-h25m-26qc-wcjf: next RSC DoS (false positive — only affects Next.js 15+ RSC, app uses 14.x)
- GHSA-mw96-cpmx-2vgc: rollup path traversal (unfixable — next-pwa uses rollup 1.x, override breaks build)
- GHSA-3ppc-4f35-3m26: minimatch ReDoS (via next-pwa > @types/minimatch, override not applied in lockfile)
- GHSA-7r86-cg39-jmmj: minimatch ReDoS (same path as above)
- GHSA-23c5-xmqv-rm74: minimatch ReDoS (same path as above)
- GHSA-c2c7-rcm5-vvqj: picomatch ReDoS (via tinyglobby > fdir, override not applied in lockfile)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force lockfile to honor overrides and determine final ignore list</name>
  <files>pnpm-lock.yaml, pnpm-workspace.yaml</files>
  <read_first>
    - pnpm-workspace.yaml (current overrides and auditConfig)
    - .planning/phases/23-supply-chain-hardening/23-01-SUMMARY.md (deviation #3: lockfile regeneration broke @privy-io/react-auth)
  </read_first>
  <action>
    Attempt to get the existing overrides for minimatch and picomatch honored in the lockfile. The overrides already exist in pnpm-workspace.yaml:
    - `minimatch@>=10.0.0 <10.2.3: '>=10.2.3'`
    - `picomatch@>=4.0.0 <4.0.4: '>=4.0.4'`

    But `pnpm why minimatch@10.1.1` shows it is still resolved via `next-pwa > clean-webpack-plugin > del > @types/glob > @types/minimatch > minimatch`. And `pnpm why picomatch@4.0.3` shows it via `tinyglobby > fdir > picomatch` and `tinyglobby > picomatch`.

    Steps:
    1. Run `pnpm install` (without --frozen-lockfile) to let pnpm re-resolve with overrides applied
    2. Run `pnpm audit --audit-level high` to check which findings remain
    3. Run `pnpm build` to verify nothing broke (especially next-pwa and @privy-io/react-auth)
    4. Run `pnpm test` to verify tests still pass

    If `pnpm install` resolves the overrides (minimatch@10.1.1 and picomatch@4.0.3 replaced):
    - The remaining ignore list is: GHSA-h25m-26qc-wcjf (next RSC false positive) and GHSA-mw96-cpmx-2vgc (rollup unfixable)

    If `pnpm install` does NOT resolve the overrides (lockfile still has vulnerable versions):
    - These are structurally unfixable without a full lockfile delete (which broke @privy-io/react-auth in plan 01)
    - Add these 4 additional GHSAs to auditConfig.ignoreCves in pnpm-workspace.yaml with comments explaining they are override-resistant transitive deps:
      - GHSA-3ppc-4f35-3m26 (minimatch via next-pwa)
      - GHSA-7r86-cg39-jmmj (minimatch via next-pwa)
      - GHSA-23c5-xmqv-rm74 (minimatch via next-pwa)
      - GHSA-c2c7-rcm5-vvqj (picomatch via tinyglobby)

    Also clean up the test/fake entries in auditConfig.ignoreCves. Remove `FAKE-0000-0000` and `GHSA-FAKE-0000` — these are clearly test placeholders that should not be in the config.

    IMPORTANT: Do NOT delete pnpm-lock.yaml. Plan 01 documented that full lockfile deletion resolved @privy-io/react-auth to a breaking 3.18.0 version. Only run `pnpm install` which does incremental resolution.

    After determining final state, run `pnpm audit --audit-level high` and record the exact set of GHSAs that still need --ignore flags for Task 2.
  </action>
  <verify>
    <automated>pnpm build && pnpm audit --audit-level high 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `pnpm audit --audit-level high` output is understood: either exit 0 (all fixed) or a known set of unfixable GHSAs remain
    - `pnpm-workspace.yaml` auditConfig.ignoreCves does NOT contain `FAKE-0000-0000` or `GHSA-FAKE-0000`
    - No `@privy-io/react-auth` version change (still resolves to 3.12.0, verified via `pnpm why @privy-io/react-auth`)
  </acceptance_criteria>
  <done>Lockfile regenerated with overrides applied where possible. Final set of unfixable GHSAs identified and documented in auditConfig.ignoreCves. Build and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add --ignore flags to CI audit step for all documented unfixable GHSAs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current supply-chain job at lines 108-134)
    - pnpm-workspace.yaml (auditConfig.ignoreCves — source of truth for which GHSAs to ignore)
  </read_first>
  <action>
    Update the CI audit step in the supply-chain job to add `--ignore` flags for every GHSA listed in `pnpm-workspace.yaml` `auditConfig.ignoreCves`. This is necessary because pnpm 10.30.2 does NOT read `auditConfig.ignoreCves` from the workspace config (documented in 23-01-SUMMARY.md).

    The audit step at ci.yml line 133-134 currently reads:
    ```yaml
    - name: Audit dependencies
      run: pnpm audit --audit-level high
    ```

    Change it to include `--ignore` flags for each GHSA in auditConfig.ignoreCves. The exact GHSAs depend on Task 1's outcome, but the minimum set is:
    - `--ignore GHSA-h25m-26qc-wcjf` (Next.js RSC DoS — false positive, only affects >=15.0 with RSC; app uses 14.x)
    - `--ignore GHSA-mw96-cpmx-2vgc` (rollup path traversal — unfixable, override breaks next-pwa build)

    If Task 1 was unable to resolve minimatch/picomatch overrides, also add:
    - `--ignore GHSA-3ppc-4f35-3m26` (minimatch ReDoS via next-pwa, override-resistant)
    - `--ignore GHSA-7r86-cg39-jmmj` (minimatch ReDoS via next-pwa, override-resistant)
    - `--ignore GHSA-23c5-xmqv-rm74` (minimatch ReDoS via next-pwa, override-resistant)
    - `--ignore GHSA-c2c7-rcm5-vvqj` (picomatch ReDoS via tinyglobby, override-resistant)

    Format the audit step as a multi-line command for readability:
    ```yaml
    - name: Audit dependencies
      run: |
        # --ignore flags mirror auditConfig.ignoreCves in pnpm-workspace.yaml
        # Required because pnpm 10.30 does not read ignoreCves from workspace config
        pnpm audit --audit-level high \
          --ignore GHSA-h25m-26qc-wcjf \
          --ignore GHSA-mw96-cpmx-2vgc
    ```

    Add a comment in the YAML explaining why the flags exist and that they mirror pnpm-workspace.yaml.

    After updating ci.yml, verify the audit command passes locally by running the exact same command from the CI step.
  </action>
  <verify>
    <automated>grep -c "\-\-ignore GHSA-" .github/workflows/ci.yml && pnpm audit --audit-level high --ignore GHSA-h25m-26qc-wcjf --ignore GHSA-mw96-cpmx-2vgc 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `--ignore GHSA-h25m-26qc-wcjf` (verified via `grep -q "\-\-ignore GHSA-h25m-26qc-wcjf" .github/workflows/ci.yml`)
    - `.github/workflows/ci.yml` contains `--ignore GHSA-mw96-cpmx-2vgc` (verified via `grep -q "\-\-ignore GHSA-mw96-cpmx-2vgc" .github/workflows/ci.yml`)
    - The exact `pnpm audit` command from ci.yml, when run locally, exits 0
    - The ci.yml audit step includes a comment explaining the --ignore flags mirror pnpm-workspace.yaml auditConfig.ignoreCves
    - The supply-chain job config drift check (lines 119-132) is NOT modified — it already works correctly
  </acceptance_criteria>
  <done>CI supply-chain audit step passes locally with --ignore flags. The gate will correctly exit 0 on clean PRs and exit 1 only when NEW high/critical vulnerabilities are introduced.</done>
</task>

</tasks>

<verification>
1. `pnpm audit --audit-level high [--ignore flags]` exits 0 locally
2. `pnpm build` passes (no override-induced breakage)
3. `grep -c "\-\-ignore GHSA-" .github/workflows/ci.yml` returns at least 2 (minimum: h25m, mw96)
4. `grep -q "FAKE-0000-0000" pnpm-workspace.yaml` returns 1 (not found — test entries removed)
5. The exact audit command from ci.yml, copy-pasted and run locally, exits 0
</verification>

<success_criteria>
- The supply-chain CI job will pass on clean PRs (audit exits 0 with documented --ignore flags)
- The gate remains meaningful: any NEW high/critical vulnerability will cause exit 1
- All --ignore flags have documented rationale (false-positive or unfixable-without-breaking-build)
- No test placeholder entries remain in auditConfig.ignoreCves
</success_criteria>

<output>
After completion, create `.planning/phases/23-supply-chain-hardening/23-03-SUMMARY.md`
</output>
