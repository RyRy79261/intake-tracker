---
phase: 06-medication-ux-core
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - src/components/medications/schedule-view.tsx
  - src/components/medications/dose-row.tsx
  - src/components/medications/dose-detail-dialog.tsx
  - src/components/medications/retroactive-time-picker.tsx
  - src/app/medications/page.tsx
autonomous: false
requirements: [MEDX-03]

must_haves:
  truths:
    - "Navigating to a past date shows that day's schedule with missed doses actionable"
    - "Tapping Take on a past date shows a time picker pre-filled to the scheduled time"
    - "Today's Take is immediate with no time picker"
    - "Tapping a taken dose opens detail view with when taken, which inventory, and Untake action"
    - "Untake action reverses the dose and restores stock"
    - "User can navigate back to any date since prescription was active"
  artifacts:
    - path: "src/components/medications/retroactive-time-picker.tsx"
      provides: "Time picker for past dose logging"
      min_lines: 30
    - path: "src/components/medications/dose-detail-dialog.tsx"
      provides: "Updated dose detail with Untake action"
      min_lines: 80
  key_links:
    - from: "src/components/medications/dose-row.tsx"
      to: "src/components/medications/retroactive-time-picker.tsx"
      via: "Opens time picker when Take pressed on past date"
      pattern: "RetroactiveTimePicker|isToday"
    - from: "src/components/medications/dose-detail-dialog.tsx"
      to: "src/hooks/use-medication-queries.ts"
      via: "useUntakeDose mutation for Untake action"
      pattern: "useUntakeDose"
---

<objective>
Add retroactive dose logging (past date Take shows time picker), update dose detail dialog with Untake action, and do a final verification checkpoint of the complete medication UX.

Purpose: MEDX-03 requires retroactive logging with time picker pre-filled to scheduled time. The dose detail dialog needs the Untake action for correcting mistakes. This plan completes the medication workflow.
Output: Retroactive logging working, dose detail with Untake, full visual verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-medication-ux-core/06-CONTEXT.md
@.planning/phases/06-medication-ux-core/06-RESEARCH.md
@.planning/phases/06-medication-ux-core/06-01-SUMMARY.md
@.planning/phases/06-medication-ux-core/06-02-SUMMARY.md
@.planning/phases/06-medication-ux-core/06-03-SUMMARY.md

@src/components/medications/schedule-view.tsx
@src/components/medications/dose-row.tsx
@src/components/medications/dose-detail-dialog.tsx
@src/app/medications/page.tsx

<interfaces>
From src/lib/dose-log-service.ts:
```typescript
export interface TakeDoseInput {
  prescriptionId: string;
  phaseId: string;
  scheduleId: string;
  date: string;      // YYYY-MM-DD
  time: string;      // HH:MM — the time the dose was taken
  dosageMg: number;
}

export interface UntakeDoseInput {
  prescriptionId: string;
  phaseId: string;
  scheduleId: string;
  date: string;
  time: string;
  dosageMg: number;
}
```

From src/lib/dose-schedule-service.ts:
```typescript
export interface DoseSlot {
  // ... all fields as before
  localTime: string; // scheduled time in "HH:MM" — use as time picker default
  existingLog?: DoseLog; // present if dose was already logged
  // ...
}
```

From src/hooks/use-medication-queries.ts:
```typescript
export function useTakeDose() // mutation
export function useUntakeDose() // mutation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retroactive time picker + past-date Take flow</name>
  <files>src/components/medications/retroactive-time-picker.tsx, src/components/medications/dose-row.tsx, src/components/medications/schedule-view.tsx</files>
  <action>
1. Create `src/components/medications/retroactive-time-picker.tsx`:
   - shadcn Dialog wrapping a native `<input type="time">` (already used in dose-detail-dialog, mobile-friendly)
   - Props: `open: boolean, onOpenChange: (open: boolean) => void, defaultTime: string, compoundName: string, onConfirm: (time: string) => void`
   - Header: "When did you take {compoundName}?"
   - Time input pre-filled to defaultTime (the scheduled time from DoseSlot.localTime)
   - "Log Dose" primary button that calls onConfirm(selectedTime)
   - "Cancel" secondary button

2. Update `src/components/medications/dose-row.tsx`:
   - The Take button behavior must be date-aware:
     - If isToday: call onTake(slot) immediately (existing behavior from Plan 03)
     - If NOT isToday (past date): open RetroactiveTimePicker instead
   - Add local state for time picker: `const [timePickerOpen, setTimePickerOpen] = useState(false)`
   - On time picker confirm: call onTake with a modified slot/time — the parent (schedule-view) needs to know the user-selected time
   - Update onTake prop signature or add a separate onRetroactiveTake prop: `onRetroactiveTake?: (slot: DoseSlot, time: string) => void`
   - When past date Take confirmed with time: call onRetroactiveTake(slot, selectedTime)

3. Update `src/components/medications/schedule-view.tsx`:
   - Add handleRetroactiveTake handler:
     - Same as handleTake but uses the user-provided time instead of slot.localTime
     - Calls takeDose with `time: userSelectedTime` instead of `time: slot.localTime`
     - Haptic feedback and undo toast same as regular take
   - Pass onRetroactiveTake to DoseRow components
   - Past dates: no progress summary (per CONTEXT.md), time-slot-groups still render, missed slots actionable
   - Future dates: read-only schedule preview (no Take/Skip buttons)

Pitfall reminder: Date string format must be "YYYY-MM-DD" with zero-padding for getDailyDoseSchedule. The WeekDaySelector already provides dates correctly — just ensure the dateStr computation is consistent.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm lint && pnpm build</automated>
  </verify>
  <done>Past date Take opens time picker pre-filled to scheduled time, today's Take is immediate, future dates are read-only. Retroactive doses log with user-specified time.</done>
</task>

<task type="auto">
  <name>Task 2: Dose detail dialog with Untake + cleanup</name>
  <files>src/components/medications/dose-detail-dialog.tsx, src/app/medications/page.tsx</files>
  <action>
1. Update `src/components/medications/dose-detail-dialog.tsx`:
   - Update to accept DoseSlot (or both DoseSlot and the existing props for backward compat)
   - For taken doses, show:
     - When taken: formatted timestamp from existingLog.actionTimestamp
     - Which inventory: brand name, region, strength from slot.inventory
     - Pills consumed: formatPillCount(slot.pillsPerDose)
     - "Untake" destructive action button (red text, bottom of dialog)
   - Untake action:
     - Call useUntakeDose().mutateAsync with UntakeDoseInput derived from DoseSlot
     - Call hapticSkip() for feedback (different pattern than take)
     - Show toast: "{genericName} dose reversed"
     - Close dialog
   - For skipped doses, show:
     - Skip reason
     - Skipped at timestamp
     - No untake action (or optionally a "Log as taken" action — use discretion)
   - For pending/missed doses, show:
     - Scheduled time, dosage, inventory info
     - Take and Skip action buttons (same as dose-row but in dialog context)
   - Keep existing dialog structure (shadcn Dialog), update content

2. Update `src/app/medications/page.tsx`:
   - Ensure DoseDetailDialog receives the correct data type (DoseSlot from the new schedule-view)
   - Update handleDoseClick to work with DoseSlot:
     ```typescript
     const handleDoseClick = useCallback((slot: DoseSlot) => {
       setSelectedSlot(slot);
       setDoseDetailOpen(true);
     }, []);
     ```
   - Update state: replace selectedEntry/selectedDoseLog with selectedSlot: DoseSlot | null
   - Clean up any unused imports from old schedule-view integration
   - Remove old component files that are no longer imported anywhere:
     - Delete src/components/medications/status-view.tsx (absorbed into schedule progress summary)
     - Delete src/components/medications/prescriptions-list.tsx (replaced by compound-list)
     - Delete src/components/medications/medications-list.tsx (replaced by compound-list)
   - Verify no other files import the deleted components (grep for StatusView, PrescriptionsList, MedicationsList)
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm lint && pnpm build</automated>
  </verify>
  <done>Dose detail dialog shows taken time, inventory used, pills consumed for taken doses with Untake action. Skipped doses show reason. Old component files deleted. Page.tsx uses DoseSlot throughout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete medication UX</name>
  <files></files>
  <action>
Human visual verification of the complete medication UX rebuild across all 4 plans.

Verification steps:
1. Open http://localhost:3000/medications
2. Verify 3 tabs: Schedule, Medications, Settings
3. On Medications tab: verify compound cards show genericName as heading, active brand as sub-label, stock level
4. Tap a compound card — verify it expands inline showing all inventory items with region labels, schedule, actions
5. Test brand switching if multiple inventory items exist
6. Switch to Schedule tab — verify progress bar at top (today only), time-slot groups
7. Tap Take on a pending dose — verify immediate log, undo toast with 5-second countdown
8. Tap Undo within 5 seconds — verify dose reversal
9. Tap Skip — verify reason picker with presets, dose logs as skipped
10. Tap a taken dose — verify detail dialog with Untake action
11. Navigate to a past date via WeekDaySelector — verify missed doses are actionable
12. Tap Take on a past date dose — verify time picker pre-filled to scheduled time
13. Navigate to a future date — verify schedule is read-only (no Take/Skip buttons)
14. If all doses taken today, verify "All done for today!" banner
15. Check fractional pill display (if any prescriptions use fractional doses)
  </action>
  <verify>Human visual verification of all medication UX flows</verify>
  <done>User approves the complete medication workflow visually and functionally</done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `pnpm lint` passes
- No imports of StatusView, PrescriptionsList, or MedicationsList remain in codebase
- DoseDetailDialog accepts DoseSlot data
- RetroactiveTimePicker opens only on past dates
- Untake action calls useUntakeDose mutation
- All 6 MEDX requirements addressed across Plans 01-04
</verification>

<success_criteria>
A user can manage their full medication workflow: view prescriptions by compound with multi-region inventory, log doses with one tap (today) or with time picker (past dates), see today's progress at a glance, undo mistakes within 5 seconds, and untake doses via the detail view. The schedule shows due/taken/pending/missed states clearly. Fractional doses display correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-medication-ux-core/06-04-SUMMARY.md`
</output>
