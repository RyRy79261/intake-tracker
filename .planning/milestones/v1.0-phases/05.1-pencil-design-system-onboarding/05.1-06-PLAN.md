---
phase: 05.1-pencil-design-system-onboarding
plan: 06
type: execute
wave: 3
depends_on:
  - 05.1-02
  - 05.1-03
files_modified:
  - design/app.pen
autonomous: true
execution_context: main
requirements:
  - DSGN-04

must_haves:
  truths:
    - "Analytics screen exists with all 4 tab variants designed (Records, Insights, Correlations, Titration)"
    - "Each tab variant shows populated state with representative data"
    - "Analytics screen has empty, loading, and error state variants"
  artifacts:
    - path: "design/app.pen"
      provides: "Analytics screen designs with all tab and state variants"
  key_links:
    - from: "Screen / Analytics"
      to: "Tabs component + domain components"
      via: "ref instances"
---

<objective>
Design the Analytics screen with all 4 tab variants and state variants.

Purpose: Analytics is a data-heavy screen with 4 distinct tabs, each requiring its own populated design. This provides the design reference for the existing analytics implementation and future enhancements.

Output: Analytics screen with Records, Insights, Correlations, and Titration tab views (populated) + empty/loading/error states.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-RESEARCH.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-02-SUMMARY.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-03-SUMMARY.md

@design/app.pen
@src/components/analytics/analytics-page.tsx
@src/components/analytics/records-tab.tsx
@src/components/analytics/insights-tab.tsx
@src/components/analytics/correlations-tab.tsx
@src/components/analytics/titration-tab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design Analytics screen with Records and Insights tabs</name>
  <files>design/app.pen</files>
  <action>
Read the analytics page and tab components to understand the layout. Create screens using Pencil MCP `batch_design`:

**Screen / Analytics Records** (Records tab active, populated):
- 393x852px frame, fill=$bg, vertical layout
- Chrome / App Header ref (title: "Analytics")
- Time range selector: Tabs ref with "Today", "7D", "30D", "90D", "All" options
- Tabs ref below for tab navigation: Records (active), Insights, Correlations, Titration
- Records content:
  - Sorted list of unified records (most recent first)
  - Each record row: timestamp, domain icon (colored by domain), description, value
  - Example records: water intake, BP reading, medication dose, eating log, weight measurement
  - Mix of domain types to show the unified view
  - Domain-colored left accent on each row (use $water, $bp, $weight, etc.)

**Screen / Analytics Insights** (Insights tab active, populated):
- Same chrome and time range selector
- Tabs with Insights active
- Insights content:
  - Insight banner cards (2-3 examples):
    - "Your water intake is 15% above average this week" with upward trend icon, $water accent
    - "Blood pressure trending down over 30 days" with chart mini-icon, $bp accent
    - "Caffeine intake peaks on Mondays" with $caffeine accent
  - Each insight: icon + text + dismiss button + "View Details" link

Use `get_screenshot` to verify both variants.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findFrames(node,name,found=[]){if(node.name&&node.name.includes(name))found.push(node.name);if(node.children)node.children.forEach(c=>findFrames(c,name,found));return found;} const screens=[...findFrames(d,'Screen / Analytics')]; console.log('analytics screens:', screens.length, screens);"</automated>
  </verify>
  <done>Analytics screen exists with Records tab (unified record list with domain-colored rows) and Insights tab (insight banner cards with domain accents). Both show populated state with sample data.</done>
</task>

<task type="auto">
  <name>Task 2: Design Analytics Correlations and Titration tabs + state variants</name>
  <files>design/app.pen</files>
  <action>
Continue Analytics screen designs using Pencil MCP `batch_design`:

**Screen / Analytics Correlations** (Correlations tab active, populated):
- Same chrome and time range selector
- Tabs with Correlations active
- Correlations content:
  - Correlation selector: two domain dropdowns (e.g., "Water" vs "Blood Pressure")
  - Chart area: represent a scatter plot or overlay chart using simple shapes (dots for scatter, lines for overlay)
  - Use rectangles and circles to approximate chart visualization (exact chart rendering is Claude's discretion per CONTEXT.md)
  - Correlation coefficient display: "r = 0.42" with strength indicator
  - Lag days selector if applicable

**Screen / Analytics Titration** (Titration tab active, populated):
- Same chrome and time range selector
- Tabs with Titration active
- Titration content:
  - Prescription selector dropdown
  - Phase timeline: visual representation of medication phases with date ranges
  - Dosage chart: stepped line showing dosage changes over time
  - Adherence metric during each phase

**Screen / Analytics Empty** (any tab, no data):
- Same chrome and tabs
- Center: BarChart3 icon (large, $muted-foreground), "No data yet" title, "Start tracking to see analytics" description

**Screen / Analytics Loading**:
- Skeleton variant: tab bar visible, content area filled with $muted placeholder bars and circles

**Screen / Analytics Error**:
- Same error pattern as other screens

Use `get_screenshot` to verify all variants.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findFrames(node,name,found=[]){if(node.name&&node.name.includes(name))found.push(node.name);if(node.children)node.children.forEach(c=>findFrames(c,name,found));return found;} const screens=findFrames(d,'Screen / Analytics'); console.log('total analytics screens:', screens.length, screens);"</automated>
  </verify>
  <done>All 4 Analytics tab variants are designed (Records, Insights, Correlations, Titration) plus empty, loading, and error states. Total of 7 Analytics screen frames.</done>
</task>

</tasks>

<verification>
- 4 tab-specific Analytics screen variants exist (Records, Insights, Correlations, Titration)
- 3 state variants exist (Empty, Loading, Error)
- All screens use 393px width and component refs
- Charts/graphs represented reasonably (Claude's discretion per CONTEXT.md)
- get_screenshot confirms visual correctness
</verification>

<success_criteria>
Analytics screen is fully designed across all 4 tabs with populated data and all state variants, providing complete design reference.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-pencil-design-system-onboarding/05.1-06-SUMMARY.md`
</output>
