---
phase: 05.1-pencil-design-system-onboarding
plan: 02
type: execute
wave: 2
depends_on:
  - 05.1-01
files_modified:
  - design/app.pen
autonomous: true
execution_context: main
requirements:
  - DSGN-02

must_haves:
  truths:
    - "All 17 shadcn primitive components exist as reusable Pencil components"
    - "Each primitive visually matches the corresponding shadcn/ui component in the codebase"
    - "Components use $variable references for all colors, not hardcoded hex"
    - "Components use Outfit font family"
  artifacts:
    - path: "design/app.pen"
      provides: "17 reusable primitive components"
  key_links:
    - from: "design/app.pen primitives"
      to: "src/components/ui/*.tsx"
      via: "1:1 visual parity"
---

<objective>
Create all 17 shadcn primitive components as reusable Pencil components in design/app.pen.

Purpose: These primitives are the building blocks for all domain-specific components and screen designs. They must exist before domain components can be composed from them.

Output: 17 reusable Pencil components matching the app's shadcn/ui primitives: button, card, collapsible, dialog, drawer, input, label, numeric-input, progress, scroll-area, select, sheet, switch, tabs, textarea, toast, toaster.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-RESEARCH.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-01-SUMMARY.md

@design/app.pen
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/ui/dialog.tsx
@src/components/ui/drawer.tsx
@src/components/ui/input.tsx
@src/components/ui/tabs.tsx
@src/components/ui/switch.tsx
@src/components/ui/select.tsx
@src/components/ui/toast.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core primitive components (button, card, input, label, select, switch, tabs)</name>
  <files>design/app.pen</files>
  <action>
Using Pencil MCP `batch_design` tool, create a container frame named "Components / Primitives" to hold all primitive component definitions.

Create each component as a `reusable: true` frame. Use separate reusable components for each variant (e.g., "Button / Primary", "Button / Secondary", "Button / Destructive", "Button / Outline", "Button / Ghost"). Reference the corresponding `src/components/ui/*.tsx` file for visual specs.

**Button** (5 variants: Primary, Secondary, Destructive, Outline, Ghost):
- Height: 40px, padding: 0 16px, border-radius: $radius, font-weight: 500, font-size: 14px
- Primary: fill=$primary, text=$primary-foreground
- Secondary: fill=$secondary, text=$secondary-foreground
- Destructive: fill=$destructive, text=$destructive-foreground
- Outline: stroke=$border, fill=transparent, text=$foreground
- Ghost: fill=transparent, text=$foreground

**Card** (container component):
- fill=$card-bg, border-radius=$radius, stroke=$border, padding: 24px
- Card with header sub-variant: title text (font-weight: 600, font-size: 16px) + description text ($muted-foreground, font-size: 14px) + content area

**Input**:
- Height: 40px, fill=transparent, stroke=$border, border-radius: calc(radius-2px), padding: 0 12px
- Placeholder text in $muted-foreground, value text in $foreground
- Font-size: 14px

**Label**:
- Text node, font-size: 14px, font-weight: 500, fill=$foreground

**Select**:
- Trigger: same style as Input + chevron-down Lucide icon on right
- Create trigger variant only (dropdown content is an overlay concept)

**Switch**:
- Track: 44x24px rounded-full, unchecked=$muted, checked=$primary
- Thumb: 20x20px circle, fill=white
- Two variants: on and off

**Tabs**:
- TabsList container: fill=$muted, border-radius=$radius, padding: 4px, horizontal layout
- TabsTrigger: padding 8px 16px, border-radius: calc(radius-4px), font-size: 14px
- Active variant: fill=$card-bg, text=$foreground
- Inactive variant: fill=transparent, text=$muted-foreground

All components: Outfit font family, `$variable` references for all colors.

Use `get_screenshot` after creating all components to verify visual appearance.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findReusable(node,found=[]){if(node.reusable)found.push(node.name);if(node.children)node.children.forEach(c=>findReusable(c,found));return found;} const r=findReusable(d); console.log('reusable components:', r.length, r.filter(n=>n.includes('Button')).length>0?'has Button':'missing Button', r.filter(n=>n.includes('Card')).length>0?'has Card':'missing Card');"</automated>
  </verify>
  <done>Button (5 variants), Card, Input, Label, Select, Switch (2 variants), and Tabs components exist as reusable Pencil components with correct styling using $variable references and Outfit font.</done>
</task>

<task type="auto">
  <name>Task 2: Create remaining primitive components (dialog, drawer, sheet, collapsible, progress, numeric-input, textarea, scroll-area, toast, toaster)</name>
  <files>design/app.pen</files>
  <action>
Continue building reusable components in the "Components / Primitives" frame using Pencil MCP `batch_design`.

**Dialog**:
- Overlay: semi-transparent black background (fill=#000000, opacity: 0.5)
- Content: centered card, max-width: 350px, fill=$card-bg, border-radius=$radius, padding: 24px
- Close button (X icon) top-right
- Title: font-weight: 600, font-size: 18px
- Description: $muted-foreground, font-size: 14px
- Footer with action buttons (use Button / Primary and Button / Outline refs)

**Drawer**:
- Bottom sheet style: fill=$card-bg, border-radius: 12px 12px 0 0, padding: 16px
- Drag handle: centered 32x4px rounded bar, fill=$muted
- Full width (393px for mobile frame)

**Sheet**:
- Side panel: fill=$card-bg, width: 320px, full height
- Close button top-right, content area with padding: 24px

**Collapsible**:
- Header row: text + chevron-down icon, clickable area
- Content area below (shown in expanded variant, hidden in collapsed)
- Two variants: expanded and collapsed

**Progress**:
- Track: height 8px, fill=$secondary, rounded-full
- Fill: fill=$primary, rounded-full, width percentage-based
- Create at 60% fill as example

**Numeric Input** (the custom component from the app):
- Horizontal layout: minus button (-) | value display | plus button (+)
- Buttons: 40x40px, fill=$secondary, border-radius: rounded
- Value: centered text, font-size: 18px, font-weight: 600

**Textarea**:
- Same style as Input but height: 80px, multi-line, padding: 12px

**Scroll Area**:
- Container with thin scrollbar track (4px wide, $muted fill, right edge)
- Content area placeholder

**Toast**:
- Horizontal card: fill=$card-bg, stroke=$border, border-radius=$radius, padding: 12px 16px
- Left: text content (title 14px/600 + description 14px/$muted-foreground)
- Right: close button or action button
- Success variant: left accent border in green
- Error variant: left accent border in $destructive

**Toaster**:
- Container positioned bottom-right, vertical stack of toast instances, gap: 8px

Use `get_screenshot` to verify all components.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findReusable(node,found=[]){if(node.reusable)found.push(node.name);if(node.children)node.children.forEach(c=>findReusable(c,found));return found;} const r=findReusable(d); console.log('total reusable:', r.length, 'has Dialog:', r.some(n=>n.includes('Dialog')), 'has Drawer:', r.some(n=>n.includes('Drawer')), 'has Toast:', r.some(n=>n.includes('Toast')));"</automated>
  </verify>
  <done>All 17 shadcn primitive types are represented as reusable Pencil components (with variants where applicable). Total reusable component count should be 20+ (accounting for variants). All use $variable references and Outfit font.</done>
</task>

</tasks>

<verification>
- design/app.pen contains a "Components / Primitives" frame
- At least 20 reusable components exist (17 types + variants for button, switch, collapsible, toast)
- All components use $variable color references
- All text nodes use Outfit font family
- get_screenshot shows visually correct components
</verification>

<success_criteria>
All 17 shadcn primitives from src/components/ui/ have 1:1 visual parity as reusable Pencil components. Components use design token variables and can be instantiated via ref in screen designs.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-pencil-design-system-onboarding/05.1-02-SUMMARY.md`
</output>
