---
phase: 05-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth-middleware.ts
  - src/stores/settings-store.ts
  - src/hooks/use-settings.ts
  - src/lib/security.ts
  - src/lib/perplexity.ts
  - src/components/voice-input.tsx
  - src/hooks/use-medicine-search.ts
  - src/components/settings/ai-integration-section.tsx
  - src/lib/backup-service.ts
  - src/lib/retention.ts
autonomous: true
requirements:
  - SECU-01
  - SECU-03

must_haves:
  truths:
    - "No client-side code references perplexityApiKey or aiAuthSecret"
    - "Settings store persists cleanly without API key fields after migration"
    - "Auth middleware function exists and rejects unauthenticated requests with 401"
    - "AI integration settings section no longer appears in /settings"
  artifacts:
    - path: "src/lib/auth-middleware.ts"
      provides: "Centralized withAuth HOF for API route authentication"
      exports: ["withAuth", "AuthenticatedRequest"]
    - path: "src/stores/settings-store.ts"
      provides: "Settings store with API key fields removed and persist migration"
      contains: "version: 1"
    - path: "src/lib/security.ts"
      provides: "Security utils with obfuscation functions removed"
  key_links:
    - from: "src/lib/auth-middleware.ts"
      to: "src/lib/privy-server.ts"
      via: "imports verifyAndCheckWhitelist"
      pattern: "verifyAndCheckWhitelist"
    - from: "src/stores/settings-store.ts"
      to: "localStorage"
      via: "Zustand persist migrate function"
      pattern: "migrate.*version"
---

<objective>
Create centralized auth middleware and remove all client-side API key storage, obfuscation code, and related UI.

Purpose: Eliminate the insecure pattern of storing API keys client-side and establish the auth middleware that API routes will migrate to in Plan 02.
Output: `auth-middleware.ts` with `withAuth` HOF, cleaned settings store with persist migration, removed obfuscation code, deleted AI settings UI section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-hardening/05-CONTEXT.md
@.planning/phases/05-security-hardening/05-RESEARCH.md

@src/lib/privy-server.ts
@src/stores/settings-store.ts
@src/lib/security.ts
@src/lib/backup-service.ts

<interfaces>
<!-- Key types from privy-server.ts that auth-middleware.ts will consume -->

From src/lib/privy-server.ts:
```typescript
export interface VerificationResult {
  success: boolean;
  userId?: string;
  email?: string;
  error?: string;
  source?: "privy" | "dev-fallback";
}

export async function verifyAndCheckWhitelist(authToken: string | null): Promise<VerificationResult>;
export function isPrivyConfigured(): boolean;
```

From src/lib/security.ts (functions to REMOVE):
```typescript
export function obfuscateApiKey(key: string): string;
export function deobfuscateApiKey(obfuscated: string): string;
// OBFUSCATION_KEY constant also to remove
```

From src/lib/crypto.ts (referenced by Plan 02):
```typescript
export interface EncryptedData {
  data: string;
  iv: string;
  salt: string;
  algorithm: string;
}
export async function encrypt(plaintext: string, pin: string): Promise<EncryptedData>;
export async function decrypt(encrypted: EncryptedData, pin: string): Promise<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth middleware and clean settings store</name>
  <files>src/lib/auth-middleware.ts, src/stores/settings-store.ts, src/hooks/use-settings.ts</files>
  <action>
1. Create `src/lib/auth-middleware.ts` with a `withAuth` higher-order function:
   - Import `verifyAndCheckWhitelist` and `VerificationResult` from `./privy-server`
   - Export `AuthenticatedRequest` interface: `{ request: NextRequest; auth: VerificationResult }`
   - Export `withAuth(handler: AuthenticatedHandler)` that:
     - Extracts Bearer token from Authorization header
     - Calls `verifyAndCheckWhitelist(authToken)`
     - Returns 401 with `{ error, requiresAuth: true }` on failure
     - Calls the wrapped handler with `{ request, auth }` on success
   - Add production guard: if `process.env.NEXT_PUBLIC_LOCAL_AGENT_MODE === "true"` AND `process.env.NODE_ENV === "production"`, log a warning and ignore LOCAL_AGENT_MODE (do NOT bypass auth in production)

2. Modify `src/stores/settings-store.ts`:
   - Remove `perplexityApiKey` and `aiAuthSecret` from the Settings interface and default values
   - Remove `setPerplexityApiKey`, `getDeobfuscatedApiKey`, `setAiAuthSecret`, `getDeobfuscatedAuthSecret` from SettingsActions interface and implementation
   - Remove imports of `obfuscateApiKey` and `deobfuscateApiKey` from security.ts
   - Add Zustand persist migration: set `version: 1` in persist config, add `migrate` function that deletes `perplexityApiKey` and `aiAuthSecret` from persisted state when version is 0

3. Modify `src/hooks/use-settings.ts`:
   - Remove `usePerplexityKey()` and `useAiAuthSecret()` hooks entirely
   - Remove any imports related to API key hooks
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Auth middleware exists with withAuth export. Settings store no longer has perplexityApiKey or aiAuthSecret fields. Zustand persist migration removes old fields from localStorage. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove obfuscation code and client API key references</name>
  <files>src/lib/security.ts, src/lib/perplexity.ts, src/components/voice-input.tsx, src/hooks/use-medicine-search.ts, src/components/settings/ai-integration-section.tsx, src/lib/backup-service.ts, src/lib/retention.ts</files>
  <action>
1. Modify `src/lib/security.ts`:
   - Remove `OBFUSCATION_KEY` constant
   - Remove `obfuscateApiKey()` function entirely
   - Remove `deobfuscateApiKey()` function entirely
   - Keep all other functions (sanitizeForAI, sanitizeNumericInput, sanitizeTextInput, isSecureContext, getSecurityWarnings)

2. Modify `src/lib/perplexity.ts`:
   - Remove `clientApiKey` option/parameter from `parseIntakeWithPerplexity()` function
   - The function should only use the server-side API key path (called from API routes)
   - If the function is only ever called server-side, simplify accordingly

3. Modify `src/components/voice-input.tsx`:
   - Remove any client API key fallback logic (around lines 89-92 per research)
   - AI features should work only when authenticated via server-side API routes

4. Modify `src/hooks/use-medicine-search.ts`:
   - Remove `clientApiKey` from fetch request body (around lines 53, 60-61 per research)
   - The hook should send auth token (Bearer header) instead of client API key

5. DELETE `src/components/settings/ai-integration-section.tsx` entirely
   - Find and remove the import/render of `AiIntegrationSection` from the parent settings page component

6. Modify `src/lib/backup-service.ts`:
   - Remove the `perplexityApiKey` destructure/exclusion from `exportBackup()` (line ~63)
   - Settings no longer have that field, so the exclusion is dead code

7. Modify `src/lib/retention.ts`:
   - Remove `perplexityApiKey` deletion (line ~97) -- field no longer exists

8. Grep the entire `src/` directory for any remaining references to `perplexityApiKey`, `aiAuthSecret`, `clientApiKey`, `obfuscateApiKey`, `deobfuscateApiKey`, `OBFUSCATION_KEY` and fix any stragglers.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm build && grep -r "perplexityApiKey\|aiAuthSecret\|clientApiKey\|obfuscateApiKey\|deobfuscateApiKey\|OBFUSCATION_KEY" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v ".next" ; echo "Exit: $?"</automated>
  </verify>
  <done>Zero grep matches for any client API key references in src/. All obfuscation code removed. AI integration settings section deleted. Build passes clean.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with no TypeScript errors
- `grep -r "perplexityApiKey\|aiAuthSecret\|clientApiKey\|obfuscateApiKey\|deobfuscateApiKey" src/` returns nothing
- `src/lib/auth-middleware.ts` exists and exports `withAuth`
- `src/components/settings/ai-integration-section.tsx` does not exist
</verification>

<success_criteria>
All client-side API key storage paths eliminated. Auth middleware ready for API route migration in Plan 03. Settings store migrates cleanly. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-security-hardening/05-01-SUMMARY.md`
</output>
