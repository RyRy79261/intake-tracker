---
phase: 04-analytics-service
plan: 06
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - src/components/analytics/insights-tab.tsx
  - src/components/analytics/insight-banner.tsx
  - src/components/insight-badge.tsx
  - src/app/page.tsx
  - src/stores/settings-store.ts
autonomous: true
requirements: [SRVC-05]

must_haves:
  truths:
    - "Insights tab displays auto-generated headline insights with drill-down capability"
    - "Insight banners are dismissable with condition-based reappearance"
    - "Dashboard shows compact insight badge that navigates to /analytics"
    - "Dismissed insights persist and only reappear when underlying condition changes"
  artifacts:
    - path: "src/components/analytics/insights-tab.tsx"
      provides: "Insights tab with auto-generated insights"
    - path: "src/components/analytics/insight-banner.tsx"
      provides: "Dismissable insight banner component"
    - path: "src/components/insight-badge.tsx"
      provides: "Compact dashboard badge showing insight count"
    - path: "src/stores/settings-store.ts"
      provides: "Dismissed insight state persistence"
      contains: "dismissedInsights"
  key_links:
    - from: "src/components/analytics/insights-tab.tsx"
      to: "src/hooks/use-analytics-queries.ts"
      via: "useInsights hook"
      pattern: "useInsights"
    - from: "src/components/insight-badge.tsx"
      to: "src/hooks/use-analytics-queries.ts"
      via: "useInsights hook"
      pattern: "useInsights"
---

<objective>
Build the Insights tab for the analytics page, the insight banner component with dismissal/reappearance logic, and a dashboard badge for surfacing insights.

Purpose: Auto-generated insights surface notable health trends and changes without requiring the user to manually analyze data.
Output: Insights tab, insight banner, dashboard badge, dismissal persistence
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics-service/04-CONTEXT.md
@.planning/phases/04-analytics-service/04-03-SUMMARY.md

@src/app/page.tsx
@src/stores/settings-store.ts

<interfaces>
From src/hooks/use-analytics-queries.ts (created in Plan 03):
```typescript
export function useInsights(range: TimeRange): Insight[]
export function useTimeScopeRange(scope: TimeScope): TimeRange
```

From src/lib/analytics-types.ts (created in Plan 01):
```typescript
export interface Insight {
  id: string;
  type: InsightType;
  title: string;
  description: string;
  severity: 'info' | 'warning' | 'alert';
  value: number;
  threshold: number;
  timestamp: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insight dismissal persistence and banner component</name>
  <files>src/stores/settings-store.ts, src/components/analytics/insight-banner.tsx</files>
  <action>
1. Add dismissed insight state to Zustand settings store:
   - Add `dismissedInsights: Record<string, { dismissedAt: number; triggerValue: number }>` to persisted state
   - Add `dismissInsight(id: string, triggerValue: number): void` action -- stores dismissedAt timestamp and the value that triggered the insight
   - Add `clearDismissedInsight(id: string): void` action
   - Add `isDismissed(id: string, currentValue: number, threshold?: number): boolean` helper:
     - Returns true if insight is dismissed AND currentValue hasn't changed by more than 10% from the trigger value
     - If currentValue differs by >10% from dismissed triggerValue, the insight should reappear (condition changed)
     - Default threshold: 0.1 (10%)

2. Create `src/components/analytics/insight-banner.tsx`:
   - Props: `insight: Insight`, `onDismiss: (id: string) => void`
   - Renders a card with:
     - Severity indicator: info (blue), warning (amber), alert (red) -- left border color
     - Title (bold) + description
     - Value display showing the triggering metric
     - Dismiss button (X icon) in top-right corner
   - Severity-specific icons: Info, AlertTriangle, AlertCircle from lucide-react
   - Tailwind styling: rounded card with shadow-sm, left border colored by severity
   - Animate out on dismiss (simple opacity transition, CSS only)
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && npx tsc --noEmit src/stores/settings-store.ts src/components/analytics/insight-banner.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Settings store persists dismissed insights with trigger values. Insight banner renders with severity coloring and dismiss button. Dismissal uses 10% threshold for reappearance logic.</done>
</task>

<task type="auto">
  <name>Task 2: Insights tab and dashboard badge</name>
  <files>src/components/analytics/insights-tab.tsx, src/components/insight-badge.tsx, src/app/page.tsx</files>
  <action>
1. Create `src/components/analytics/insights-tab.tsx`:
   - Props: `range: TimeRange`
   - Uses `useInsights(range)` to get insight array
   - Reads dismissedInsights from settings store to filter out dismissed insights (using isDismissed check)
   - Headline section at top: "X active insights" summary
   - Renders InsightBanner for each non-dismissed insight, sorted by severity (alert first, then warning, then info)
   - On dismiss: calls settings store dismissInsight with insight.id and insight.value
   - Empty state: "No notable insights for this period" with a brief explanation of what insights track
   - Section below insights for "drill-down": for each insight type, show a brief expanded view:
     - adherence_drop: show daily adherence chart (simple bar chart via Recharts)
     - bp_trend: show BP trend line
     - weight_trend: show weight trend line
     - fluid_deficit: show fluid balance daily bars
     - Use small inline Recharts components (200px height) for drill-down visualizations

2. Create `src/components/insight-badge.tsx`:
   - Uses `useInsights` with a default 7d range to get current insights
   - Filters out dismissed insights using settings store
   - If no active insights: renders nothing (returns null)
   - If active insights exist: renders a compact badge showing count (e.g., "2 insights")
   - Badge is a small pill-shaped element with a subtle pulse animation for alerts
   - onClick navigates to /analytics?tab=insights using next/navigation
   - Severity coloring: takes the highest severity among active insights

3. Integrate insight badge into main dashboard (`src/app/page.tsx`):
   - Add InsightBadge component near the top of the dashboard, below the header area
   - Only renders when there are active insights (component handles this internally)
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Insights tab shows auto-generated insights sorted by severity with dismiss functionality. Drill-down charts appear for each insight type. Dashboard shows compact badge when insights are active. Build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- Insights tab renders insight banners sorted by severity
- Dismissing an insight persists to localStorage via Zustand
- Dismissed insights reappear when underlying value changes by >10%
- Dashboard badge shows insight count and navigates to /analytics?tab=insights
</verification>

<success_criteria>
- Insights tab auto-generates insights from cross-query analysis
- Insight banners show severity, title, description, and dismiss button
- Dismissed insights persist across sessions and reappear on condition change
- Dashboard badge provides at-a-glance insight awareness
</success_criteria>

<output>
After completion, create `.planning/phases/04-analytics-service/04-06-SUMMARY.md`
</output>
