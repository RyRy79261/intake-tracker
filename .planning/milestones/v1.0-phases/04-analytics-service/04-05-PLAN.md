---
phase: 04-analytics-service
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - src/app/analytics/page.tsx
  - src/components/analytics/time-range-selector.tsx
  - src/components/analytics/records-tab.tsx
  - src/components/app-header.tsx
  - src/app/history/page.tsx
autonomous: true
requirements: [SRVC-05]

must_haves:
  truths:
    - "New /analytics route exists and renders a tabbed page with Records tab as default"
    - "Navigation shows 'Analytics' tab instead of 'History'"
    - "Shared time range selector at top applies across all tabs"
    - "Records tab preserves all functionality from /history (edit, delete, domain filtering)"
    - "/history route redirects to /analytics"
  artifacts:
    - path: "src/app/analytics/page.tsx"
      provides: "Analytics page with tabbed layout"
    - path: "src/components/analytics/time-range-selector.tsx"
      provides: "Shared time range picker with presets and custom date range"
    - path: "src/components/analytics/records-tab.tsx"
      provides: "Records browsing tab (migrated from /history)"
    - path: "src/components/app-header.tsx"
      provides: "Updated navigation with Analytics tab"
  key_links:
    - from: "src/app/analytics/page.tsx"
      to: "src/components/analytics/time-range-selector.tsx"
      via: "shared state prop"
      pattern: "TimeRangeSelector"
    - from: "src/components/analytics/records-tab.tsx"
      to: "src/hooks/use-analytics-queries.ts"
      via: "hook imports"
      pattern: "use.*queries"
---

<objective>
Create the /analytics page shell with tabbed layout, shared time range selector, and Records tab (migrated from /history). Update navigation to point to Analytics instead of History.

Purpose: This is the main analytics UI container. Records tab preserves existing record browsing capability while the shell enables Insights, Correlations, and Titration tabs to be added in subsequent plans.
Output: /analytics page, time range selector, records tab, updated nav
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics-service/04-CONTEXT.md
@.planning/phases/04-analytics-service/04-03-SUMMARY.md

@src/app/history/page.tsx
@src/components/app-header.tsx
@src/hooks/use-history-queries.ts
@src/components/history-drawer.tsx
@src/components/history/record-row.tsx
@src/lib/history-types.ts

<interfaces>
From src/hooks/use-analytics-queries.ts (created in Plan 03):
```typescript
export function useTimeScopeRange(scope: TimeScope): TimeRange
export function useFluidBalance(range: TimeRange): AnalyticsResult<FluidBalanceResult>
// etc.
```

From src/lib/analytics-types.ts (created in Plan 01):
```typescript
export type TimeScope = '24h' | '7d' | '30d' | '90d' | 'all';
export interface TimeRange { start: number; end: number; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics page shell with time range selector</name>
  <files>src/app/analytics/page.tsx, src/components/analytics/time-range-selector.tsx, src/components/app-header.tsx, src/app/history/page.tsx</files>
  <action>
1. Create `src/components/analytics/time-range-selector.tsx`:
   - Props: `scope: TimeScope`, `onScopeChange: (scope: TimeScope) => void`, `customRange: TimeRange | null`, `onCustomRangeChange: (range: TimeRange | null) => void`
   - Render preset scope buttons: 24h, 7d, 30d, 90d, All -- as a horizontal button group (pill-shaped toggle buttons)
   - Active scope highlighted with primary color
   - "Custom" button that when selected shows a date range picker (two date inputs: start date, end date)
   - When custom is selected, preset buttons are deselected
   - Mobile-first: compact horizontal layout, fits within max-w-lg
   - Use Tailwind classes consistent with existing app styling

2. Create `src/app/analytics/page.tsx`:
   - "use client" directive
   - State: `activeTab` ('records' | 'insights' | 'correlations' | 'titration'), default 'records'
   - State: `scope` (TimeScope), default '7d'
   - State: `customRange` (TimeRange | null), default null
   - Compute effective range: if customRange set, use it; otherwise use useTimeScopeRange(scope)
   - Layout: TimeRangeSelector at top, then tab bar, then tab content
   - Tab bar: 4 tabs using shadcn Tabs component -- Records, Insights, Correlations, Titration
   - For now: Records tab renders RecordsTab component (next task), other tabs render placeholder "Coming soon" text
   - Pass the effective TimeRange to the active tab component
   - Mobile-first layout: max-w-lg mx-auto, padding consistent with other pages

3. Update `src/components/app-header.tsx`:
   - Change the History nav item: path from "/history" to "/analytics", label from "History" to "Analytics"
   - Change icon from `History` to `BarChart3` (or `TrendingUp`) from lucide-react
   - Keep all other nav items unchanged

4. Update `src/app/history/page.tsx`:
   - Replace the entire page with a redirect to /analytics
   - Use `redirect('/analytics')` from next/navigation at the top of the component
   - This ensures any bookmarks or saved links still work
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && npx tsc --noEmit src/app/analytics/page.tsx src/components/analytics/time-range-selector.tsx src/components/app-header.tsx 2>&1 | head -10</automated>
  </verify>
  <done>/analytics page renders with time range selector and tab bar. Nav shows "Analytics" instead of "History". /history redirects to /analytics. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Records tab migrated from /history</name>
  <files>src/components/analytics/records-tab.tsx</files>
  <action>
Create `src/components/analytics/records-tab.tsx`:

1. Props: `range: TimeRange`

2. Migrate the core record browsing functionality from the current /history page:
   - Use `useLiveQuery` to fetch records across all domains within the given TimeRange (water, salt, weight, BP, eating, urination, defecation, caffeine, alcohol)
   - Call individual domain services' `getXByDateRange(range.start, range.end)` functions inside useLiveQuery
   - Also call `getSubstanceRecordsByDateRange(range.start, range.end)` for substance records

3. UI features (migrated from /history, adapted):
   - Domain filter: buttons or chips to filter by record type (All, Water, Salt, BP, Weight, Eating, Urination, Defecation, Caffeine, Alcohol)
   - Unified timeline view: all records sorted by timestamp descending
   - Each record row shows: timestamp, type icon, value, note (if any)
   - Edit/delete functionality: existing edit drawers/modals should work for health records
   - Substance records: show description, amount (mg or drinks), delete button
   - Empty state: "No records in this time range"

4. Reuse existing components where possible:
   - `record-row.tsx` from `src/components/history/` if compatible
   - `history-drawer.tsx` for edit functionality
   - Adapt as needed for the new time range prop (was previously loading all records)

5. Default empty array for useLiveQuery (no loading states, per Phase 3 pattern).
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Records tab displays all domain records within selected time range. Domain filter allows type-specific browsing. Edit/delete functionality preserved from /history. Substance records included. Build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- /analytics page loads with tab bar showing Records, Insights, Correlations, Titration
- Time range selector shows preset scopes + custom date range option
- Records tab shows records filtered by selected time range
- Navigation shows "Analytics" tab, /history redirects to /analytics
</verification>

<success_criteria>
- /analytics route replaces /history with tabbed layout
- Time range selector is shared across all tabs
- Records tab preserves all /history functionality (view, edit, delete records)
- Records tab includes substance records (caffeine/alcohol)
- Navigation updated from "History" to "Analytics"
</success_criteria>

<output>
After completion, create `.planning/phases/04-analytics-service/04-05-SUMMARY.md`
</output>
