---
phase: 04-analytics-service
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/lib/medication-service.ts
autonomous: true
gap_closure: true
requirements:
  - SRVC-05

must_haves:
  truths:
    - "Titration tab loads without IndexedDB error"
    - "All 4 analytics tabs switch content when tapped"
    - "Prescriptions can be queried by createdAt"
  artifacts:
    - path: "src/lib/db.ts"
      provides: "Dexie v13 with createdAt index on prescriptions"
      contains: "db.version(13)"
    - path: "src/lib/medication-service.ts"
      provides: "getPrescriptions query works against indexed field"
  key_links:
    - from: "src/lib/medication-service.ts"
      to: "src/lib/db.ts"
      via: "orderBy('createdAt') on prescriptions table"
      pattern: "orderBy.*createdAt"
---

<objective>
Fix the blocker preventing the Titration tab from loading: prescriptions table is missing a `createdAt` Dexie index, causing `getPrescriptions()` to throw "KeyPath createdAt on object store prescriptions is not indexed".

Purpose: Unblocks titration tab and analytics tab switching (UAT tests 3 and 12).
Output: Dexie v13 migration adding createdAt index to prescriptions store.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/db.ts
@src/lib/medication-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dexie v13 migration with createdAt index on prescriptions</name>
  <files>src/lib/db.ts</files>
  <action>
Add a new `db.version(13).stores({...})` block after the existing v12 block. Copy ALL existing store definitions from v12 exactly, then change the prescriptions line from:

```
prescriptions: "id, isActive, updatedAt"
```

to:

```
prescriptions: "id, isActive, updatedAt, createdAt"
```

No upgrade function needed -- Dexie auto-creates the index from existing data. All other store definitions remain identical to v12.

IMPORTANT per CLAUDE.md: "When adding a new Dexie version, you must repeat all existing store definitions (Dexie requires the full schema each version)."
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>db.version(13) exists with prescriptions store including createdAt index. All other stores unchanged. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify getPrescriptions orderBy works and titration tab loads</name>
  <files>src/lib/medication-service.ts</files>
  <action>
Verify that `getPrescriptions()` at line 210 uses `.orderBy("createdAt")` -- this should now work with the new index. No code change needed in medication-service.ts unless the function signature has changed.

Run the dev server and confirm no console errors on the analytics page titration tab. Run lint to ensure no regressions.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <done>getPrescriptions() orderBy('createdAt') resolves against the new index. Lint passes. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes without errors
- `pnpm lint` passes without errors
- db.ts contains `db.version(13)` with `prescriptions: "id, isActive, updatedAt, createdAt"`
- No other store definitions changed between v12 and v13
</verification>

<success_criteria>
The Dexie prescriptions table has a createdAt index, eliminating the "KeyPath createdAt is not indexed" error. The titration tab loads successfully and all 4 analytics tabs are switchable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-analytics-service/04-09-SUMMARY.md`
</output>
