---
phase: 16-dashboard-cleanup-and-text-metrics
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/components/food-calculator.tsx (DELETE)
  - src/components/eating-card.tsx (DELETE)
  - src/components/voice-input.tsx (DELETE)
  - src/components/parsed-intake-display.tsx (DELETE)
  - src/components/historical-graph.tsx (DELETE)
  - src/components/charts/intake-chart.tsx (DELETE)
  - src/components/charts/weight-chart.tsx (DELETE)
  - src/components/charts/bp-chart.tsx (DELETE)
  - src/components/charts/metrics-section.tsx (DELETE)
  - src/components/charts/chart-utils.ts (DELETE)
  - src/components/intake-card.tsx (DELETE)
  - src/hooks/use-graph-data.ts (DELETE)
  - src/hooks/use-now.ts (DELETE)
  - src/components/quick-nav-footer.tsx
  - src/lib/card-themes.ts
  - src/lib/constants.ts
  - src/stores/settings-store.ts
autonomous: true
requirements:
  - DASH-07
must_haves:
  truths:
    - "Food calculator is fully removed from the codebase with no remaining references"
    - "All replaced/unused UI components are deleted"
    - "No dead imports or references to deleted files remain"
    - "Build compiles successfully after all deletions"
    - "E2E tests pass after cleanup"
  artifacts:
    - path: "src/components/quick-nav-footer.tsx"
      provides: "Cleaned QuickNavFooter without utility row props"
    - path: "src/lib/card-themes.ts"
      provides: "Card themes without UTILITY_THEMES"
    - path: "src/lib/constants.ts"
      provides: "Constants without COFFEE_PRESETS"
    - path: "src/stores/settings-store.ts"
      provides: "Settings store without coffeeDefaultType field"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/text-metrics.tsx"
      via: "TextMetrics replaces HistoricalGraph"
      pattern: "import.*TextMetrics"
    - from: "src/components/quick-nav-footer.tsx"
      to: "src/lib/card-themes.ts"
      via: "CARD_THEMES only (no UTILITY_THEMES)"
      pattern: "CARD_THEMES"
---

<objective>
Delete all replaced/unused UI components, remove dead code references, and clean up constants, themes, and settings store fields.

Purpose: Remove significant technical debt from prior phases. food-calculator.tsx, eating-card.tsx, voice-input.tsx, parsed-intake-display.tsx, historical-graph.tsx, all chart sub-components, and intake-card.tsx are fully replaced by Phase 14/15/16 components. Cleaning up ensures no confusion about which components are active.
Output: 13 files deleted, 4 files cleaned up. Zero dead code from old intake UI.
</objective>

<execution_context>
@/home/ryan/repos/Personal/intake-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/repos/Personal/intake-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-cleanup-and-text-metrics/16-CONTEXT.md
@.planning/phases/16-dashboard-cleanup-and-text-metrics/16-RESEARCH.md
@.planning/phases/16-dashboard-cleanup-and-text-metrics/16-02-SUMMARY.md

<interfaces>
<!-- Dead code reference map from RESEARCH.md verification. -->

Files to DELETE and their import chains:
- food-calculator.tsx: only referenced by QuickNavFooter (optional callback prop)
- eating-card.tsx: zero external references
- voice-input.tsx: only referenced by QuickNavFooter (optional callback prop)
- parsed-intake-display.tsx: only imported by voice-input.tsx (deleted together)
- historical-graph.tsx: only in page.tsx (already removed in Plan 02)
- charts/intake-chart.tsx: only in historical-graph.tsx
- charts/weight-chart.tsx: only in historical-graph.tsx
- charts/bp-chart.tsx: only in historical-graph.tsx
- charts/metrics-section.tsx: only in historical-graph.tsx
- charts/chart-utils.ts: only in chart components
- intake-card.tsx: zero external references
- hooks/use-graph-data.ts: only in chart components
- hooks/use-now.ts: only in historical-graph.tsx

Constants/themes to clean:
- COFFEE_PRESETS in constants.ts: only in intake-card.tsx (deleted) + customization-panel.tsx (replaced in Plan 02)
- UTILITY_THEMES in card-themes.ts: only in quick-nav-footer.tsx
- coffeeDefaultType in settings-store.ts: only in intake-card.tsx (deleted) + customization-panel.tsx (replaced)
- setCoffeeDefaultType in settings-store.ts: same consumers

Still-alive references (do NOT delete):
- getLiquidTypeLabel in utils.ts: used by water-tab.tsx and history/record-row.tsx
- recharts: used by analytics page components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete replaced component files</name>
  <files>src/components/food-calculator.tsx, src/components/eating-card.tsx, src/components/voice-input.tsx, src/components/parsed-intake-display.tsx, src/components/historical-graph.tsx, src/components/charts/intake-chart.tsx, src/components/charts/weight-chart.tsx, src/components/charts/bp-chart.tsx, src/components/charts/metrics-section.tsx, src/components/charts/chart-utils.ts, src/components/intake-card.tsx, src/hooks/use-graph-data.ts, src/hooks/use-now.ts</files>
  <read_first>src/app/page.tsx, src/components/quick-nav-footer.tsx</read_first>
  <action>
    **Per D-05: Full removal of all replaced/unused UI components.**

    Delete the following 13 files:
    ```
    rm src/components/food-calculator.tsx
    rm src/components/eating-card.tsx
    rm src/components/voice-input.tsx
    rm src/components/parsed-intake-display.tsx
    rm src/components/historical-graph.tsx
    rm src/components/charts/intake-chart.tsx
    rm src/components/charts/weight-chart.tsx
    rm src/components/charts/bp-chart.tsx
    rm src/components/charts/metrics-section.tsx
    rm src/components/charts/chart-utils.ts
    rm src/components/intake-card.tsx
    rm src/hooks/use-graph-data.ts
    rm src/hooks/use-now.ts
    ```

    After deletion, remove the empty `src/components/charts/` directory:
    ```
    rmdir src/components/charts/
    ```

    Run `pnpm build` to verify no orphan imports remain. The following should already be clean from Plan 02:
    - page.tsx: HistoricalGraph import already removed
    - customization-panel.tsx: COFFEE_PRESETS import already removed

    If build fails, fix any remaining references by removing the import lines. Expected clean files:
    - `page.tsx` should have no references to any deleted file (verified in Plan 02)
    - `quick-nav-footer.tsx` references are via optional callback props (onOpenFoodCalculator, onOpenVoiceInput) which are never passed -- but the TYPE imports may reference deleted files. Check and clean.

    IMPORTANT: Do NOT delete:
    - `recharts` package (still used by analytics page)
    - `getLiquidTypeLabel` in utils.ts (still used by water-tab.tsx and record-row.tsx)
    - Any analytics chart components in `src/components/analytics/`
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ! test -f src/components/food-calculator.tsx
    - ! test -f src/components/eating-card.tsx
    - ! test -f src/components/voice-input.tsx
    - ! test -f src/components/parsed-intake-display.tsx
    - ! test -f src/components/historical-graph.tsx
    - ! test -f src/components/charts/intake-chart.tsx
    - ! test -f src/components/charts/weight-chart.tsx
    - ! test -f src/components/charts/bp-chart.tsx
    - ! test -f src/components/charts/metrics-section.tsx
    - ! test -f src/components/charts/chart-utils.ts
    - ! test -f src/components/intake-card.tsx
    - ! test -f src/hooks/use-graph-data.ts
    - ! test -f src/hooks/use-now.ts
    - pnpm build exits 0
  </acceptance_criteria>
  <done>All 13 replaced component/hook files deleted. Charts directory removed. Build compiles successfully with no orphan import references.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up dead constants, themes, settings fields, and QuickNavFooter props</name>
  <files>src/lib/constants.ts, src/lib/card-themes.ts, src/stores/settings-store.ts, src/components/quick-nav-footer.tsx</files>
  <read_first>src/lib/constants.ts, src/lib/card-themes.ts, src/stores/settings-store.ts, src/components/quick-nav-footer.tsx</read_first>
  <action>
    **Step 1: Remove COFFEE_PRESETS from constants.ts**

    Delete the `CoffeePreset` interface and `COFFEE_PRESETS` constant (lines ~82-94 in current file). These were only used by the now-deleted intake-card.tsx and the replaced Coffee tab in customization-panel.tsx. Verify no remaining imports with grep before removing.

    Also check and remove `LIQUID_TYPE_OPTIONS` if no longer imported anywhere (it was used by intake-card.tsx which is deleted).

    **Step 2: Remove UTILITY_THEMES from card-themes.ts**

    The `UTILITY_THEMES` object is only imported by quick-nav-footer.tsx. After removing the utility row from QuickNavFooter (Step 3), UTILITY_THEMES has zero consumers. Delete the UTILITY_THEMES export and its associated icon imports (if icons are only used by UTILITY_THEMES). Also remove the `CardThemeKey` type export if it only included UTILITY_THEMES keys.

    Verify the CardThemeKey type -- if it covers CARD_THEMES keys, keep it. If it was specifically for UTILITY_THEMES, remove.

    **Step 3: Clean up QuickNavFooter props**

    Remove from the interface and component:
    - `onOpenFoodCalculator?: () => void` prop
    - `onOpenVoiceInput?: () => void` prop
    - `utilityOrder` prop (only used for utility row ordering)
    - The entire utility row rendering logic: `showUtilityRow` check, `utilityLeft`/`utilityRight` variables, `onClickLeft`/`onClickRight` variables, and the utility row JSX
    - The `FOOD_ITEM` and `AI_ITEM` constants at module scope
    - Remove `UTILITY_THEMES` import from card-themes

    After cleanup, also update page.tsx to remove the `utilityOrder` prop from the QuickNavFooter usage (it currently passes `utilityOrder={settings.utilityOrder}`). If `utilityOrder` is removed from the component, remove it from the caller.

    Also remove `utilityOrder` from the Settings interface and store if it has no other consumers:
    - Check: grep for `utilityOrder` across src/ -- if only in settings-store.ts, quick-nav-footer.tsx, and page.tsx, remove from all three.

    **Step 4: Remove coffeeDefaultType from settings store (completing D-07)**

    Now that intake-card.tsx and the old Coffee tab are both deleted:
    - Remove `coffeeDefaultType: string` from the Settings interface
    - Remove `coffeeDefaultType: "double-espresso"` from defaultSettings
    - Remove `setCoffeeDefaultType` from SettingsActions interface
    - Remove `setCoffeeDefaultType` action implementation
    - The migration in persist config already handles removal from localStorage (added in Plan 01)

    **Step 5: Build verification**

    Run `pnpm build && pnpm lint -- --no-warn-ignored` to confirm all cleanup is complete.

    Run `pnpm test:e2e` to verify E2E tests still pass (section IDs preserved from Plan 02).

    Run `pnpm test` to verify unit tests still pass.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm lint -- --no-warn-ignored && pnpm build 2>&1 | tail -5 && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -qv 'COFFEE_PRESETS' src/lib/constants.ts (no COFFEE_PRESETS export)
    - grep -qv 'CoffeePreset' src/lib/constants.ts (no CoffeePreset interface)
    - grep -qv 'UTILITY_THEMES' src/lib/card-themes.ts (no UTILITY_THEMES export)
    - grep -qv 'onOpenFoodCalculator' src/components/quick-nav-footer.tsx
    - grep -qv 'onOpenVoiceInput' src/components/quick-nav-footer.tsx
    - grep -qv 'coffeeDefaultType' src/stores/settings-store.ts (fully removed from interface)
    - grep -qv 'setCoffeeDefaultType' src/stores/settings-store.ts
    - pnpm build exits 0
    - pnpm lint exits 0
    - pnpm test exits 0
  </acceptance_criteria>
  <done>COFFEE_PRESETS constant removed. UTILITY_THEMES removed. QuickNavFooter utility row props and logic removed. coffeeDefaultType and setCoffeeDefaultType fully removed from settings store. All dead code cleaned up. Build, lint, and tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes (no TypeScript errors, no orphan imports)
- `pnpm lint -- --no-warn-ignored` passes
- `pnpm test` passes (unit tests)
- `pnpm test:e2e` passes (section IDs preserved, page loads correctly)
- No file in `src/` imports from any deleted file (verified by build success)
- Grep for deleted component names across src/ returns zero results
</verification>

<success_criteria>
- 13 dead files deleted: food-calculator, eating-card, voice-input, parsed-intake-display, historical-graph, 5 chart files, intake-card, use-graph-data, use-now
- charts/ directory removed
- COFFEE_PRESETS, CoffeePreset, UTILITY_THEMES, coffeeDefaultType, setCoffeeDefaultType all removed
- QuickNavFooter cleaned of utility row logic
- Zero dead code references remain
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-cleanup-and-text-metrics/16-03-SUMMARY.md`
</output>
