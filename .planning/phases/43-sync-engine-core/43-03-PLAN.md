---
phase: 43-sync-engine-core
plan: 03
type: execute
wave: 1
depends_on:
  - 43-01
files_modified:
  - src/lib/sync-payload.ts
  - src/app/api/sync/push/route.ts
  - src/__tests__/sync-push-route.test.ts
autonomous: true
requirements:
  - SYNC-02
  - SYNC-04
tags:
  - sync
  - api
  - security
  - lww

must_haves:
  truths:
    - "POST /api/sync/push requires a valid Neon Auth session (via withAuth)"
    - "Push body is validated by a drizzle-zod discriminated union keyed by tableName"
    - "Server derives userId from auth — never trusts a client-supplied userId"
    - "LWW: client op with newer updatedAt overwrites server row; exact tie → server wins; non-null deletedAt on either side → deleted wins"
    - "Client updatedAt clamped to min(client.updatedAt, serverNow + 60s) before comparison"
    - "Response returns { accepted: [{queueId, serverUpdatedAt}, ...] } for every op acknowledged"
    - "Batch capped at 500 ops via Zod max(500) to block DoS payloads"
  artifacts:
    - path: src/lib/sync-payload.ts
      provides: "drizzle-zod discriminated union for push body; PushOp and PushBody types"
      exports:
        - pushBodySchema
        - PushOp
        - PushBody
        - schemaByTableName
      min_lines: 40
    - path: src/app/api/sync/push/route.ts
      provides: "POST handler with withAuth + LWW merge + server-authoritative ack"
      exports:
        - POST
      contains: "withAuth"
    - path: src/__tests__/sync-push-route.test.ts
      provides: "Server-side LWW unit coverage"
      contains: "sync-push-route"
  key_links:
    - from: src/app/api/sync/push/route.ts
      to: src/lib/auth-middleware.ts
      via: "withAuth HOF wraps the POST export (established Phase 41 pattern)"
      pattern: "withAuth\\(async"
    - from: src/app/api/sync/push/route.ts
      to: src/lib/sync-payload.ts
      via: "pushBodySchema.safeParse(body) before any DB write"
      pattern: "pushBodySchema\\.safeParse"
    - from: src/lib/sync-payload.ts
      to: src/db/schema.ts
      via: "createInsertSchema(table).omit({userId: true}) per table"
      pattern: "createInsertSchema"
---

<objective>
Create the push-side sync API: `src/lib/sync-payload.ts` (drizzle-zod discriminated union for a heterogeneous batch push body) + `src/app/api/sync/push/route.ts` (auth-guarded POST handler implementing LWW merge with server-authoritative ack, clock-skew clamp, and `deletedAt`-wins-tie rule).

Purpose: D-05/D-06 lock the push route's shape; D-12 locks the conflict matrix; security research requires `.omit({userId: true})` to prevent user-id forgery and `z.array().max(500)` to prevent DoS payloads.

Output: two new source files + populated Plan 01 stub test (7 passing assertions covering LWW rules, tie rules, clamp, security, and DoS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/43-sync-engine-core/43-CONTEXT.md
@.planning/phases/43-sync-engine-core/43-RESEARCH.md
@.planning/phases/43-sync-engine-core/43-PATTERNS.md
@.planning/phases/43-sync-engine-core/43-VALIDATION.md
@src/db/schema.ts
@src/app/api/push/subscribe/route.ts
@src/lib/auth-middleware.ts
@src/lib/push-db.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/lib/sync-payload.ts — drizzle-zod discriminated union for push body</name>
  <read_first>
    - src/db/schema.ts (Phase 42 Drizzle source of truth — all 16 table exports, FK shape, userId column)
    - 43-RESEARCH.md section "Pattern 6: drizzle-zod Discriminated Union for Push Payload"
    - 43-PATTERNS.md section "Apply to push route"
    - 43-CONTEXT.md D-06 (full-row payload, drizzle-zod validation)
  </read_first>
  <behavior>
    - pushBodySchema.safeParse({ ops: [] }) succeeds (empty batch allowed)
    - pushBodySchema.safeParse({ ops: [500 valid ops] }) succeeds
    - pushBodySchema.safeParse({ ops: [501 valid ops] }) fails with a DoS-cap error
    - When a caller passes a row with userId set, the parsed output drops userId (via .omit)
    - Unknown tableName fails the discriminated-union parse with a clear error
    - schemaByTableName maps every tableName to its Drizzle table reference
  </behavior>
  <files>src/lib/sync-payload.ts</files>
  <action>
    Create src/lib/sync-payload.ts. The module must:

    1. Import `z` from "zod", `createInsertSchema` from "drizzle-zod", and `* as schema` from "@/db/schema".
    2. Declare one per-table row schema for every data table by calling `createInsertSchema(schema.X).omit({ userId: true })`. Cover all 16 tables listed in 43-PATTERNS.md TABLE_PUSH_ORDER: intakeRecords, weightRecords, bloodPressureRecords, eatingRecords, urinationRecords, defecationRecords, prescriptions, medicationPhases, phaseSchedules, inventoryItems, inventoryTransactions, doseLogs, dailyNotes, auditLogs, substanceRecords, titrationPlans.
    3. Declare a base op shape: `{ queueId: z.number().int(), op: z.enum(["upsert", "delete"]) }`. Every member of the discriminated union spreads this base and adds `tableName: z.literal("...")` and `row: <perTableSchema>`.
    4. Export `pushBodySchema = z.object({ ops: z.array(opSchema).max(500) })`.
    5. Export types `PushOp = z.infer<typeof opSchema>`, `PushBody = z.infer<typeof pushBodySchema>`, and `TableName = PushOp["tableName"]`.
    6. Export `schemaByTableName` — a const object mapping every tableName literal to its Drizzle table reference (`schema.intakeRecords`, etc.). Use `as const` for literal narrowing.
    7. Top-of-file doc comment cites D-06, D-12, and the security threat model row "User A's client sends ops for User B's records" as rationale for the .omit({userId: true}).

    **Export-name resolution:** if src/db/schema.ts uses different symbol names than the plural names above (e.g. `intakeRecordsTable`), grep schema.ts and use the actual exported symbols — do NOT invent names.

    **Edge case:** if a table has no `userId` column (unlikely per Phase 42 SCHEMA-02 but auditLogs is a candidate if Phase 42 treated it differently), drop the `.omit({userId: true})` call for that table only and leave an inline comment like `// no userId column on auditLogs per Phase 42 schema`.

    Expected file length: ~60-80 lines.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep "error TS" | grep "sync-payload" | wc -l | awk '{ if ($1==0) print "ok"; else print "fail" }' | grep ok && test $(grep -c "createInsertSchema" src/lib/sync-payload.ts) -ge 14 && grep "ops: z.array(opSchema).max(500)" src/lib/sync-payload.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` produces zero errors mentioning sync-payload.ts
    - `grep -c "createInsertSchema" src/lib/sync-payload.ts` returns at least 14 (allows up to 2 tables without userId column)
    - `grep -c "z.literal(" src/lib/sync-payload.ts` returns exactly 16 (one per data table)
    - `grep "ops: z.array(opSchema).max(500)" src/lib/sync-payload.ts` matches
    - `grep "export const pushBodySchema" src/lib/sync-payload.ts` matches
    - `grep "export const schemaByTableName" src/lib/sync-payload.ts` matches
    - `grep "export type PushOp" src/lib/sync-payload.ts` matches
  </acceptance_criteria>
  <done>sync-payload.ts compiles cleanly, exports all required schemas/types, and discriminated union covers all 16 tables</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement POST /api/sync/push route with LWW merge + ack + populate sync-push-route.test.ts</name>
  <read_first>
    - src/app/api/push/subscribe/route.ts (exact template — withAuth + safeParse + NextResponse + try/catch + [module] log prefix)
    - src/lib/auth-middleware.ts (withAuth signature; auth.userId! non-null convention)
    - src/db/schema.ts (actual Drizzle exports; find the drizzle client helper, typically src/lib/drizzle.ts)
    - 43-RESEARCH.md "Example 2: Server-side LWW merge in /api/sync/push"
    - 43-RESEARCH.md Pattern 9 (clock skew: MAX_FUTURE_MS = 60_000)
    - 43-CONTEXT.md D-12 (conflict precedence order)
    - 43-VALIDATION.md row 8–13 (the 7 required test names for sync-push-route.test.ts)
    - Existing sync-push-route.test.ts stub from Plan 01 (replace the MISSING test; convert todo entries to real tests)
  </read_first>
  <behavior>
    - Handler is `export const POST = withAuth(async ({ request, auth }) => ...)` — matches Phase 41 pattern
    - Unauthenticated request → 401 (delegated to withAuth)
    - Invalid body → 400 with `{ error, details: parsed.error.flatten() }`
    - Body with `ops.length > 500` → 400
    - For each op, server computes `clampedUpdatedAt = Math.min(op.row.updatedAt, Date.now() + 60_000)`
    - LWW rules (in order):
      1. If existing server row has non-null deletedAt AND incoming row has null deletedAt → skip write, ack with existing.updatedAt (deleted wins)
      2. Else if no existing row OR clampedUpdatedAt > existing.updatedAt → upsert via `onConflictDoUpdate` (strict greater-than, so exact tie keeps server row)
      3. Else → skip write, ack with existing.updatedAt
    - Every write sets `userId: auth.userId!` — NEVER uses any userId from the op.row
    - Every Drizzle SELECT includes `and(eq(table.id, op.row.id), eq(table.userId, auth.userId!))`
    - Response body: `{ accepted: Array<{queueId: number; serverUpdatedAt: number}> }` — one entry per op processed
    - No console.log of request body (PHI avoidance); errors logged with "[sync/push]" prefix only
  </behavior>
  <files>src/app/api/sync/push/route.ts, src/__tests__/sync-push-route.test.ts</files>
  <action>
    **Part A — Create src/app/api/sync/push/route.ts**

    Structure (mirrors src/app/api/push/subscribe/route.ts):

    1. Imports: `NextResponse` from "next/server"; `withAuth` from "@/lib/auth-middleware"; `pushBodySchema, schemaByTableName, PushOp, TableName` from "@/lib/sync-payload"; `eq, and` from "drizzle-orm"; the Drizzle client (resolve path by grepping `src/lib/` for `drizzle(neon(` or reuse Phase 42's helper; common path is `@/lib/drizzle` or similar).
    2. Module constant: `const MAX_FUTURE_MS = 60_000;`
    3. POST handler body:
       - Parse body with `pushBodySchema.safeParse(await request.json())`. On failure return 400 with `{ error: "Invalid request", details: parsed.error.flatten() }`.
       - Capture `const serverNow = Date.now();`
       - Build `const accepted: Array<{ queueId: number; serverUpdatedAt: number }> = [];`
       - Loop `for (const op of parsed.data.ops) { ... }`:
         a. `const table = schemaByTableName[op.tableName as TableName];`
         b. `const clampedUpdatedAt = Math.min(op.row.updatedAt, serverNow + MAX_FUTURE_MS);`
         c. Fetch existing: `const existing = await drizzleDb.select().from(table).where(and(eq(table.id, op.row.id), eq(table.userId, auth.userId!))).limit(1);` → `const serverRow = existing[0];`
         d. D-12 step 3 (deleted wins): if `serverRow && serverRow.deletedAt != null && op.row.deletedAt == null` → `accepted.push({ queueId: op.queueId, serverUpdatedAt: serverRow.updatedAt }); continue;`
         e. D-12 step 1+2 (strict >): if `!serverRow || clampedUpdatedAt > serverRow.updatedAt` → `await drizzleDb.insert(table).values({ ...op.row, userId: auth.userId!, updatedAt: clampedUpdatedAt }).onConflictDoUpdate({ target: table.id, set: { ...op.row, userId: auth.userId!, updatedAt: clampedUpdatedAt } });`
         f. Ack: `accepted.push({ queueId: op.queueId, serverUpdatedAt: clampedUpdatedAt });` (if we wrote) OR `accepted.push({ queueId: op.queueId, serverUpdatedAt: serverRow!.updatedAt });` (if we skipped because server row newer).
       - Return `NextResponse.json({ accepted });`
    4. Wrap in try/catch: on catch, `console.error("[sync/push] Error:", error);` then return `NextResponse.json({ error: "Failed to apply push batch" }, { status: 500 });`.

    **Important:** do NOT log request body. Do NOT return stack traces. Do NOT accept op.row.userId — only derive from `auth.userId!`.

    **Part B — Rewrite src/__tests__/sync-push-route.test.ts**

    Replace the MISSING stub with real tests. Strategy: invoke the route's POST handler directly with mocked `Request` and `withAuth` session context. Mock the Drizzle client by intercepting at the module boundary (vitest `vi.mock("@/lib/drizzle", ...)` or whatever the actual drizzle client module path is). Do NOT hit a real Neon branch in unit tests — Plan 08's E2E does that.

    Required tests (names MUST match 43-VALIDATION.md exactly so `-t "..."` commands work):

    - `it("LWW: newer client updatedAt wins over older server row")` — mock existing row with `updatedAt: 1000`, post op with `updatedAt: 2000`, assert insert called and accepted includes `{ queueId, serverUpdatedAt: 2000 }`.
    - `it("server wins tie: strict > comparison means equal updatedAt keeps server row")` — mock existing row with `updatedAt: 5000`, post op with `updatedAt: 5000`, assert insert NOT called and accepted includes `{ queueId, serverUpdatedAt: 5000 }`.
    - `it("deletedAt wins: non-null deletedAt on either side prevents resurrection")` — mock existing row with `deletedAt: 999, updatedAt: 1000`, post op with `deletedAt: null, updatedAt: 9999`, assert insert NOT called and accepted serverUpdatedAt === 1000 (server row preserved).
    - `it("clamp future: client updatedAt > serverNow+60s clamps to serverNow+60s")` — freeze `Date.now` at 1_000_000, post op with `updatedAt: 1_000_000 + 120_000` (2 min future), assert written row's updatedAt === 1_000_000 + 60_000.
    - `it("rejects client-forged userId")` — post op whose row JSON contains userId: "attacker" but the discriminated-union parse drops it (assert parsed ops[0].row.userId is undefined, OR assert the DB insert call's values.userId === auth.userId and not "attacker").
    - `it("rejects oversized batch")` — post body with 501 ops, expect 400 response and parse error mentioning max length 500.
    - `it("returns accepted array with serverUpdatedAt per queueId")` — post 3 ops, assert response.accepted is an array of length 3 with queueIds matching the request.

    Remove the MISSING `it` and all `it.todo` entries.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/sync-push-route.test.ts 2>&1 | tee /tmp/w1-push.log; grep -E "(7 passed|7 tests passed)" /tmp/w1-push.log && pnpm tsc --noEmit 2>&1 | grep "error TS" | grep -E "(sync/push|sync-push)" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/sync/push/route.ts exists and exports `POST`
    - `grep "withAuth" src/app/api/sync/push/route.ts` matches
    - `grep "pushBodySchema.safeParse" src/app/api/sync/push/route.ts` matches
    - `grep "MAX_FUTURE_MS" src/app/api/sync/push/route.ts` matches and the value is 60000
    - `grep "auth.userId!" src/app/api/sync/push/route.ts` appears in at least 3 lines (insert.values, and-clauses for select and update)
    - No occurrence of `op.row.userId` (server never trusts client userId): `grep -c "op.row.userId" src/app/api/sync/push/route.ts` returns 0
    - `grep -c "console.log" src/app/api/sync/push/route.ts` returns 0 (PHI avoidance)
    - sync-push-route.test.ts has 7 `it(...)` blocks matching the exact names above and 0 `it.todo` entries
    - `pnpm vitest run src/__tests__/sync-push-route.test.ts` → 7 passed, 0 failed
    - `pnpm tsc --noEmit` stays clean
  </acceptance_criteria>
  <done>Push route enforces LWW + tie rules + clamp + user_id scoping; all 7 unit tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/sync/push | Untrusted request body crosses here. All validation and user_id scoping happens server-side. |
| /api/sync/push → Postgres | Trusted side of the trust boundary; queries are parameterized via Drizzle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03-01 / T-sync-05 | Elevation of Privilege | push route body | mitigate | `.omit({userId: true})` on every createInsertSchema call. Server writes `auth.userId!` explicitly. Test "rejects client-forged userId" enforces at CI. |
| T-43-03-02 / T-sync-06 | Denial of Service | push route body size | mitigate | `z.array(opSchema).max(500)` caps ops. Test "rejects oversized batch" asserts 400 on 501 ops. Vercel function timeout is backstop. |
| T-43-03-03 / T-sync-01 | Tampering | LWW merge | mitigate | Strict `>` comparison (server wins tie) + `onConflictDoUpdate` idempotent replay + `deletedAt`-wins-tie rule. Three unit tests (LWW, server-wins-tie, deletedAt-wins) enforce. |
| T-43-03-04 / T-sync-04 | Tampering | client-set updatedAt | mitigate | `clampedUpdatedAt = Math.min(op.row.updatedAt, serverNow + 60_000)` before compare. Test "clamp future" enforces at CI. |
| T-43-03-05 | Information Disclosure | error responses | mitigate | try/catch returns generic 500 message; never returns error.message or error.stack. `[sync/push]` log prefix server-side only. |
| T-43-03-06 | Information Disclosure | PHI in logs | mitigate | Grep acceptance criterion forbids console.log in route. `console.error` logs only the error object, not request body. |
| T-43-03-07 | Tampering | SQL injection via tableName | mitigate | tableName is a Zod literal enum — finite set; cannot inject free strings. |
| T-43-03-08 | Elevation of Privilege | cross-user row access | mitigate | Every Drizzle SELECT/UPDATE includes `eq(table.userId, auth.userId!)`. Grep acceptance criterion requires ≥3 occurrences of `auth.userId!`. |

No high-severity residual risks. All six ASVS L1 controls in the Security Review Checklist from 43-RESEARCH.md are enforced by tests or grep gates.
</threat_model>

<verification>
- `pnpm vitest run src/__tests__/sync-push-route.test.ts` → 7 passed, 0 failed, 0 todo
- `pnpm tsc --noEmit` → 0 errors
- `grep -c "auth.userId!" src/app/api/sync/push/route.ts` ≥ 3
- `grep -c "op.row.userId" src/app/api/sync/push/route.ts` == 0
- `grep -c "console.log" src/app/api/sync/push/route.ts` == 0
- `grep "MAX_FUTURE_MS = 60" src/app/api/sync/push/route.ts` matches
- `grep "ops: z.array(opSchema).max(500)" src/lib/sync-payload.ts` matches
</verification>

<success_criteria>
- POST /api/sync/push validates batch, derives userId from session, applies record-level LWW with clamp + deletedAt-wins + server-wins-tie, and returns authoritative ack
- sync-payload.ts provides reusable pushBodySchema and schemaByTableName for Plan 04 (pull route) and Plan 06 (engine)
- All 7 SYNC-04 tests pass; all six ASVS L1 security controls enforced
</success_criteria>

<output>
After completion, create `.planning/phases/43-sync-engine-core/43-03-SUMMARY.md`
</output>
