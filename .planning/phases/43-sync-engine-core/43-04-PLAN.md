---
phase: 43-sync-engine-core
plan: 04
type: execute
wave: 2
depends_on:
  - 43-01
  - 43-03
files_modified:
  - src/app/api/sync/pull/route.ts
  - src/__tests__/sync-pull-route.test.ts
  - src/lib/sync-payload.ts
autonomous: true
requirements:
  - SYNC-03
tags:
  - sync
  - api
  - security
  - cursor

must_haves:
  truths:
    - "POST /api/sync/pull requires a valid Neon Auth session (via withAuth)"
    - "Pull body is validated by pullBodySchema — cursors keyed by tableName"
    - "Every SELECT scopes by eq(table.userId, auth.userId!) — no cross-user reads possible"
    - "Response includes tombstones (rows with non-null deletedAt)"
    - "Each table result is capped at PULL_SOFT_CAP (500) with hasMore flag"
    - "Response body includes serverTime (captured before query) so client can clamp cursor safely"
  artifacts:
    - path: src/app/api/sync/pull/route.ts
      provides: "POST handler with withAuth + cursor-paginated per-table SELECT + tombstones"
      exports:
        - POST
      contains: "withAuth"
    - path: src/lib/sync-payload.ts
      provides: "pullBodySchema + PullResponse type (added alongside existing push exports)"
      exports:
        - pullBodySchema
        - PullResponse
    - path: src/__tests__/sync-pull-route.test.ts
      provides: "Pull route unit coverage: cursor, user_id scope, tombstones, soft-cap, skew"
      contains: "sync-pull-route"
  key_links:
    - from: src/app/api/sync/pull/route.ts
      to: src/lib/auth-middleware.ts
      via: "withAuth HOF wraps the POST export"
      pattern: "withAuth\\(async"
    - from: src/app/api/sync/pull/route.ts
      to: src/lib/sync-payload.ts
      via: "pullBodySchema.safeParse(body) + schemaByTableName dispatch"
      pattern: "pullBodySchema|schemaByTableName"
---

<objective>
Create `src/app/api/sync/pull/route.ts` — an auth-guarded POST handler that returns per-table changes since a client-supplied cursor map, soft-capped at 500 rows/table with a hasMore flag and a serverTime anchor for race-safe cursor advancement. Add `pullBodySchema` and the corresponding response type to sync-payload.ts (co-locates with push schemas for audit).

Purpose: D-07/D-08 lock the pull route's shape. Pattern 7 in 43-RESEARCH.md mandates returning serverTime so the client can clamp its cursor to `serverTime - 30s` and avoid missing rows written mid-query. Security requires every SELECT scopes by session userId.

Output: new route file + extended sync-payload.ts + populated Plan 01 stub test (6 passing assertions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/43-sync-engine-core/43-CONTEXT.md
@.planning/phases/43-sync-engine-core/43-RESEARCH.md
@.planning/phases/43-sync-engine-core/43-PATTERNS.md
@.planning/phases/43-sync-engine-core/43-VALIDATION.md
@.planning/phases/43-sync-engine-core/43-03-PLAN.md
@src/app/api/sync/push/route.ts
@src/lib/sync-payload.ts
@src/db/schema.ts
@src/lib/auth-middleware.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend sync-payload.ts with pullBodySchema and PullResponse type</name>
  <read_first>
    - src/lib/sync-payload.ts (current state after Plan 03 — has pushBodySchema, schemaByTableName, TableName)
    - 43-CONTEXT.md D-07 (body shape: cursors keyed by tableName, response shape: per-table rows + hasMore)
    - 43-RESEARCH.md Pattern 7 (race mitigation — serverTime in response)
  </read_first>
  <behavior>
    - pullBodySchema.safeParse({ cursors: {} }) succeeds
    - pullBodySchema.safeParse({ cursors: { intakeRecords: 0, doseLogs: 1700000000000 } }) succeeds
    - pullBodySchema.safeParse({ cursors: { invalidTable: 0 } }) fails (tableName must be one of the 16 literals)
    - Cursor values below 0 fail validation
    - PullResponse type exposes `{ result: Record<TableName, { rows: unknown[]; hasMore: boolean }>, serverTime: number }`
  </behavior>
  <files>src/lib/sync-payload.ts</files>
  <action>
    Append (do NOT replace existing push-side exports) to src/lib/sync-payload.ts:

    1. Export `PULL_SOFT_CAP = 500` as a module constant (justified in 43-RESEARCH.md Pattern 7 + Pitfall 6).
    2. Build `tableNameSchema = z.enum([...16 literals...])` once and reuse (the existing opSchema already implies these literals; extract to a single enum for clarity).
    3. Export `pullBodySchema = z.object({ cursors: z.record(tableNameSchema, z.number().int().min(0)) })`.
    4. Export type `PullBody = z.infer<typeof pullBodySchema>`.
    5. Export type `PullResponse = { result: Record<TableName, { rows: Record<string, unknown>[]; hasMore: boolean }>; serverTime: number }`. This is a plain TS type, not a Zod schema — response typing only.

    Keep existing Plan 03 exports untouched. Use a section divider comment like `// --- Pull side (D-07, D-08) ---` before the new block.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep "error TS" | grep "sync-payload" | wc -l | grep -E "^0$" && grep "export const pullBodySchema" src/lib/sync-payload.ts && grep "PULL_SOFT_CAP = 500" src/lib/sync-payload.ts && grep "export type PullResponse" src/lib/sync-payload.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const pullBodySchema" src/lib/sync-payload.ts` matches
    - `grep "PULL_SOFT_CAP = 500" src/lib/sync-payload.ts` matches
    - `grep "export type PullResponse" src/lib/sync-payload.ts` matches
    - `grep "export const pushBodySchema" src/lib/sync-payload.ts` STILL matches (no regression on Plan 03)
    - `pnpm tsc --noEmit` clean (no errors mentioning sync-payload.ts)
  </acceptance_criteria>
  <done>sync-payload.ts provides a validated pullBodySchema, a constant PULL_SOFT_CAP, and a PullResponse type reusable by the pull route and by Plan 06's engine loop</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement POST /api/sync/pull route and populate sync-pull-route.test.ts</name>
  <read_first>
    - src/app/api/sync/push/route.ts (sibling route — same withAuth + drizzle client pattern)
    - src/app/api/push/subscribe/route.ts (error-handling template + log prefix convention)
    - 43-RESEARCH.md Pattern 7 (cursor race, server-time return, soft-cap implementation)
    - 43-RESEARCH.md Pattern 8 (tombstones included verbatim)
    - 43-CONTEXT.md D-07, D-08
    - 43-VALIDATION.md rows 14-16 (exact test names required)
    - src/__tests__/sync-pull-route.test.ts (Plan 01 stub — replace MISSING + todo entries with real tests)
  </read_first>
  <behavior>
    - Handler is `export const POST = withAuth(async ({ request, auth }) => ...)`
    - Unauthenticated → 401 (via withAuth)
    - Invalid body → 400 with `{ error: "Invalid request", details }`
    - `serverTime = Date.now()` captured BEFORE any query (race safety)
    - For each tableName in TABLE_PUSH_ORDER (reuse for consistency; pull order is less critical than push, but using the same order keeps docs simple):
      - `cursor = body.cursors[tableName] ?? 0`
      - SELECT with `WHERE user_id = auth.userId! AND updated_at > cursor ORDER BY updated_at ASC LIMIT 501`
      - If 501 returned → slice to 500 and set `hasMore: true`; else return all with `hasMore: false`
    - Tombstones (rows where `deletedAt != null`) are INCLUDED in rows — no filter
    - Response: `{ result: {tableName: {rows, hasMore}, ...}, serverTime }`
    - Error responses never leak drizzle stack traces; log prefix `[sync/pull]`
  </behavior>
  <files>src/app/api/sync/pull/route.ts, src/__tests__/sync-pull-route.test.ts</files>
  <action>
    **Part A — Create src/app/api/sync/pull/route.ts**

    1. Imports: `NextResponse` from "next/server"; `withAuth` from "@/lib/auth-middleware"; `pullBodySchema, schemaByTableName, PULL_SOFT_CAP, TableName` from "@/lib/sync-payload"; `TABLE_PUSH_ORDER` — note: Plan 05 will create sync-topology.ts with this export. Since Plan 04 runs in Wave 2 after Plan 05 (which is also Wave 2 but is an independent Wave-1 dep) — wait, check the dep: Plan 04 depends on Plans 01 and 03 only. sync-topology.ts lands in Plan 05 (Wave 2, parallel to Plan 04). To decouple: in THIS task, iterate `Object.keys(schemaByTableName)` directly — the order doesn't matter for pull since the client re-calls until every table reports `hasMore: false`. Cite this decision in a code comment referencing D-08.
    2. Import Drizzle client (same helper Plan 03 used) and `eq, and, gt, asc` from "drizzle-orm".
    3. POST handler body:
       - `const parsed = pullBodySchema.safeParse(await request.json()); if (!parsed.success) return NextResponse.json({ error: "Invalid request", details: parsed.error.flatten() }, { status: 400 });`
       - `const serverTime = Date.now();` (MUST be captured before any SELECT — race safety)
       - `const result: Record<string, { rows: unknown[]; hasMore: boolean }> = {};`
       - Loop over `(Object.keys(schemaByTableName) as TableName[])`:
         - `const cursor = parsed.data.cursors[tableName] ?? 0;`
         - `const table = schemaByTableName[tableName];`
         - `const rows = await drizzleDb.select().from(table).where(and(eq(table.userId, auth.userId!), gt(table.updatedAt, cursor))).orderBy(asc(table.updatedAt)).limit(PULL_SOFT_CAP + 1);`
         - `const hasMore = rows.length > PULL_SOFT_CAP;`
         - `result[tableName] = { rows: rows.slice(0, PULL_SOFT_CAP), hasMore };`
       - `return NextResponse.json({ result, serverTime });`
    4. Wrap in try/catch: `console.error("[sync/pull] Error:", error);` and return generic 500.

    **Important:** `auth.userId!` appears in every where-clause. Do NOT filter out tombstones (PATTERN 8). Do NOT `console.log` request body.

    **Part B — Rewrite src/__tests__/sync-pull-route.test.ts**

    Replace Plan 01 stub. Mock the Drizzle client at module boundary. Required tests (names must match 43-VALIDATION.md exactly):

    - `it("user_id scoped: every Drizzle SELECT includes eq(table.userId, auth.userId!)")` — spy on the mocked drizzle .where() / verify the eq(...userId...) clause is present for every table queried.
    - `it("returns rows with updated_at > cursor, ordered ASC")` — mock returns rows sorted ASC, assert response.result[tableName].rows reflects the order and all rows have updatedAt > request cursor.
    - `it("soft-caps per table at PULL_SOFT_CAP (500) and sets hasMore=true when exceeded")` — mock returns 501 rows, assert response has rows.length === 500 and hasMore === true.
    - `it("includes tombstones (deletedAt != null) in response")` — mock returns a row with deletedAt set, assert it appears in response.result[...].rows (no filtering).
    - `it("returns serverTime for client-side skew-margin cursor clamp")` — assert response.body.serverTime is a number close to Date.now() at test time (within 1000ms).
    - `it("rejects unauthenticated requests with 401 via withAuth")` — call handler without session, expect 401 (withAuth handles this; test asserts the status — can be a thin test because Phase 41 already covers withAuth in depth).

    Remove all it.todo and the MISSING it from the stub.
  </action>
  <verify>
    <automated>pnpm vitest run src/__tests__/sync-pull-route.test.ts 2>&1 | tee /tmp/w2-pull.log; grep -E "(6 passed|6 tests passed)" /tmp/w2-pull.log && pnpm tsc --noEmit 2>&1 | grep "error TS" | grep -E "(sync/pull|sync-pull)" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/sync/pull/route.ts exists and exports POST wrapped in withAuth
    - `grep "pullBodySchema.safeParse" src/app/api/sync/pull/route.ts` matches
    - `grep "serverTime = Date.now()" src/app/api/sync/pull/route.ts` matches
    - `grep -c "auth.userId!" src/app/api/sync/pull/route.ts` returns at least 1 (and actually should be inside every where clause — let the test enforce that)
    - `grep -c "console.log" src/app/api/sync/pull/route.ts` returns 0
    - `grep "limit(PULL_SOFT_CAP + 1)" src/app/api/sync/pull/route.ts` matches (cap+1 detection idiom)
    - sync-pull-route.test.ts has 6 real `it(...)` blocks with the exact validation names, 0 todo, 0 MISSING
    - `pnpm vitest run src/__tests__/sync-pull-route.test.ts` → 6 passed, 0 failed
  </acceptance_criteria>
  <done>Pull route returns scoped, cursor-paginated, tombstone-inclusive changes with serverTime; all 6 unit tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/sync/pull | Untrusted cursor map crosses here; validated and scoped server-side. |
| /api/sync/pull → Postgres | Trusted; parameterized via Drizzle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04-01 / T-sync-05 | Elevation of Privilege | pull SELECT | mitigate | `eq(table.userId, auth.userId!)` on every where clause. Test "user_id scoped" enforces at CI. |
| T-43-04-02 / T-sync-07 | Tampering | cursor-race missed rows | mitigate | serverTime captured before query + client clamps to serverTime - 30s (client side: Plan 06). Test "returns serverTime" enforces server side. |
| T-43-04-03 | Denial of Service | unbounded pull response | mitigate | PULL_SOFT_CAP = 500 per table; client re-calls until hasMore=false. Test "soft-caps" enforces. |
| T-43-04-04 | Information Disclosure | error responses | mitigate | Generic 500 message; `[sync/pull]` log prefix server-side only. No console.log of request body. |
| T-43-04-05 | Tampering | cursor injection (negative or non-numeric) | mitigate | `z.number().int().min(0)` on cursor values; Zod enum on tableName keys. |

No high-severity residual risks.
</threat_model>

<verification>
- `pnpm vitest run src/__tests__/sync-pull-route.test.ts` → 6 passed
- `pnpm vitest run src/__tests__/sync-push-route.test.ts` → 7 passed (regression)
- `pnpm tsc --noEmit` → 0 errors
- grep acceptance criteria all pass
</verification>

<success_criteria>
- POST /api/sync/pull returns scoped, cursor-paginated, tombstone-inclusive changes
- sync-payload.ts houses pullBodySchema alongside the existing pushBodySchema
- All 6 SYNC-03 pull tests pass; serverTime return + user_id scope security controls enforced
- Plan 06's engine can import pullBodySchema, PullResponse, and PULL_SOFT_CAP from sync-payload.ts
</success_criteria>

<output>
After completion, create `.planning/phases/43-sync-engine-core/43-04-SUMMARY.md`
</output>
