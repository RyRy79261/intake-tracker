---
phase: 40-settings-accordion-restructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/urination-defecation-defaults.tsx
  - src/components/settings/weight-settings-section.tsx
  - src/components/settings/substance-settings-section.tsx
autonomous: true
requirements: [SET-02, SET-06]

must_haves:
  truths:
    - Urination and defecation default amount selectors render inline in the Tracking group
    - Weight graph toggles (Eating, Urination, Defecation, Drinking) render inline under Weight settings
    - Liquid presets CRUD (list, add, edit, delete) renders inline under Substance settings
    - No CustomizationPanel dialog exists in the codebase
  artifacts:
    - src/components/settings/urination-defecation-defaults.tsx
  key_links:
    - UrinationDefecationDefaults uses useSettings hook for urinationDefaultAmount and defecationDefaultAmount
    - Weight graph toggles use useSettings for weightGraphShow* settings
    - Liquid presets use useSettingsStore for liquidPresets, addLiquidPreset, updateLiquidPreset, deleteLiquidPreset
---

<objective>
Decompose the CustomizationPanel dialog into inline settings components. Extract urination/defecation defaults, weight graph toggles, and liquid presets into their respective settings sections.

Purpose: Eliminate the CustomizationPanel modal so all settings are configurable inline without opening any dialog (SET-02, SET-06).
Output: New urination/defecation defaults component, enhanced weight and substance sections with inline controls, deleted CustomizationPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-settings-accordion-restructure/40-CONTEXT.md
@.planning/phases/40-settings-accordion-restructure/40-RESEARCH.md
@src/components/customization-panel.tsx
@src/components/settings/weight-settings-section.tsx
@src/components/settings/substance-settings-section.tsx
@src/hooks/use-settings.ts
@src/stores/settings-store.ts
</context>

<threat_model>
No security threats identified. This plan decomposes an existing UI dialog into inline components. All settings data flows remain identical (Zustand store reads/writes). No new data handling, API calls, or external dependencies.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create inline urination/defecation defaults and add weight graph toggles</name>
  <files>src/components/settings/urination-defecation-defaults.tsx, src/components/settings/weight-settings-section.tsx</files>
  <read_first>
    - src/components/customization-panel.tsx (source of urination/defecation defaults in Tracking tab, and GraphToggle component in Graph tab)
    - src/components/settings/weight-settings-section.tsx (target for graph toggles)
    - src/hooks/use-settings.ts (settings hook interface)
    - .planning/phases/40-settings-accordion-restructure/40-CONTEXT.md (D-08 through D-11)
  </read_first>
  <action>
1. Create `src/components/settings/urination-defecation-defaults.tsx` (per D-10):

Extract the Tracking tab content from CustomizationPanel. The component should render:
- A colored header: `text-rose-600 dark:text-rose-400` icon (use `Droplets` from lucide-react) with h3 "Bathroom Defaults"
- Two Select dropdowns (urination default amount, defecation default amount)
- Same options as the dialog: small, medium, large
- Use `useSettings()` hook for `urinationDefaultAmount`, `setUrinationDefaultAmount`, `defecationDefaultAmount`, `setDefecationDefaultAmount`
- Follow the existing section pattern: `space-y-4` wrapper, colored icon + h3 header, `space-y-3 pl-6` content

2. Enhance `src/components/settings/weight-settings-section.tsx` (per D-11):

Add the weight graph toggles from CustomizationPanel's Graph tab below the existing weight increment control:
- Copy the `GraphToggle` helper component from `customization-panel.tsx` into `weight-settings-section.tsx` (it is a small local component)
- Add a sub-heading "Weight Graph Overlays" (text-xs font-medium uppercase tracking-wide text-muted-foreground) below the increment control
- Render 4 GraphToggle buttons for: Eating, Urination, Defecation, Drinking
- Use the same color-coded active states from the original:
  - Eating: `bg-orange-100 border-orange-300 dark:bg-orange-900/50 dark:border-orange-700`
  - Urination: `bg-violet-100 border-violet-300 dark:bg-violet-900/50 dark:border-violet-700`
  - Defecation: `bg-stone-100 border-stone-300 dark:bg-stone-900/50 dark:border-stone-700`
  - Drinking: `bg-sky-100 border-sky-300 dark:bg-sky-900/50 dark:border-sky-700`
- Use `useSettings()` for `weightGraphShowEating`, `setWeightGraphShowEating`, etc.
- Import `cn` from `@/lib/utils`
  </action>
  <acceptance_criteria>
    - `src/components/settings/urination-defecation-defaults.tsx` exists and exports `UrinationDefecationDefaults`
    - `UrinationDefecationDefaults` contains `Select` components with values "small", "medium", "large"
    - `src/components/settings/weight-settings-section.tsx` contains `GraphToggle` component
    - `weight-settings-section.tsx` contains 4 GraphToggle elements for Eating, Urination, Defecation, Drinking
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Urination/defecation defaults render as a standalone inline section. Weight graph toggles render inline under weight settings. Both extracted from CustomizationPanel.</done>
</task>

<task type="auto">
  <name>Task 2: Move liquid presets inline under Substance settings and delete CustomizationPanel</name>
  <files>src/components/settings/substance-settings-section.tsx, src/components/customization-panel.tsx</files>
  <read_first>
    - src/components/customization-panel.tsx (liquid presets tab content, PresetEditForm component)
    - src/components/settings/substance-settings-section.tsx (target for inline presets)
    - src/stores/settings-store.ts (LiquidPreset type, liquidPresets, addLiquidPreset, updateLiquidPreset, deleteLiquidPreset)
    - .planning/phases/40-settings-accordion-restructure/40-CONTEXT.md (D-09: liquid presets keep tabbed view, rendered inline)
  </read_first>
  <action>
1. Enhance `src/components/settings/substance-settings-section.tsx` (per D-09):

Add the liquid presets section below the existing Caffeine and Alcohol sections:
- Add a new "Liquid Presets" sub-section with header using `Droplets` icon (from lucide-react) and `text-blue-600 dark:text-blue-400` color
- Copy the `PresetEditForm` component and `formatPresetSubstances` utility from `customization-panel.tsx` into `substance-settings-section.tsx`
- Render the full preset list with edit/delete controls, exactly as in the dialog's Presets tab:
  - List all `liquidPresets` from `useSettingsStore`
  - Each preset row shows: name, volume (ml), substance info, edit button (Pencil icon), delete button (Trash2 icon, hidden for defaults)
  - Clicking edit shows inline `PresetEditForm`
  - Delete shows inline confirmation row
  - Add Preset button at bottom
- Use the same state management: `useState` for `editingPresetId`, `isAdding`, `deletingPresetId`
- Import additional dependencies: `Pencil`, `Trash2`, `Plus`, `Droplets` from lucide-react; `Badge` from `@/components/ui/badge`; `Tabs`, `TabsContent`, `TabsList`, `TabsTrigger` from `@/components/ui/tabs` (if keeping the coffee/alcohol/beverage tab filtering)
- NOTE: Keep the preset display as a flat list (not tabbed) for inline presentation simplicity. The tab filtering was specific to the dialog layout. The existing presets already have a `tab` field that categorizes them.

2. Delete `src/components/customization-panel.tsx` entirely (per D-08):
   ```bash
   rm src/components/customization-panel.tsx
   ```
   This file should already have no imports (page.tsx import was removed in Plan 01). Verify with:
   ```bash
   grep -r "customization-panel\|CustomizationPanel" src/ --include="*.tsx" --include="*.ts"
   ```
   If any remaining references exist, remove them.
  </action>
  <acceptance_criteria>
    - `src/components/settings/substance-settings-section.tsx` contains `PresetEditForm` component
    - `substance-settings-section.tsx` contains `formatPresetSubstances` function
    - `substance-settings-section.tsx` imports `useSettingsStore` for `liquidPresets`, `addLiquidPreset`, `updateLiquidPreset`, `deleteLiquidPreset`
    - `src/components/customization-panel.tsx` does NOT exist (file deleted)
    - `grep -r "CustomizationPanel" src/` returns no matches
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Liquid presets CRUD renders inline under Substance settings. CustomizationPanel dialog file is deleted. No modal exists for settings except Debug panel.</done>
</task>

</tasks>

<verification>
1. `pnpm build` — TypeScript compiles without errors
2. `pnpm lint` — No lint errors
3. `grep -r "CustomizationPanel" src/` — Returns no matches (dialog fully removed)
4. `grep -r "customization-panel" src/` — Returns no import references
5. Verify `src/components/customization-panel.tsx` does not exist
6. Verify `src/components/settings/urination-defecation-defaults.tsx` exists
</verification>

<success_criteria>
- CustomizationPanel dialog is completely deleted from codebase (D-08)
- Urination/defecation defaults render inline in their own section (D-10)
- Weight graph toggles render inline under Weight settings (D-11)
- Liquid presets with full CRUD render inline under Substance settings (D-09)
- All settings that were in the dialog still function identically
- No settings modal exists except Debug panel (SET-06)
</success_criteria>
