---
phase: 05.1-pencil-design-system-onboarding
plan: 04
type: execute
wave: 3
depends_on:
  - 05.1-02
  - 05.1-03
files_modified:
  - design/app.pen
autonomous: true
requirements:
  - DSGN-04

must_haves:
  truths:
    - "Dashboard screen design exists with populated, empty, loading, and error states"
    - "Settings screen design exists with populated state"
    - "Screen designs use component refs (not copy-pasted elements)"
    - "Screens use 393px width (iPhone 14 Pro logical width)"
  artifacts:
    - path: "design/app.pen"
      provides: "Dashboard (4 states) + Settings screen designs"
  key_links:
    - from: "Screen / Dashboard"
      to: "Domain components + Chrome components"
      via: "ref instances"
---

<objective>
Design the Dashboard and Settings screens with all required state variants.

Purpose: The Dashboard is the primary screen and most complex layout. Settings is straightforward. Together they cover the home experience and app configuration. All screens compose from reusable components built in Plans 02 and 03.

Output: Dashboard screen in 4 states (populated, empty, loading skeleton, error) + Settings screen (populated state). All 393px wide, composing component refs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-RESEARCH.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-02-SUMMARY.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-03-SUMMARY.md

@design/app.pen
@src/app/page.tsx
@src/components/settings/settings-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design Dashboard screen (all 4 states)</name>
  <files>design/app.pen</files>
  <action>
Read `src/app/page.tsx` to understand the Dashboard layout and component ordering. Create 4 screen frames using Pencil MCP `batch_design`:

**Screen / Dashboard** (populated state):
- 393x852px frame, fill=$bg, vertical layout
- Chrome / App Header ref at top
- Scrollable content area with vertical stack, gap: 12px, padding: 16px:
  - Domain / Intake Card Water ref (showing sample data: "2400 / 3000 ml")
  - Domain / Intake Card Salt ref (showing sample data: "4.2 / 6.0 g")
  - Row of health cards: Domain / Health Card BP ref + Domain / Health Card Weight ref (side by side or stacked based on actual app layout)
  - Domain / Tracking Card Eating ref
  - Domain / Tracking Card Urination ref
  - Domain / Tracking Card Defecation ref
  - Domain / Substance Card Caffeine ref
  - Domain / Substance Card Alcohol ref
- Chrome / Quick Nav Footer ref at bottom (Home tab active)

**Screen / Dashboard Empty** (empty/new user state):
- Same chrome (header + footer)
- Content area: intake cards showing 0/3000 ml and 0/6.0 g
- Health cards showing "No data" or dashes
- Tracking cards showing count: 0
- Substance cards showing 0 mg / 0 units

**Screen / Dashboard Loading** (skeleton state):
- Same chrome (header + footer)
- Replace all card content with $muted rounded rectangles (skeleton placeholders)
- Maintain card shapes and sizes but replace text/numbers with gray bars
- Add text annotation: "animate-pulse in code"

**Screen / Dashboard Error** (error state):
- Same chrome (header + footer)
- Central error message: AlertTriangle Lucide icon + "Something went wrong" title + "Could not load your data" description + "Try Again" button (Button / Primary ref)

Use `get_screenshot` for each frame to verify layout.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findFrames(node,name,found=[]){if(node.name&&node.name.includes(name))found.push(node.name);if(node.children)node.children.forEach(c=>findFrames(c,name,found));return found;} const screens=findFrames(d,'Screen / Dashboard'); console.log('dashboard screens:', screens.length, screens);"</automated>
  </verify>
  <done>Dashboard screen exists in 4 states: populated (with sample data in all domain cards), empty (zero values), loading (skeleton placeholders), error (error message with retry). All use component refs and 393px width.</done>
</task>

<task type="auto">
  <name>Task 2: Design Settings screen</name>
  <files>design/app.pen</files>
  <action>
Read `src/components/settings/settings-page.tsx` (and related settings components) to understand the Settings layout. Create screen frame(s) using Pencil MCP `batch_design`:

**Screen / Settings** (populated state):
- 393x852px frame, fill=$bg, vertical layout
- Chrome / App Header ref at top (with "Settings" title or back arrow)
- Scrollable content, padding: 16px, vertical stack, gap: 16px:
  - **Tracking Limits section** (Card ref):
    - Section title: "Daily Limits"
    - Water limit setting with numeric input (e.g., 3000 ml)
    - Salt limit setting with numeric input (e.g., 6.0 g)
  - **Increments section** (Card ref):
    - Section title: "Quick Add Amounts"
    - Water increment options
    - Salt increment options
  - **Appearance section** (Card ref):
    - Theme toggle (Light/Dark/System) using switch or select
    - Day start hour setting
  - **Substance Tracking section** (Card ref):
    - Caffeine tracking toggle (Switch ref)
    - Alcohol tracking toggle (Switch ref)
  - **Data section** (Card ref):
    - Export data button
    - Import data button
    - Clear data button (destructive)
  - **About section** (Card ref):
    - App version
    - Links
- Chrome / Quick Nav Footer ref at bottom (Settings tab active)

**Screen / Settings Empty** — Settings always has default values, so this is the same as populated with default values. Create if meaningfully different, otherwise skip.

**Screen / Settings Loading** — Skeleton variant with muted bars replacing setting values.

**Screen / Settings Error** — Same pattern as Dashboard error state.

Use `get_screenshot` to verify.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findFrames(node,name,found=[]){if(node.name&&node.name.includes(name))found.push(node.name);if(node.children)node.children.forEach(c=>findFrames(c,name,found));return found;} const screens=findFrames(d,'Screen / Settings'); console.log('settings screens:', screens.length, screens);"</automated>
  </verify>
  <done>Settings screen exists with populated state showing all setting sections (limits, increments, appearance, substance tracking, data management, about). Loading and error variants created. All use component refs and 393px width.</done>
</task>

</tasks>

<verification>
- At least 4 Dashboard screen frames exist (populated, empty, loading, error)
- At least 1 Settings screen frame exists (populated)
- All screens are 393px wide
- All screens use component ref instances (not copy-pasted elements)
- get_screenshot shows correct layout composition
</verification>

<success_criteria>
Dashboard and Settings screens are fully designed with all required state variants, composing from reusable component refs.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-pencil-design-system-onboarding/05.1-04-SUMMARY.md`
</output>
