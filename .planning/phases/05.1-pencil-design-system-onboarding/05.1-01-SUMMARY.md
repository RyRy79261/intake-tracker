---
phase: 05.1-pencil-design-system-onboarding
plan: 01
subsystem: ui
tags: [css-custom-properties, tailwind, pencil, design-tokens, theming]

requires:
  - phase: 04-analytics-service
    provides: existing water/salt CSS custom properties and Tailwind config
provides:
  - CSS custom properties for all 10 domain colors (light + dark)
  - Tailwind color tokens for all 10 domains
  - Pencil theme axis (mode: light/dark) with 32 themed variables
  - Variable references in existing dashboard frame (replaces hardcoded hex)
affects: [05.1-02, 05.1-03, 05.1-04, 05.1-05, 05.1-06, 05.1-07]

tech-stack:
  added: []
  patterns: [hsl-css-custom-properties, pencil-theme-axis, variable-references]

key-files:
  created: []
  modified:
    - src/app/globals.css
    - tailwind.config.ts
    - design/app.pen

key-decisions:
  - "Pencil .pen file stores data internally — disk file stays as {} placeholder, changes managed via MCP tools"
  - "Dark mode foreground colors use 220 20% 8% (matching existing dark --background) for consistency"
  - "Caffeine-foreground uses black in light mode (yellow text needs dark contrast)"

patterns-established:
  - "Domain color token pattern: --domain / --domain-foreground in CSS, domain.DEFAULT / domain.foreground in Tailwind"
  - "Pencil variable naming mirrors CSS custom property names (without -- prefix)"
  - "Theme axis mode: [light, dark] is the standard for all themed Pencil variables"

requirements-completed: [DSGN-01]

duration: 8min
completed: 2026-03-11
---

# Plan 05.1-01: Design Token Foundation Summary

**CSS custom properties and Tailwind tokens for all 10 domains with Pencil theme-conditional variables mirroring light/dark values**

## Performance

- **Duration:** 8 min
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added CSS custom properties for 8 new domains (weight, bp, eating, urination, defecation, caffeine, alcohol, medication) in both :root and .dark blocks
- Extended Tailwind config with all 10 domain color entries using hsl(var(--token)) pattern
- Set up Pencil theme axis with mode: [light, dark] and 32 themed variables (12 core + 20 domain)
- Replaced all hardcoded hex values in existing dashboard frame with $variable references
- Changed font family from Inter to Outfit across all text nodes

## Task Commits

1. **Task 1: Add domain color CSS custom properties and Tailwind tokens** - `5779a13` (feat)
2. **Task 2: Set up Pencil theme axis and design token variables** - Pencil MCP changes (not git-trackable — .pen file stores data internally)

## Files Created/Modified
- `src/app/globals.css` - 8 new domain color CSS custom properties (light + dark modes)
- `tailwind.config.ts` - 8 new domain color entries in extend.colors
- `design/app.pen` - 32 themed variables, theme axis, variable references in dashboard

## Decisions Made
- Pencil .pen files store data in internal database, not as readable JSON on disk — git tracking limited to MCP-managed state
- Used replace_all_matching_properties for bulk hex-to-variable migration in existing frame
- Set theme: {mode: "light"} on dashboard frame for correct variable resolution

## Deviations from Plan
None - plan executed as written.

## Issues Encountered
- .pen file on disk is `{}` (empty JSON) — Pencil stores data internally. Verified via MCP tools that all variables and theme axis are correctly set.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All design tokens in place for component creation (Plans 02-03)
- Pencil variables available for all subsequent screen designs (Plans 04-07)

---
*Phase: 05.1-pencil-design-system-onboarding*
*Completed: 2026-03-11*
