---
phase: 05.1-pencil-design-system-onboarding
plan: 03
type: execute
wave: 2
depends_on:
  - 05.1-01
files_modified:
  - design/app.pen
autonomous: true
requirements:
  - DSGN-03

must_haves:
  truths:
    - "Domain-specific components exist for all tracking domains: intake (water/salt), health (BP/weight), bodily (eating/urination/defecation), substance (caffeine/alcohol), medication"
    - "App header and quick-nav footer exist as reusable components"
    - "Each domain component uses its domain color token ($water, $salt, $weight, etc.)"
    - "Components visually match the current app implementation"
  artifacts:
    - path: "design/app.pen"
      provides: "Domain-specific reusable components and app chrome"
  key_links:
    - from: "design/app.pen domain components"
      to: "src/components/*.tsx"
      via: "1:1 visual parity with codebase components"
---

<objective>
Create all domain-specific components and app chrome (header, footer) as reusable Pencil components.

Purpose: These domain components compose the primitives into the actual UI elements visible in the app. Screen designs in later plans instantiate these components via ref.

Output: Reusable Pencil components for: intake card (water/salt), health metric card (BP/weight), tracking cards (eating/urination/defecation), caffeine card, alcohol card, substance type picker, prescription card, dose logger, inventory widget, app header, quick-nav footer.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-RESEARCH.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-01-SUMMARY.md

@design/app.pen
@src/components/intake-card.tsx
@src/components/blood-pressure-card.tsx
@src/components/weight-card.tsx
@src/components/eating-card.tsx
@src/components/urination-card.tsx
@src/components/defecation-card.tsx
@src/components/substance/caffeine-card.tsx
@src/components/substance/alcohol-card.tsx
@src/components/app-header.tsx
@src/components/quick-nav-footer.tsx
@src/components/medications/prescription-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create health tracking domain components</name>
  <files>design/app.pen</files>
  <action>
Create a container frame named "Components / Domain" in design/app.pen using Pencil MCP `batch_design`. Read each source component file to understand its visual structure, then create the Pencil equivalent.

**Intake Card (Water)** - reference `src/components/intake-card.tsx`:
- Card container with $water-tinted background (light: water at 10% opacity background, or use a `water-bg` variable)
- Header: Droplets Lucide icon + "Water" title + current value (e.g., "2400 ml") in $water color
- Progress bar showing progress toward daily limit
- Quick-add buttons row (numeric-input ref or custom buttons with +250, +500 labels)
- Create as reusable, name: "Domain / Intake Card Water"

**Intake Card (Salt)** - same structure as water but with $salt color:
- Salt icon (could use a different Lucide icon) + "Salt" title
- Name: "Domain / Intake Card Salt"

**Health Metric Card (Blood Pressure)** - reference `src/components/blood-pressure-card.tsx`:
- Card with $bp accent color
- Header: Heart Lucide icon + "Blood Pressure" + latest reading (e.g., "120/80")
- Optional heart rate sub-value
- Add button
- Name: "Domain / Health Card BP"

**Health Metric Card (Weight)** - reference `src/components/weight-card.tsx`:
- Card with $weight accent color
- Header: Scale Lucide icon + "Weight" + latest reading (e.g., "75.2 kg")
- Trend indicator (arrow up/down)
- Add button
- Name: "Domain / Health Card Weight"

**Tracking Cards** - reference eating-card.tsx, urination-card.tsx, defecation-card.tsx:
- Each: card with domain accent color, icon, title, today's count, log button
- Eating: Utensils icon, $eating color. Name: "Domain / Tracking Card Eating"
- Urination: Droplet icon, $urination color. Name: "Domain / Tracking Card Urination"
- Defecation: appropriate icon, $defecation color. Name: "Domain / Tracking Card Defecation"

**Caffeine Card** - reference `src/components/substance/caffeine-card.tsx`:
- Card with $caffeine accent, Coffee icon, daily total, quick-add buttons
- Name: "Domain / Substance Card Caffeine"

**Alcohol Card** - reference `src/components/substance/alcohol-card.tsx`:
- Card with $alcohol accent, Wine icon, daily total, quick-add buttons
- Name: "Domain / Substance Card Alcohol"

All components: use $variable references, Outfit font, reusable: true.
Use `get_screenshot` to verify appearance.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findReusable(node,found=[]){if(node.reusable)found.push(node.name);if(node.children)node.children.forEach(c=>findReusable(c,found));return found;} const r=findReusable(d); const domain=r.filter(n=>n.includes('Domain')); console.log('domain components:', domain.length, domain);"</automated>
  </verify>
  <done>All health tracking domain components exist as reusable Pencil components: 2 intake cards (water/salt), 2 health cards (BP/weight), 3 tracking cards (eating/urination/defecation), 2 substance cards (caffeine/alcohol). Each uses its domain color token.</done>
</task>

<task type="auto">
  <name>Task 2: Create medication components and app chrome</name>
  <files>design/app.pen</files>
  <action>
Continue in design/app.pen using Pencil MCP `batch_design`.

**Substance Type Picker** - reference substance type picker UI:
- Grid of substance type options (caffeine, alcohol) with icons and labels
- Selected state with $primary border
- Name: "Domain / Substance Type Picker"

**Prescription Card** - reference `src/components/medications/prescription-card.tsx`:
- Card with $medication accent
- Compound name (primary, bold), brand name (secondary, muted)
- Current phase badge (e.g., "Maintenance" or "Titration Week 2")
- Next dose time indicator
- Inventory status indicator (pill count or warning)
- Name: "Domain / Prescription Card"

**Dose Logger** - simplified dose logging interaction:
- Medication name + scheduled time
- Take / Skip buttons
- Dosage display (e.g., "10mg - 1 tablet")
- Name: "Domain / Dose Logger"

**Inventory Widget**:
- Current stock display with pill icon
- Low stock warning state
- Add inventory button
- Name: "Domain / Inventory Widget"

**App Header** - create frame named "Components / Chrome", reference `src/components/app-header.tsx`:
- Height: 56px, full width (393px), fill=$card-bg
- Left: app title "Intake Tracker" (font-weight: 700, font-size: 18px)
- Right: settings gear icon button
- Bottom border: 1px $border
- Name: "Chrome / App Header"

**Quick-Nav Footer** - reference `src/components/quick-nav-footer.tsx`:
- Height: 64px, full width (393px), fill=$card-bg
- 4 tab buttons evenly spaced: Home (LayoutDashboard icon), Medications (Pill icon), Analytics (BarChart3 icon), Settings (Settings icon)
- Active tab: $primary color, inactive: $muted-foreground
- Top border: 1px $border
- Name: "Chrome / Quick Nav Footer"
- Create 4 variants (one for each active tab) OR use a single component with active tab being the first one

All components: reusable: true, $variable references, Outfit font.
Use `get_screenshot` to verify all components.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findReusable(node,found=[]){if(node.reusable)found.push(node.name);if(node.children)node.children.forEach(c=>findReusable(c,found));return found;} const r=findReusable(d); const chrome=r.filter(n=>n.includes('Chrome')); const med=r.filter(n=>n.includes('Prescription')||n.includes('Dose')||n.includes('Inventory')); console.log('chrome:', chrome.length, chrome, 'medication:', med.length, med);"</automated>
  </verify>
  <done>Medication components (prescription card, dose logger, inventory widget, substance type picker) and app chrome (header, footer) exist as reusable Pencil components. All use domain color tokens and Outfit font.</done>
</task>

</tasks>

<verification>
- "Components / Domain" frame contains all 9 domain-specific components
- "Components / Chrome" frame contains app header and quick-nav footer
- All components are reusable: true
- All components use $variable color references
- Visual parity with codebase components (verified via get_screenshot)
</verification>

<success_criteria>
All domain-specific components and app chrome exist as reusable Pencil components, ready for composition into full screen designs.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-pencil-design-system-onboarding/05.1-03-SUMMARY.md`
</output>
