---
phase: 05.1-pencil-design-system-onboarding
plan: 05
type: execute
wave: 3
depends_on:
  - 05.1-02
  - 05.1-03
files_modified:
  - design/app.pen
autonomous: true
requirements:
  - DSGN-04

must_haves:
  truths:
    - "Medications list screen exists with populated and empty states"
    - "Medication detail view screen exists"
    - "Add-prescription wizard steps are designed"
    - "Dose logging and inventory management screens exist"
  artifacts:
    - path: "design/app.pen"
      provides: "Medications screens with all states and flows"
  key_links:
    - from: "Screen / Medications"
      to: "Domain / Prescription Card"
      via: "ref instances"
---

<objective>
Design the Medications screens and flows: list view, detail view, add-prescription wizard, dose logging interaction, and inventory management.

Purpose: Medications is the most complex feature area with multiple sub-screens and flows. These designs will be the authoritative reference for Phase 6 (Medication UX Core) implementation.

Output: Medications list (populated/empty/loading/error), detail view, wizard steps, dose logging, and inventory screens.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-RESEARCH.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-02-SUMMARY.md
@.planning/phases/05.1-pencil-design-system-onboarding/05.1-03-SUMMARY.md

@design/app.pen
@src/app/medications/page.tsx
@src/components/medications/prescription-card.tsx
@src/components/medications/add-prescription-wizard.tsx
@src/components/medications/dose-schedule-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design Medications list and detail screens</name>
  <files>design/app.pen</files>
  <action>
Read the medications page and components to understand layout. Create screens using Pencil MCP `batch_design`:

**Screen / Medications** (populated list view):
- 393x852px frame, fill=$bg, vertical layout
- Chrome / App Header ref (title: "Medications")
- "Add Prescription" button (Button / Primary ref) at top of content
- Vertical list of Domain / Prescription Card refs (show 3 example prescriptions with different states):
  - One with "Maintenance" phase badge, green stock indicator
  - One with "Titration Week 2" badge, yellow low-stock warning
  - One showing next dose due time
- Chrome / Quick Nav Footer ref (Medications tab active)

**Screen / Medications Empty**:
- Same chrome
- Center: Pill Lucide icon (large, $muted-foreground), "No prescriptions" title, "Add your first prescription to start tracking" description, "Add Prescription" button

**Screen / Medications Loading**:
- Skeleton cards (3 placeholder prescription card shapes with $muted fills)

**Screen / Medications Error**:
- Same error pattern as Dashboard

**Screen / Medication Detail**:
- 393x852px frame, fill=$bg
- Back arrow + prescription compound name as title
- Brand name sub-label in $muted-foreground
- Current phase section: phase name, status badge, schedule display (time + dosage per day)
- Today's doses section: Domain / Dose Logger ref(s) showing scheduled dose slots
- Inventory section: Domain / Inventory Widget ref showing current stock
- Phase history: list of past phases with dates
- Action buttons: Edit, Deactivate

Use `get_screenshot` to verify each screen.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findFrames(node,name,found=[]){if(node.name&&node.name.includes(name))found.push(node.name);if(node.children)node.children.forEach(c=>findFrames(c,name,found));return found;} const screens=[...findFrames(d,'Screen / Medication'),...findFrames(d,'Screen / Medications')]; console.log('medication screens:', screens.length, screens);"</automated>
  </verify>
  <done>Medications list screen in 4 states (populated with 3 example prescriptions, empty, loading, error) and Medication Detail screen exist. All use component refs.</done>
</task>

<task type="auto">
  <name>Task 2: Design medication flows (wizard, dose logging, inventory)</name>
  <files>design/app.pen</files>
  <action>
Create flow and interaction screens using Pencil MCP `batch_design`:

**Flow / Add Prescription Step 1** (Medicine Search):
- Drawer/sheet frame (bottom sheet style, 393px wide)
- Step indicator: "Step 1 of 3"
- Search input for medicine name
- AI search results list (3 example results with compound name + brand + form)
- "Manual Entry" link at bottom

**Flow / Add Prescription Step 2** (Schedule Setup):
- Drawer/sheet frame
- Step indicator: "Step 2 of 3"
- Phase type selector: Maintenance / Titration
- Dosage input: amount (mg) + unit selector
- Schedule: day-of-week checkboxes + time picker per schedule entry
- "Add Another Time" button

**Flow / Add Prescription Step 3** (Inventory):
- Drawer/sheet frame
- Step indicator: "Step 3 of 3"
- Region/brand input
- Pill strength (mg per pill)
- Current stock count input
- "Save Prescription" button (Button / Primary)

**Screen / Dose Logging** (interaction overlay):
- Drawer frame showing today's due medications
- Each dose slot: medication name, scheduled time, dosage, Take/Skip buttons
- Taken doses: green checkmark, struck-through
- Skipped doses: gray X mark
- Retroactive logging: time picker for past doses

**Screen / Inventory Management**:
- Drawer frame
- Prescription name at top
- Current stock display (large number + "pills remaining")
- Stock history list (recent transactions: +30 added, -1 taken, -0.5 taken)
- "Add Stock" button with quantity input
- Low stock warning banner if applicable

Use `get_screenshot` to verify all flows.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && node -e "const d=JSON.parse(require('fs').readFileSync('design/app.pen','utf8')); function findFrames(node,name,found=[]){if(node.name&&node.name.includes(name))found.push(node.name);if(node.children)node.children.forEach(c=>findFrames(c,name,found));return found;} const flows=[...findFrames(d,'Flow'),...findFrames(d,'Dose Logging'),...findFrames(d,'Inventory Management')]; console.log('flow screens:', flows.length, flows);"</automated>
  </verify>
  <done>Add-prescription wizard (3 steps), dose logging interaction, and inventory management screens are designed. All flows use appropriate component refs and follow the drawer/sheet pattern used in the app.</done>
</task>

</tasks>

<verification>
- Medications list has 4 state variants
- Medication detail view exists
- Add-prescription wizard has 3 step designs
- Dose logging and inventory management screens exist
- All screens follow 393px mobile width
- get_screenshot shows correct compositions
</verification>

<success_criteria>
Complete medication flow is designed from list view through detail, prescription creation wizard, dose logging, and inventory management -- providing full design reference for Phase 6.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-pencil-design-system-onboarding/05.1-05-SUMMARY.md`
</output>
