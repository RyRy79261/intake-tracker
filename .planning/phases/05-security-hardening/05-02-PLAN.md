---
phase: 05-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-encrypted-field.ts
  - src/lib/backup-service.ts
  - SECURITY.md
autonomous: true
requirements:
  - SECU-02

must_haves:
  truths:
    - "useEncryptedField hook encrypts a string and decrypts it back to the same value"
    - "Encrypted backup export produces a file with encrypted:true wrapper"
    - "Encrypted backup import with correct PIN restores the original data"
    - "SECURITY.md documents the encryption model, PIN gate pattern, and field-level encryption guidance"
  artifacts:
    - path: "src/hooks/use-encrypted-field.ts"
      provides: "Reusable encryption hook using crypto.ts primitives"
      exports: ["useEncryptedField"]
    - path: "src/lib/backup-service.ts"
      provides: "Backup service with optional encrypted export/import"
      exports: ["exportBackup", "exportEncryptedBackup", "importBackup"]
    - path: "SECURITY.md"
      provides: "Security documentation for the project"
      min_lines: 50
  key_links:
    - from: "src/hooks/use-encrypted-field.ts"
      to: "src/lib/crypto.ts"
      via: "imports encrypt, decrypt"
      pattern: "import.*encrypt.*decrypt.*crypto"
    - from: "src/lib/backup-service.ts"
      to: "src/lib/crypto.ts"
      via: "imports encrypt, decrypt for backup encryption"
      pattern: "import.*encrypt.*crypto"
---

<objective>
Build encryption foundations: useEncryptedField hook, encrypted backup export/import, and SECURITY.md documentation.

Purpose: Establish the encryption patterns that can be wired to tables later, and give users the option to export encrypted backups. Document the security model for future reference.
Output: `use-encrypted-field.ts` hook, encrypted backup capability in backup-service, `SECURITY.md` at repo root.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-security-hardening/05-CONTEXT.md
@.planning/phases/05-security-hardening/05-RESEARCH.md

@src/lib/crypto.ts
@src/lib/backup-service.ts

<interfaces>
From src/lib/crypto.ts:
```typescript
export interface EncryptedData {
  data: string;   // base64 ciphertext
  iv: string;     // base64 IV
  salt: string;   // base64 salt
  algorithm: string;
}

export function isCryptoAvailable(): boolean;
export async function encrypt(plaintext: string, pin: string): Promise<EncryptedData>;
export async function decrypt(encrypted: EncryptedData, pin: string): Promise<string>;
export async function hashPin(pin: string, salt?: Uint8Array): Promise<{ hash: string; salt: string }>;
```

From src/lib/backup-service.ts:
```typescript
export interface BackupData {
  version: number;
  exportedAt: string;
  // ... record arrays for all tables
  settings?: Record<string, unknown>;
}

export async function exportBackup(): Promise<Blob>;
export async function importBackup(file: File): Promise<ServiceResult<ImportResult>>;
export async function downloadBackup(): Promise<ServiceResult<void>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useEncryptedField hook and encrypted backup</name>
  <files>src/hooks/use-encrypted-field.ts, src/lib/backup-service.ts</files>
  <action>
1. Create `src/hooks/use-encrypted-field.ts`:
   - Import `encrypt`, `decrypt`, `isCryptoAvailable`, and `EncryptedData` from `@/lib/crypto`
   - Export `useEncryptedField()` hook that returns:
     - `encryptField(value: string, pin: string): Promise<EncryptedData>` -- wraps crypto.encrypt
     - `decryptField(encrypted: EncryptedData, pin: string): Promise<string>` -- wraps crypto.decrypt
     - `isAvailable: boolean` -- from isCryptoAvailable()
   - This is a foundation hook -- NOT wired to any tables or Dexie in this phase (per user decision: "hooks will be built but not wired")
   - Include JSDoc explaining future usage pattern for field-level encryption

2. Modify `src/lib/backup-service.ts` to add encrypted export option:
   - Import `encrypt`, `decrypt`, `EncryptedData` from `./crypto`
   - Define `EncryptedBackup` interface: `{ encrypted: true; payload: EncryptedData; version: number }`
   - Add `exportEncryptedBackup(pin: string): Promise<Blob>`:
     - Calls existing `exportBackup()` logic to get the backup data JSON string
     - Encrypts the JSON string using `encrypt(json, pin)`
     - Wraps in `EncryptedBackup` envelope
     - Returns as Blob
   - Add `downloadEncryptedBackup(pin: string): Promise<ServiceResult<void>>`:
     - Like `downloadBackup()` but uses encrypted export
     - Filename: `intake-tracker-backup-YYYY-MM-DD-encrypted.json`
   - Modify `importBackup(file: File)` to auto-detect format:
     - Parse JSON, check for `encrypted: true` property
     - If encrypted: return an error result with message "This backup is encrypted. Please use importEncryptedBackup() with your PIN."
     - Add `importEncryptedBackup(file: File, pin: string): Promise<ServiceResult<ImportResult>>`:
       - Parse outer JSON as EncryptedBackup
       - Decrypt payload using `decrypt(payload, pin)`
       - Parse inner JSON as BackupData
       - Proceed with existing import logic
   - NOTE: Plan 01 may also modify backup-service.ts (removing perplexityApiKey exclusion). Since Plans 01 and 02 are Wave 1 (parallel), the executor should handle both changes. If Plan 01 runs first, adapt to its changes. If Plan 02 runs first, the perplexityApiKey exclusion line may still exist -- leave it for Plan 01 to clean up.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>useEncryptedField hook exists and exports encryptField/decryptField/isAvailable. Backup service supports encrypted export and import with auto-format detection. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write SECURITY.md documentation</name>
  <files>SECURITY.md</files>
  <action>
Create `SECURITY.md` at the repository root documenting the security model. Include these sections:

1. **Overview** -- Single-user health tracking PWA with access control prioritized over encryption at rest
2. **Access Control Model**:
   - Privy authentication (email/Google) with ALLOWED_EMAILS whitelist
   - PIN gate for quick local access (sessionStorage-based, 24h expiry)
   - LOCAL_AGENT_MODE for E2E testing (disabled in production)
3. **API Security**:
   - All API keys server-side only (PERPLEXITY_API_KEY, PRIVY_APP_SECRET)
   - No NEXT_PUBLIC_ env vars contain secrets
   - Auth middleware (`withAuth` HOF) on all AI API routes
   - PII sanitization via `sanitizeForAI()` before external API calls
4. **Encryption Model**:
   - AES-GCM encryption with PBKDF2 key derivation (crypto.ts)
   - PIN used as encryption passphrase via PBKDF2 (100,000 iterations)
   - `useEncryptedField` hook available for future field-level encryption
   - Encrypted backup export option (PIN-protected)
5. **Data at Rest**:
   - IndexedDB is NOT encrypted (prioritizing useLiveQuery reactivity)
   - Access control (PIN + Privy auth) is the primary protection
   - Field-level encryption available but not wired (deferred)
6. **Content Security Policy** -- Reference next.config.js, document allowed domains and why
7. **Sync Readiness** (NeonDB/Postgres):
   - Auth patterns designed for server-side JWT verification
   - No Dexie Cloud dependency
   - Schema has realmId fields for future multi-tenant partitioning
8. **Environment Variables** -- Table of all env vars with secret status (safe/secret) and where they're used

Keep it factual and concise. No aspirational language. This is a reference document for future development.
  </action>
  <verify>
    <automated>test -f /home/ryan/repos/Personal/intake-tracker/SECURITY.md && wc -l /home/ryan/repos/Personal/intake-tracker/SECURITY.md</automated>
  </verify>
  <done>SECURITY.md exists at repo root with all 8 sections. At least 80 lines. Documents actual implemented patterns, not aspirational ones.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- `src/hooks/use-encrypted-field.ts` exists and exports `useEncryptedField`
- `SECURITY.md` exists at repo root with encryption model, PIN gate, and sync readiness documentation
- `src/lib/backup-service.ts` exports `exportEncryptedBackup` and `importEncryptedBackup`
</verification>

<success_criteria>
Encryption foundations in place: hook ready for future field-level encryption, backup service supports encrypted exports, and SECURITY.md documents the complete security model.
</success_criteria>

<output>
After completion, create `.planning/phases/05-security-hardening/05-02-SUMMARY.md`
</output>
