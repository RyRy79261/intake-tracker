---
phase: 41-neon-auth-privy-removal
plan: 04
type: execute
wave: 3
depends_on: ["41-02", "41-03"]
files_modified:
  - src/lib/ai-client.ts
  - src/lib/substance-enrich.ts
  - src/components/food-salt/food-section.tsx
  - src/components/substance/substance-type-picker.tsx
  - src/components/liquids/preset-tab.tsx
  - src/components/medications/titrations-view.tsx
  - src/hooks/use-medicine-search.ts
  - src/hooks/use-interaction-check.ts
  - src/components/history-drawer.tsx
  - src/components/settings/privacy-security-section.tsx
  - src/app/settings/page.tsx
  - src/hooks/use-pin-gate.tsx
  - src/components/pin-dialog.tsx
  - src/lib/pin-service.ts
  - src/lib/privy-server.ts
  - next.config.js
  - src/__tests__/bundle-security.test.ts
  - package.json
  - pnpm-lock.yaml
  - README.md
  - .env.template
  - vercel.json
autonomous: true
requirements:
  - AUTH-02
  - AUTH-04
must_haves:
  truths:
    - "No source file imports from @privy-io/react-auth or @privy-io/server-auth"
    - "No source file imports from @/hooks/use-pin-gate, @/lib/pin-service, or @/components/pin-dialog"
    - "The files src/hooks/use-pin-gate.tsx, src/lib/pin-service.ts, src/components/pin-dialog.tsx, src/lib/privy-server.ts are deleted"
    - "package.json no longer lists @privy-io/react-auth or @privy-io/server-auth"
    - "src/lib/ai-client.ts parseIntakeWithAI function has zero auth parameters"
    - "All 5 API consumer sites use fetch() without manually attaching Authorization headers"
    - "PrivacySecuritySection no longer renders PIN setup cards — or is removed if it becomes empty"
    - "next.config.js CSP rules replace *.privy.io with the Neon Auth base URL domain"
    - "bundle-security.test.ts CSP assertions reference Neon Auth domain, not Privy"
    - "No environment variable named NEXT_PUBLIC_PRIVY_APP_ID, NEXT_PUBLIC_PRIVY_CLIENT_ID, PRIVY_APP_SECRET, or ALLOW_DEV_FALLBACK appears in .env.template, README, vercel.json, or CI workflow"
    - "pnpm build succeeds end-to-end (the build gate that proves all consumers compile)"
  artifacts:
    - path: "src/lib/ai-client.ts"
      provides: "Auth-free parseIntakeWithAI helper"
    - path: "src/lib/substance-enrich.ts"
      provides: "Auth-free substance enrich helper"
    - path: "next.config.js"
      provides: "Updated CSP with Neon Auth domain"
  key_links:
    - from: "src/components/food-salt/food-section.tsx"
      to: "/api/ai/parse"
      via: "parseIntakeWithAI without options.authHeaders"
      pattern: "parseIntakeWithAI\\([^,)]+\\)"
    - from: "src/components/history-drawer.tsx"
      to: "no PIN gate import"
      via: "import removal"
      pattern: "^(?!.*use-pin-gate).*$"
---

<objective>
Execute the full sweep: delete every Privy artifact, delete every PIN gate artifact, drop `authToken`/`authHeaders` parameters from all client helpers, update all 7 consumer sites, remove `@privy-io/*` dependencies, update CSP rules to point at Neon Auth instead of Privy, and scrub env vars from all config files. After this plan lands, `pnpm build` must succeed and zero references to "privy" or "pin-gate" remain in the source tree.

Purpose: This is the cleanup wave. Plans 41-01, 41-02, 41-03 introduced the new plumbing and broke the compile in known ways (documented in 41-02). This plan fixes the breakage by removing the obsolete consumers, deleting the dead files, and updating the CSP + env scaffolding so the app is fully on Neon Auth. It is the largest file-count plan but each file change is mechanical.

Output: A clean codebase with zero Privy references. A working `pnpm build`. A working `pnpm lint`. All non-E2E vitest suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-neon-auth-privy-removal/41-CONTEXT.md
@.planning/phases/41-neon-auth-privy-removal/41-02-PLAN.md

# Files this plan deletes
@src/lib/privy-server.ts
@src/hooks/use-pin-gate.tsx
@src/components/pin-dialog.tsx
@src/lib/pin-service.ts

# Files this plan modifies
@src/lib/ai-client.ts
@src/components/food-salt/food-section.tsx
@src/components/settings/privacy-security-section.tsx
@next.config.js
@src/__tests__/bundle-security.test.ts

<interfaces>
From src/lib/ai-client.ts (NEW signature — drops options entirely):
```typescript
export async function parseIntakeWithAI(input: string): Promise<ParsedIntake>;
```

Consumer sites before/after:
```typescript
// BEFORE (food-section.tsx line 135-136)
const authHeaders = await getAuthHeader();
const result = await parseIntakeWithAI(trimmed, { authHeaders });

// AFTER
const result = await parseIntakeWithAI(trimmed);
```

```typescript
// BEFORE (any consumer calling /api/ai/* or /api/push/* directly)
const authHeaders = await getAuthHeader();
const res = await fetch("/api/ai/medicine-search", {
  method: "POST",
  headers: { "Content-Type": "application/json", ...authHeaders },
  body: JSON.stringify(payload),
});

// AFTER
const res = await fetch("/api/ai/medicine-search", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify(payload),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop authHeaders params from AI clients and sweep 7 consumer sites</name>
  <files>
    src/lib/ai-client.ts,
    src/lib/substance-enrich.ts,
    src/components/food-salt/food-section.tsx,
    src/components/substance/substance-type-picker.tsx,
    src/components/liquids/preset-tab.tsx,
    src/components/medications/titrations-view.tsx,
    src/hooks/use-medicine-search.ts,
    src/hooks/use-interaction-check.ts
  </files>
  <action>
    Implements D-06 tail (client-side cleanup). This is mechanical — every file has the same pattern to remove.

    1. Edit `src/lib/ai-client.ts`:
       - Change `parseIntakeWithAI(input: string, options?: { authToken?: string; authHeaders?: Record<string, string> })` to `parseIntakeWithAI(input: string)`.
       - Delete the auth header building block (lines 30-40).
       - Keep the `Content-Type: application/json` header.
       - Update the JSDoc: remove "Uses Privy authentication" line; add "Uses Neon Auth cookie session (attached automatically by browser on same-origin fetch)".

    2. Edit `src/lib/substance-enrich.ts`:
       - Find the `authHeaders?: Record<string, string>` param (line 19 per the grep in context gathering).
       - Remove it from the function signature.
       - Remove the `...authHeaders` spread from the fetch headers.

    3. For each of the 6 consumer files, remove `useAuth` / `getAuthHeader` usage:
       - `src/components/food-salt/food-section.tsx` (lines 33, 55, 135, 136, 166): delete `import { useAuth } from "@/components/auth-guard"`, delete `const { getAuthHeader } = useAuth();`, delete `const authHeaders = await getAuthHeader();`, change `parseIntakeWithAI(trimmed, { authHeaders })` to `parseIntakeWithAI(trimmed)`, remove `getAuthHeader` from the `useCallback` dependency array on line 166.
       - `src/components/substance/substance-type-picker.tsx` (lines 16, 49, 117, 120): delete import, delete hook call, delete `const authHeaders = await getAuthHeader();`, change fetch headers from `{ "Content-Type": "application/json", ...authHeaders }` to `{ "Content-Type": "application/json" }`. Check the useCallback/useEffect dependency array and remove `getAuthHeader`.
       - `src/components/liquids/preset-tab.tsx` (lines 16, 55, 186, 189): same mechanical pattern.
       - `src/components/medications/titrations-view.tsx` (lines 52, 496, 648, 651): same pattern.
       - `src/hooks/use-medicine-search.ts` (lines 4, 26, 46): remove import, destructure, and the `const authHeader = await getAuthHeader();` line; clean up any fetch header merge.
       - `src/hooks/use-interaction-check.ts` (lines 4, 45, 85, 88, 125, 135, 146, 149, 194): multiple call sites in this file — apply the same transformation to each. Clean up hook dependency arrays on lines 125 and 194 (remove `getAuthHeader`).

    4. After all 7 files are edited, run this verification:
       ```bash
       grep -rn "getAuthHeader\|authHeaders" src/
       ```
       Expected output: zero hits. If any remain, finish them before marking the task done.

    5. ALSO verify:
       ```bash
       grep -rn "useAuth()" src/ | grep -v "__tests__"
       ```
       Should only match `src/components/auth-guard.tsx` (the hook definition itself). Any other production file still calling `useAuth()` is fine — but they should NOT be destructuring `getAuthHeader` from it.

    Keep `useAuth` itself (from auth-guard.tsx rewritten in plan 41-02) — some components might still call it to read `authenticated` or `user`. Only remove the `getAuthHeader` destructure and downstream usage.
  </action>
  <verify>
    <automated>pnpm lint --file src/lib/ai-client.ts --file src/lib/substance-enrich.ts --file src/components/food-salt/food-section.tsx --file src/components/substance/substance-type-picker.tsx --file src/components/liquids/preset-tab.tsx --file src/components/medications/titrations-view.tsx --file src/hooks/use-medicine-search.ts --file src/hooks/use-interaction-check.ts</automated>
  </verify>
  <done>
    All 8 files compile and lint clean. `grep -rn "getAuthHeader\|authHeaders" src/` returns zero hits. No consumer still spreads auth headers into fetch calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete PIN gate files, clean consumers, gut privacy-security-section</name>
  <files>
    src/hooks/use-pin-gate.tsx,
    src/components/pin-dialog.tsx,
    src/lib/pin-service.ts,
    src/components/history-drawer.tsx,
    src/components/settings/privacy-security-section.tsx,
    src/app/settings/page.tsx
  </files>
  <action>
    Implements D-20 (delete PIN files), D-21 (clean consumers), D-22 (remove PIN UI from Phase 40 Privacy & Security section).

    1. Delete the following files entirely:
       - `src/hooks/use-pin-gate.tsx`
       - `src/components/pin-dialog.tsx`
       - `src/lib/pin-service.ts`

       Use `rm` (or equivalent). Do not try to leave them as stubs.

    2. Edit `src/components/history-drawer.tsx`:
       - Remove line 44: `import { usePinProtected } from "@/hooks/use-pin-gate";`
       - Remove line 61: `const { requirePin } = usePinProtected();`
       - At line 133, remove the `const unlocked = await requirePin();` block and any branching on `unlocked`. The PIN check is gone — the drawer opens unconditionally now (auth has already been verified at the page level by middleware).
       - Update the dependency array on line 138 to remove `requirePin`.
       - Re-read the file to confirm no dangling references to `requirePin` or `usePinProtected`.

    3. Rewrite `src/components/settings/privacy-security-section.tsx` to remove the PIN UI entirely per D-22. Two options:

       **Option A (PREFERRED):** Delete the file and remove its import + render from `src/app/settings/page.tsx`. Phase 40's accordion still has the "Privacy & Security" group (from `settings-accordion-group` in line 81), but now only `<AccountSection />` lives inside it. The PIN card is gone.

       **Option B (fallback if Phase 40 assumes the component exists):** Replace the file contents with a minimal placeholder explaining that PIN has been removed in favor of Neon Auth sessions:
       ```typescript
       "use client";

       // Intentionally empty — PIN gate removed in Phase 41 (Neon Auth migration).
       // Auth is now enforced by middleware; no extra PIN layer.
       export function PrivacySecuritySection() {
         return null;
       }
       ```
       Then leave the import in settings/page.tsx alone so Phase 40's layout is undisturbed.

       **Choose Option A** unless Phase 40's settings/page.tsx structure actively depends on the component rendering something (check lines 81-83 of settings/page.tsx). If `PrivacySecuritySection` can be cleanly removed without leaving an empty accordion, delete the file and the import line. Otherwise fall back to Option B and render null.

    4. Verify:
       ```bash
       grep -rn "use-pin-gate\|pin-dialog\|pin-service\|usePinGate\|usePinProtected\|PinGateProvider" src/
       ```
       Expected: zero hits (or only comments).

    5. Verify `src/app/providers.tsx` (already modified in plan 41-02) has no import from `@/hooks/use-pin-gate` — if plan 41-02 left that import in (unlikely but possible), remove it here.

    Manual sanity check: open settings page in dev, click Privacy & Security accordion, confirm only the Account section renders (with email + Sign Out).
  </action>
  <verify>
    <automated>test ! -f src/hooks/use-pin-gate.tsx &amp;&amp; test ! -f src/components/pin-dialog.tsx &amp;&amp; test ! -f src/lib/pin-service.ts &amp;&amp; pnpm lint --file src/components/history-drawer.tsx --file src/app/settings/page.tsx</automated>
  </verify>
  <done>
    Three PIN gate files deleted. history-drawer.tsx no longer imports from use-pin-gate. privacy-security-section.tsx either deleted or rendering null. settings/page.tsx compiles. No `pin-gate`, `usePinProtected`, or `pin-dialog` references remain in src/.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete Privy files/packages, update CSP, scrub env vars</name>
  <files>
    src/lib/privy-server.ts,
    next.config.js,
    src/__tests__/bundle-security.test.ts,
    package.json,
    pnpm-lock.yaml,
    README.md,
    .env.template,
    vercel.json,
    .github/workflows/ci.yml
  </files>
  <action>
    Implements D-16 (delete privy-server.ts), D-17 (remove packages), D-18 (env var cleanup), D-19 (CSP update).

    1. Delete `src/lib/privy-server.ts` entirely.

    2. Remove Privy packages from `package.json`:
       ```bash
       pnpm remove @privy-io/react-auth @privy-io/server-auth
       ```
       This updates `package.json` and `pnpm-lock.yaml`. Run `pnpm install` afterward to ensure the lockfile is consistent.

    3. Update `next.config.js` CSP rules (lines 20-25 per grep):
       - The current file has these headers around line 16-25:
         - `"script-src 'self' 'unsafe-eval' 'unsafe-inline'"` (KEEP — required for Next.js)
         - `"connect-src 'self' https://api.anthropic.com https://*.privy.io https://*.walletconnect.com https://*.walletconnect.org wss://*.walletconnect.org"` — REPLACE `https://*.privy.io` with the Neon Auth domain. Since the Neon Auth base URL is set via `NEON_AUTH_BASE_URL` env var, use a pattern that covers Neon Auth endpoints. Claude's Discretion per CONTEXT.md: use `https://*.neon.tech` (Neon's canonical parent domain) OR hard-code the specific Neon Auth base URL domain if it has a distinct subdomain. Also remove `https://*.walletconnect.com https://*.walletconnect.org wss://*.walletconnect.org` — WalletConnect was pulled in by Privy and is no longer needed.
         - `"frame-src 'self' https://auth.privy.io"` — REPLACE with `"frame-src 'self' https://*.neon.tech"` or delete the `frame-src` directive entirely if Neon Auth does not use iframes. Better Auth 1.4.18 is a pure-fetch flow, so frame-src is likely unnecessary. Default to deleting the directive; if Google OAuth redirect flow surfaces iframe issues during UAT, add it back.

       Final connect-src should look roughly like:
       ```
       "connect-src 'self' https://api.anthropic.com https://*.neon.tech"
       ```

    4. Update `src/__tests__/bundle-security.test.ts` line 51:
       - Current: `expect(content).not.toContain("PRIVY_APP_SECRET");`
       - Change to: `expect(content).not.toContain("NEON_AUTH_COOKIE_SECRET");`
       - Also add at least one new assertion for Neon Auth leakage:
         ```typescript
         expect(content).not.toMatch(/postgres(ql)?:\/\/[^\s'"]+/); // already exists
         expect(content).not.toContain("NEON_AUTH_COOKIE_SECRET");
         expect(content).not.toContain("GOOGLE_CLIENT_SECRET");
         ```
       - If the file contains explicit `privy` regex patterns elsewhere, update or remove them.

    5. Scrub env vars from config files:
       - `.env.template`: remove lines for `NEXT_PUBLIC_PRIVY_APP_ID`, `NEXT_PUBLIC_PRIVY_CLIENT_ID`, `PRIVY_APP_SECRET`, `ALLOW_DEV_FALLBACK`, `PRIVY_TEST_EMAIL`, `PRIVY_TEST_OTP`. Add new lines: `NEON_AUTH_BASE_URL=`, `NEON_AUTH_COOKIE_SECRET=`, `GOOGLE_CLIENT_ID=`, `GOOGLE_CLIENT_SECRET=`, `NEON_AUTH_TEST_EMAIL=`, `NEON_AUTH_TEST_PASSWORD=`. Note: `.env.template` is at the project root but read permissions may be restricted — if so, document the required changes in a comment at the end of this task file and skip the Write. The Task 3 automated verify command must still succeed.
       - `README.md`: update the architecture diagram line that references "PrivyProvider" (grep earlier found it on line 135). Replace with "NeonAuthClient" or similar. Also update any setup instructions referencing PRIVY env vars.
       - `vercel.json`: check if it references PRIVY env vars. If it does, remove them. Earlier grep found no matches in vercel.json, so this may be a no-op.
       - `.github/workflows/ci.yml`: line 127-128 has the comment "Runs without NEXT_PUBLIC_PRIVY_APP_ID so the app skips Privy auth". Remove the comment and update the e2e job config. The `env: { NEXT_PUBLIC_PRIVY_APP_ID: '' }` passed to the webServer in `playwright.config.ts` will be removed in plan 41-05; coordinate so this plan does NOT break CI before plan 41-05 lands. ACCEPTABLE: leave the CI workflow comment for now and note in 41-05 that it is the final touch.

    6. Final verification sweep:
       ```bash
       grep -rn "privy\|PRIVY\|@privy-io" src/ next.config.js README.md package.json vercel.json .env.template .github/workflows/ci.yml
       ```
       Expected: ZERO hits in source files. If ANY remain, hunt them down before marking the task done. It's fine if .planning/ directories still contain Privy references — those are historical docs.

    7. Run the full build to prove all of Phase 41-01 through 41-04 compose correctly:
       ```bash
       pnpm build
       ```
       This is THE gate. If build fails, triage:
       - Missing Neon Auth env vars → set dummy values in .env.local for build time only
       - Type errors → track down the broken consumer and fix it in this task
       - Missing imports → likely a PIN gate or Privy reference that slipped through the grep

       Do NOT mark this task done until `pnpm build` exits 0.
  </action>
  <verify>
    <automated>test ! -f src/lib/privy-server.ts &amp;&amp; ! grep -q "privy\|@privy-io" src/ -r &amp;&amp; pnpm build</automated>
  </verify>
  <done>
    privy-server.ts deleted. @privy-io packages removed from package.json and pnpm-lock.yaml. next.config.js CSP no longer mentions privy.io; references Neon Auth domain. bundle-security.test.ts asserts no NEON_AUTH_COOKIE_SECRET leakage. .env.template, README.md, CI workflow scrubbed of PRIVY env vars. `pnpm build` succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSP directives → browser | Page security policy declares allowed origins for fetch/script/iframe |
| Env var config → runtime | Build artifacts and runtime env must not contain Privy leftovers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-15 | Information Disclosure | Leftover Privy client ID in bundle | mitigate | Bundle security test scans .next/static/* for PRIVY env names; pnpm remove ensures package is gone and tree-shaken |
| T-41-16 | Information Disclosure | CSP too permissive | mitigate | connect-src tightened to specific domains (api.anthropic.com + *.neon.tech); WalletConnect origins removed |
| T-41-17 | Tampering | PIN gate residue creating confused deputy | mitigate | All 3 PIN files physically deleted; consumer imports removed; grep verification is a done-criterion |
| T-41-18 | Denial of Service | pnpm install resolving the wrong @privy-io transitive | accept | Single-user app; lockfile regenerated via `pnpm install` after removal; minimumReleaseAge supply-chain setting (existing) provides backstop |
</threat_model>

<verification>
- `pnpm build` succeeds
- `grep -rn "privy\|@privy-io\|PRIVY" src/ next.config.js package.json` returns zero hits
- `grep -rn "use-pin-gate\|pin-dialog\|pin-service" src/` returns zero hits
- `grep -rn "getAuthHeader\|authHeaders" src/` returns zero hits
- `pnpm exec vitest run src/__tests__/bundle-security.test.ts` passes (if a build exists)
- `pnpm lint` clean across the repo
</verification>

<success_criteria>
- All 5 files physically deleted: privy-server.ts, use-pin-gate.tsx, pin-dialog.tsx, pin-service.ts, and privacy-security-section.tsx (if Option A chosen)
- @privy-io/react-auth and @privy-io/server-auth removed from package.json dependencies
- `pnpm build` exits 0
- Zero "privy" references in src/, config files, or README (excluding historical .planning/ docs)
- Bundle security test asserts NEON_AUTH_COOKIE_SECRET absence
</success_criteria>

<output>
After completion, create `.planning/phases/41-neon-auth-privy-removal/41-04-SUMMARY.md`
</output>
