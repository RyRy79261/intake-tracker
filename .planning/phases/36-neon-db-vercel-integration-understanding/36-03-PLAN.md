---
phase: 36-neon-db-vercel-integration-understanding
plan: 03
type: execute
wave: 3
depends_on: ["36-01", "36-02"]
files_modified: [docs/architecture/neon-vercel.md]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Architecture doc contains a migration path section describing the shift from IndexedDB-only to cloud-first with offline resilience"
    - "Architecture doc compares conflict resolution strategies and recommends last-write-wins for this single-user app"
    - "Architecture doc covers how both PWA and future Android app would share the NeonDB backend via API routes"
    - "Architecture doc describes Neon Auth as the Privy replacement path"
  artifacts:
    - path: "docs/architecture/neon-vercel.md"
      provides: "Migration path and future architecture sections"
      contains: "## Migration Path"
  key_links:
    - from: "docs/architecture/neon-vercel.md"
      to: "src/lib/db.ts"
      via: "documents the 16 Dexie tables that would need server-side schemas"
      pattern: "db\\.ts|Dexie"
---

<objective>
Add the future migration path to the architecture document covering the shift from IndexedDB-only to cloud-first with offline resilience.

Purpose: Document how the project will evolve from the current state (IndexedDB as primary store, NeonDB for push only) to the target state (NeonDB as primary store, IndexedDB as offline cache). This includes Neon Auth replacing Privy, sync architecture, conflict resolution strategy, and multi-platform (PWA + Android) access patterns.
Output: Three new sections added to `docs/architecture/neon-vercel.md`: Migration Path, Sync Architecture, and Neon Auth Migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-neon-db-vercel-integration-understanding/36-CONTEXT.md
@.planning/phases/36-neon-db-vercel-integration-understanding/36-RESEARCH.md
@.planning/phases/36-neon-db-vercel-integration-understanding/36-01-SUMMARY.md

@src/lib/db.ts
@src/lib/push-db.ts
@src/app/providers.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Migration Path section to architecture document</name>
  <files>docs/architecture/neon-vercel.md</files>
  <read_first>docs/architecture/neon-vercel.md, src/lib/db.ts, .planning/phases/36-neon-db-vercel-integration-understanding/36-CONTEXT.md, .planning/phases/36-neon-db-vercel-integration-understanding/36-RESEARCH.md</read_first>
  <action>
Append the following sections to `docs/architecture/neon-vercel.md`, before the Verification Results section (if it exists):

**## Migration Path: IndexedDB to Cloud-First**

Subsections:

**### Current vs Target Architecture**

Include a comparison diagram (Mermaid):
- Current: Browser -> IndexedDB (16 Dexie tables, source of truth) | Server -> NeonDB (4 push tables only)
- Target: Browser -> IndexedDB (offline cache/buffer) <-> API Routes <-> NeonDB (primary store, all tables) | Android App -> API Routes -> NeonDB

**### Phased Migration Strategy**

Document a phased approach (NOT big-bang):

**Phase A: Schema Creation** — Create server-side PostgreSQL schemas for all 16 Dexie tables (`intakeRecords`, `weightRecords`, `bloodPressureRecords`, `eatingRecords`, `urinationRecords`, `defecationRecords`, `substanceRecords`, `prescriptions`, `medicationPhases`, `phaseSchedules`, `inventoryItems`, `inventoryTransactions`, `doseLogs`, `titrationPlans`, `dailyNotes`, `auditLogs`). Use raw SQL via `@neondatabase/serverless` (matching existing pattern in `push-db.ts`), no ORM.

**Phase B: Sync API Routes** — Create Next.js API routes for sync:
- `POST /api/sync/push` — Client sends pending changes (created/updated/deleted since last sync)
- `GET /api/sync/pull` — Client fetches changes since last sync timestamp
- `POST /api/sync/full` — Full resync for initial setup or recovery
- Auth required on all sync routes

**Phase C: Dexie Schema Extension** — Add sync metadata to each Dexie table:
- `syncStatus`: 'pending' | 'synced' | 'failed'
- `lastModified`: ISO timestamp (for LWW conflict resolution)
- `serverId`: UUID assigned by server on first sync
- `isDeleted`: boolean soft-delete flag (tombstone for sync)

**Phase D: Sync Engine** — Client-side sync logic:
- Background sync on connectivity change (navigator.onLine)
- Periodic sync when app is active
- Conflict resolution: Last-Write-Wins (LWW) using `lastModified` timestamp
- Queue management: batch pending changes, retry on failure
- Settings toggle: local-only mode vs cloud sync mode

**Phase E: Migration Script** — One-time migration for existing users:
- Read all IndexedDB data
- Push to NeonDB via sync/full endpoint
- Mark all records as synced
- Switch to cloud-first mode

**### Conflict Resolution Strategy**

Include a comparison table of 4 strategies (from RESEARCH.md):
| Strategy | Pros | Cons | Fit for This Project |
|----------|------|------|---------------------|
| Last-Write-Wins (LWW) | Simple, no merge logic | Can silently lose edits | Best fit — single-user, append-mostly |
| CRDT | No data loss, auto-merge | Complex, larger payloads | Overkill — no concurrent multi-user |
| Revision History | Manual conflict resolution | UX burden on user | Unnecessary for health tracking |
| Server-Authoritative | Predictable, simple client | Requires connectivity | Too restrictive for offline use |

**Recommendation: Last-Write-Wins (LWW)** with timestamp-based resolution. Rationale:
- Single-user app — no concurrent editing from different users
- Primary conflict: same user on phone vs desktop while offline
- Health tracking records are append-mostly (new entries, not edits)
- Simplest implementation with existing `@neondatabase/serverless` pattern

**### Multi-Platform Access (PWA + Android)**

Both PWA and future Android app share the same backend:
- API routes serve as the universal data layer
- PWA: IndexedDB for offline, syncs via fetch to API routes
- Android: Room/SQLite for offline, syncs via HTTP to same API routes
- NeonDB is the single source of truth for both platforms
- No platform-specific server logic needed

**### Settings Toggle**

User-facing setting in app Settings page:
- **Cloud sync** (default when configured): Data saved to IndexedDB + synced to NeonDB
- **Local only**: Data stays in IndexedDB only, no server communication
- Toggle does not affect push notification functionality (that always uses server)
  </action>
  <verify>grep "## Migration Path" docs/architecture/neon-vercel.md && grep "Last-Write-Wins" docs/architecture/neon-vercel.md && grep "Android" docs/architecture/neon-vercel.md</verify>
  <acceptance_criteria>
    - docs/architecture/neon-vercel.md contains "## Migration Path: IndexedDB to Cloud-First"
    - docs/architecture/neon-vercel.md contains "### Phased Migration Strategy"
    - docs/architecture/neon-vercel.md contains "Phase A: Schema Creation"
    - docs/architecture/neon-vercel.md contains "Phase B: Sync API Routes"
    - docs/architecture/neon-vercel.md contains "Phase C: Dexie Schema Extension"
    - docs/architecture/neon-vercel.md contains "Phase D: Sync Engine"
    - docs/architecture/neon-vercel.md contains "Phase E: Migration Script"
    - docs/architecture/neon-vercel.md contains "### Conflict Resolution Strategy"
    - docs/architecture/neon-vercel.md contains "Last-Write-Wins"
    - docs/architecture/neon-vercel.md contains "### Multi-Platform Access"
    - docs/architecture/neon-vercel.md contains "Android"
    - docs/architecture/neon-vercel.md contains "### Settings Toggle"
  </acceptance_criteria>
  <done>Migration path section with phased strategy, conflict resolution comparison, multi-platform access, and settings toggle documented</done>
</task>

<task type="auto">
  <name>Task 2: Add Neon Auth Migration section to architecture document</name>
  <files>docs/architecture/neon-vercel.md</files>
  <read_first>docs/architecture/neon-vercel.md, src/app/providers.tsx, .planning/phases/36-neon-db-vercel-integration-understanding/36-CONTEXT.md</read_first>
  <action>
Add a "## Neon Auth: Privy Replacement Path" section to `docs/architecture/neon-vercel.md` after the Migration Path section, with:

**### Why Replace Privy**
- E2E testing limitation: Privy requires test account credentials, cannot create/destroy users in CI
- External dependency: Privy is a third-party service with its own uptime/billing
- Branch-incompatible: Privy auth state does not branch with Neon DB branches — preview deploys share production auth
- Current workaround: `NEXT_PUBLIC_PRIVY_APP_ID` check in `src/app/providers.tsx` — Privy skipped when env var unset

**### What Neon Auth Provides**
- Built on Better Auth (v1.4.18+), managed by Neon
- Auth data stored in `neon_auth` schema in Neon database
- Branch-compatible: auth state branches with the database (preview deploys get isolated auth)
- Supports: email/password, Google OAuth (out-of-box test credentials), magic links, OTP
- Next.js SDK: server (`createNeonAuth()` + `auth.handler()`), client (`createAuthClient()` + `NeonAuthUIProvider`)
- Auto-injected env vars: `NEON_AUTH_BASE_URL`, `NEON_AUTH_COOKIE_SECRET`

**### Migration Steps**
1. Enable Neon Auth on the Neon project (Neon Console)
2. Replace `PrivyProvider` with `NeonAuthUIProvider` in `src/app/providers.tsx`
3. Replace `PinGateProvider` with Neon Auth session management
4. Update API routes to use Neon Auth session verification instead of Privy token
5. Migrate allowed-email whitelist to Neon Auth domain restriction or custom validation
6. Remove Privy dependencies: `@privy-io/react-auth`, `@privy-io/server-auth`
7. Remove Privy env vars: `NEXT_PUBLIC_PRIVY_APP_ID`, `NEXT_PUBLIC_PRIVY_CLIENT_ID`, `PRIVY_APP_SECRET`
8. Update E2E tests: create test users directly in branch database (no Privy test OTP needed)
9. Update bundle security test to verify `NEON_AUTH_COOKIE_SECRET` is not in client bundle

**### E2E Testing Improvement**
- Current: Tests use `PRIVY_TEST_EMAIL` + `PRIVY_TEST_OTP` from env vars, authenticate via Privy test account
- After: Tests create a user in the `neon_auth` schema of the branch database, no external service needed
- Branch isolation means parallel test runs cannot interfere with each other

**### Timeline Note**
This migration is a future milestone. Phase 36 documents the path only. The actual migration should be its own milestone with dedicated phases for auth replacement, data migration, and E2E test updates.
  </action>
  <verify>grep "## Neon Auth" docs/architecture/neon-vercel.md && grep "Better Auth" docs/architecture/neon-vercel.md && grep "PrivyProvider" docs/architecture/neon-vercel.md</verify>
  <acceptance_criteria>
    - docs/architecture/neon-vercel.md contains "## Neon Auth: Privy Replacement Path"
    - docs/architecture/neon-vercel.md contains "### Why Replace Privy"
    - docs/architecture/neon-vercel.md contains "### What Neon Auth Provides"
    - docs/architecture/neon-vercel.md contains "### Migration Steps"
    - docs/architecture/neon-vercel.md contains "Better Auth"
    - docs/architecture/neon-vercel.md contains "PrivyProvider"
    - docs/architecture/neon-vercel.md contains "NeonAuthUIProvider"
    - docs/architecture/neon-vercel.md contains "### E2E Testing Improvement"
    - docs/architecture/neon-vercel.md contains "### Timeline Note"
  </acceptance_criteria>
  <done>Neon Auth migration path documented with rationale, steps, and E2E testing improvement</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Threats identified:** None — this plan creates documentation only, no code changes.
**Data exposure risk:** Low — documents authentication migration path conceptually without exposing secrets.
**Mitigations:** None required for documentation-only changes.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Migration Path section exists with all 5 sub-phases (A through E)
- [ ] Conflict resolution comparison table present with 4 strategies
- [ ] LWW recommended with clear rationale
- [ ] Multi-platform section covers both PWA and Android
- [ ] Neon Auth section covers why replace Privy, what Neon Auth provides, and migration steps
- [ ] E2E testing improvement documented
- [ ] Timeline note makes clear this is a future milestone
- [ ] `pnpm build` still succeeds
</verification>

<success_criteria>
- Architecture document now covers both current integration AND future migration path
- Conflict resolution strategy comparison completed with recommendation
- Neon Auth migration path documented as Privy replacement
- Multi-platform access pattern (PWA + Android) documented
- All sections serve as actionable reference for future milestone planning
</success_criteria>

<output>
After completion, create `.planning/phases/36-neon-db-vercel-integration-understanding/36-03-SUMMARY.md`
</output>
