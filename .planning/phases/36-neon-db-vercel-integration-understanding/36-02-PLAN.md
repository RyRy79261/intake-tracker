---
phase: 36-neon-db-vercel-integration-understanding
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified: [docs/architecture/neon-vercel.md]
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Live Neon dashboard was inspected to verify branch structure matches documentation"
    - "Live Vercel dashboard was inspected to verify environment variables are correctly injected"
    - "Verification results are recorded in the architecture document"
  artifacts:
    - path: "docs/architecture/neon-vercel.md"
      provides: "Verification results appendix"
      contains: "## Verification Results"
  key_links: []
---

<objective>
Verify the Neon+Vercel integration is correctly configured by inspecting live dashboards.

Purpose: The user has no way to confirm the integration is working as intended. This plan uses browser automation to inspect the Neon Console and Vercel Dashboard, then records findings in the architecture document.
Output: Verification results appendix added to `docs/architecture/neon-vercel.md` with screenshots/observations from live dashboards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-neon-db-vercel-integration-understanding/36-CONTEXT.md
@.planning/phases/36-neon-db-vercel-integration-understanding/36-RESEARCH.md

@docs/architecture/neon-vercel.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect Neon Console for branch structure</name>
  <files>docs/architecture/neon-vercel.md</files>
  <read_first>docs/architecture/neon-vercel.md, .planning/phases/36-neon-db-vercel-integration-understanding/36-CONTEXT.md</read_first>
  <action>
Use browser automation (Claude in Chrome MCP tools) to navigate to the Neon Console (https://console.neon.tech). Check:

1. Navigate to the project dashboard
2. Go to the Branches tab/section
3. Verify:
   - A "main" branch exists and is marked as the default/production branch
   - A "staging" branch exists as a child of main
   - Check if any "preview/*" branches exist (may not if no PRs are currently open)
4. Go to the Tables/SQL tab for the main branch
5. Verify the push notification tables exist: `push_subscriptions`, `push_dose_schedules`, `push_sent_log`, `push_settings`

Record all observations. If the Neon Console requires authentication that cannot be accessed, document what was attempted and mark the verification item as "requires manual check".

Note: If browser automation tools are not available or authentication prevents access, skip to the checkpoint task below — the user can verify manually.
  </action>
  <verify>grep -c "Verification Results" docs/architecture/neon-vercel.md || echo "Section not yet added"</verify>
  <acceptance_criteria>
    - Neon Console branch structure was inspected OR documented as requiring manual verification
    - Observations recorded for inclusion in verification results
  </acceptance_criteria>
  <done>Neon Console inspection completed with observations recorded</done>
</task>

<task type="auto">
  <name>Task 2: Inspect Vercel Dashboard for environment variable configuration</name>
  <files>docs/architecture/neon-vercel.md</files>
  <read_first>docs/architecture/neon-vercel.md</read_first>
  <action>
Use browser automation (Claude in Chrome MCP tools) to navigate to the Vercel Dashboard (https://vercel.com). Check:

1. Navigate to the project settings
2. Go to Environment Variables section
3. Verify:
   - DATABASE_URL is set and scoped to different environments (Production/Preview/Development)
   - Check which other POSTGRES_*/PG* variables are present
   - Verify the Neon integration is listed under Integrations
4. Check a recent preview deployment (if any exist):
   - Verify it shows unique environment variables

Record all observations. If authentication prevents access, document what was attempted and mark as "requires manual check".
  </action>
  <verify>echo "Observations recorded"</verify>
  <acceptance_criteria>
    - Vercel Dashboard environment variables were inspected OR documented as requiring manual verification
    - Observations recorded for inclusion in verification results
  </acceptance_criteria>
  <done>Vercel Dashboard inspection completed with observations recorded</done>
</task>

<task type="auto">
  <name>Task 3: Append verification results to architecture document</name>
  <files>docs/architecture/neon-vercel.md</files>
  <read_first>docs/architecture/neon-vercel.md</read_first>
  <action>
Append a "## Verification Results" section to `docs/architecture/neon-vercel.md` with:

1. **Date:** 2026-04-06
2. **Neon Console Findings:** List each verification item from the checklist with status (Confirmed/Not Confirmed/Requires Manual Check) and notes
3. **Vercel Dashboard Findings:** Same format
4. **Summary:** Overall integration health assessment

Format each finding as a checklist:
```markdown
- [x] Main branch exists as default — Confirmed: [observation]
- [x] Staging branch is child of main — Confirmed: [observation]
- [ ] Preview branches created for PRs — Requires manual check: [reason]
```

If browser automation was not possible for some checks, clearly mark those items and note that the user should verify them manually using the steps in the Verification Checklist section.
  </action>
  <verify>grep "## Verification Results" docs/architecture/neon-vercel.md</verify>
  <acceptance_criteria>
    - docs/architecture/neon-vercel.md contains "## Verification Results"
    - Verification results section contains a date
    - Each verification checklist item has a status (Confirmed/Not Confirmed/Requires Manual Check)
  </acceptance_criteria>
  <done>Verification results appendix added to architecture document</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Neon+Vercel integration verification results appended to docs/architecture/neon-vercel.md</what-built>
  <how-to-verify>
Review the "Verification Results" section in docs/architecture/neon-vercel.md. Check:
1. Do the findings match what you see in your Neon Console and Vercel Dashboard?
2. Are there any items marked "Requires Manual Check" that you can verify now?
3. Is the overall assessment accurate?

If browser automation was unable to access the dashboards, manually check:
- Neon Console (https://console.neon.tech): Branches tab shows main + staging
- Vercel Dashboard: Environment Variables show DATABASE_URL per environment
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Threats identified:**
1. **Browser automation accessing production dashboards** (Severity: Low) — Reading dashboard data only, no write operations. Neon Console and Vercel Dashboard are read-only inspection.
**Mitigations:**
- All browser automation tasks are read-only (no mutations to Neon or Vercel settings)
- No connection strings or secrets are written to the architecture document (only variable names and structure)
- Checkpoint task allows user to verify accuracy before committing
</threat_model>

<verification>
Before declaring plan complete:
- [ ] "Verification Results" section exists in docs/architecture/neon-vercel.md
- [ ] Each verification item has a clear status
- [ ] No actual secrets, passwords, or connection strings were recorded
- [ ] User approved the verification results via checkpoint
</verification>

<success_criteria>
- Live dashboard inspection completed (or documented as requiring manual check)
- Verification results recorded in architecture document
- User confirmed accuracy of findings
</success_criteria>

<output>
After completion, create `.planning/phases/36-neon-db-vercel-integration-understanding/36-02-SUMMARY.md`
</output>
