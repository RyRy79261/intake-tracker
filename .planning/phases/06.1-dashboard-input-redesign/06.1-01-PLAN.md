---
phase: 06.1-dashboard-input-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/weight-card.tsx
  - src/components/blood-pressure-card.tsx
  - src/stores/settings-store.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "Weight card shows increment/decrement buttons flanking a centered value pre-filled with latest weight"
    - "Tapping +/- on weight card changes the displayed value by the configured step (default 0.1kg)"
    - "Tapping Save on weight card records the displayed value without any popup dialog"
    - "Blood pressure card shows systolic/diastolic/HR inputs inline without any popup dialog"
    - "BP position/arm/irregular toggles are in an expandable detail section, not always visible"
    - "No Dialog import exists in weight-card.tsx or blood-pressure-card.tsx for new entry creation"
  artifacts:
    - path: "src/components/weight-card.tsx"
      provides: "Inline increment/decrement weight input"
      contains: "Minus"
    - path: "src/components/blood-pressure-card.tsx"
      provides: "Inline BP input with expandable details"
      contains: "ChevronDown"
    - path: "src/stores/settings-store.ts"
      provides: "weightIncrement setting"
      contains: "weightIncrement"
  key_links:
    - from: "src/components/weight-card.tsx"
      to: "src/stores/settings-store.ts"
      via: "useSettings().weightIncrement"
      pattern: "settings\\.weightIncrement"
    - from: "src/components/weight-card.tsx"
      to: "src/hooks/use-health-queries.ts"
      via: "useWeightRecords for pre-fill"
      pattern: "useWeightRecords"
---

<objective>
Redesign weight and blood pressure cards to use inline input flows instead of modal dialogs.

Purpose: Weight gets a centered value display with minus/plus buttons pre-filled with the most recent weight. Blood pressure keeps its inline fields but reorganizes position/arm/irregular into an expandable detail section. Both cards eliminate any popup dialog for new entries (edit dialogs for existing records stay).
Output: Updated weight-card.tsx, blood-pressure-card.tsx, and settings-store.ts with weightIncrement setting.
</objective>

<execution_context>
@/home/ryan/repos/Personal/intake-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/repos/Personal/intake-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-dashboard-input-redesign/06.1-CONTEXT.md

<interfaces>
<!-- Key types and patterns the executor needs -->

From src/lib/card-themes.ts:
```typescript
// Each theme has: activeToggle, buttonBg, outlineBorder, outlineText, iconBg, iconColor, etc.
const theme = CARD_THEMES.weight; // emerald theme
const theme = CARD_THEMES.bp;     // rose theme
```

From src/hooks/use-health-queries.ts:
```typescript
export function useWeightRecords(limit?: number): WeightRecord[] | undefined;
export function useAddWeight(): UseMutationResult;
export function useBloodPressureRecords(limit?: number): BloodPressureRecord[] | undefined;
export function useAddBloodPressure(): UseMutationResult;
```

From src/stores/settings-store.ts:
```typescript
// Current settings (no weightIncrement exists yet — must be added)
interface SettingsState {
  urinationDefaultAmount: "small" | "medium" | "large";
  defecationDefaultAmount: "small" | "medium" | "large";
  // ... other settings
}
```

From src/components/collapsible-time-input.tsx:
```typescript
export function CollapsibleTimeInputControlled(props: {
  value: string;
  onChange: (v: string) => void;
  expanded: boolean;
  onToggle: () => void;
  id: string;
}): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign weight card with increment/decrement pattern</name>
  <files>src/components/weight-card.tsx, src/stores/settings-store.ts</files>
  <read_first>
    - src/components/weight-card.tsx (current implementation to replace)
    - src/components/intake-card.tsx (reference pattern for inline +/- buttons)
    - src/stores/settings-store.ts (add weightIncrement setting)
    - src/hooks/use-health-queries.ts (useWeightRecords, useAddWeight signatures)
    - src/lib/card-themes.ts (CARD_THEMES.weight for theme classes)
  </read_first>
  <action>
**settings-store.ts changes:**
1. Add `weightIncrement: number` to the SettingsState interface (after `defecationDefaultAmount`)
2. Add default value `weightIncrement: 0.1` in the initial state object
3. Add setter `setWeightIncrement: (value: number) => void` to the actions interface
4. Add implementation `setWeightIncrement: (value) => set({ weightIncrement: sanitizeNumericInput(value, 0.01, 10) })`

**weight-card.tsx complete rewrite:**
Replace the current text input with an increment/decrement pattern:

1. **State changes:** Remove `weightInput` string state. Add `pendingWeight: number | null` state initialized to `null`. When `recentRecords` loads and `pendingWeight` is null, set it to `recentRecords[0]?.weight ?? 70` (pre-fill with latest weight, fallback 70).

2. **Layout — header:** Keep existing header with theme icon, label, and latest weight display (same pattern as current).

3. **Layout — input section:** Replace the text input with:
   - A row of three elements: Minus button (left), centered weight display (middle), Plus button (right)
   - Minus button: `<Button variant="outline" size="icon" className="h-14 w-14 rounded-full">` with `<Minus />` icon. onClick decrements `pendingWeight` by `settings.weightIncrement`. Use `theme.activeToggle` for hover/active styling.
   - Center display: `<div className="flex-1 text-center"><span className="text-4xl font-bold">{pendingWeight?.toFixed(1)}</span><span className="text-lg text-muted-foreground ml-1">kg</span></div>`
   - Plus button: Same as minus but with `<Plus />` icon and increments.
   - Round displayed value with `Math.round(value * 10) / 10` to avoid floating point drift.

4. **Below the +/- row:** Keep `CollapsibleTimeInputControlled` for optional time override (same as current).

5. **Save button:** `<Button onClick={handleSubmit} disabled={addMutation.isPending || pendingWeight === null} className={cn("w-full h-11", theme.buttonBg)}>Record Weight</Button>`

6. **handleSubmit:** Validate with existing `WeightFormSchema`. On success, call `addMutation.mutateAsync({ weight: pendingWeight, ...(timestamp) })`, show toast, then re-set `pendingWeight` to the value just saved (so it stays as starting point for next entry).

7. **Keep:** RecentEntriesList, EditWeightDialog, useEditRecord hook — all unchanged. Edit dialog is for editing existing records only.

8. **Remove:** Dialog import is NOT used currently in weight-card (it uses inline input already), but ensure no Dialog imports creep in.

9. **Import changes:** Add `Minus, Plus` from lucide-react. Remove `Input` import (no longer used for the main input). Keep `Check, Loader2`.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Minus" src/components/weight-card.tsx returns >= 2 (import + usage)
    - grep -c "Plus" src/components/weight-card.tsx returns >= 2 (import + usage)
    - grep -c "pendingWeight" src/components/weight-card.tsx returns >= 5
    - grep -c "weightIncrement" src/stores/settings-store.ts returns >= 3 (type, default, setter)
    - grep -c "settings.weightIncrement" src/components/weight-card.tsx returns >= 1
    - grep -c "text-4xl" src/components/weight-card.tsx returns >= 1 (large centered display)
    - grep "Dialog," src/components/weight-card.tsx only matches EditWeightDialog (not Dialog from ui/dialog for new entry)
    - grep -c "toFixed" src/components/weight-card.tsx returns >= 1
  </acceptance_criteria>
  <done>Weight card shows large centered value with minus/plus buttons, pre-filled with latest recorded weight, increment controlled by settings.weightIncrement (default 0.1kg), saves inline without any modal</done>
</task>

<task type="auto">
  <name>Task 2: Reorganize blood pressure card with expandable details</name>
  <files>src/components/blood-pressure-card.tsx</files>
  <read_first>
    - src/components/blood-pressure-card.tsx (current implementation)
    - src/components/intake-card.tsx (reference for inline pattern)
    - src/lib/card-themes.ts (CARD_THEMES.bp theme classes)
    - src/lib/constants.ts (getBPCategory function)
  </read_first>
  <action>
The BP card is already inline (no popup dialog for new entries). The redesign reorganizes it so the primary inputs (systolic/diastolic) are prominent, and secondary inputs (HR, position, arm, irregular) are in a collapsible "Details" section.

**Restructure the input section order:**

1. **Primary inputs (always visible):**
   - Systolic/Diastolic grid (2 columns) — keep exactly as-is with labels, h-12 inputs, validation errors
   - Record button directly below — move UP from bottom to right after sys/dia inputs

2. **Expandable detail section (collapsed by default):**
   - Triggered by a ghost button: `<Button variant="ghost" size="sm" className="w-full justify-between text-muted-foreground">` with text "More options" and ChevronDown/ChevronUp icon
   - When expanded, show in a `<div className="p-3 rounded-lg bg-muted/50 border space-y-3">`:
     a. Heart Rate input (optional) — move from always-visible to here
     b. Position toggle (sitting/standing) — move from always-visible to here
     c. Arm toggle (left/right) — move from always-visible to here
     d. Irregular heartbeat toggle — move from the "Advanced" sub-section to the same level
     e. CollapsibleTimeInputControlled — move inside details section
   - Remove the separate "Advanced" expandable section — merge irregular heartbeat into the single detail section

3. **State changes:**
   - Remove `showAdvanced` state (no separate advanced section)
   - Keep `showTimeInput` and `customTime` but they only appear inside the detail section
   - Rename nothing — keep existing state names for position, arm, irregularHeartbeat
   - Add a single `showDetails` state (replaces both `showAdvanced` and reduces visual clutter)

4. **Defaults:** Position defaults to "sitting", arm defaults to "left" — these are sent with every submission even if details section is never opened (same as current behavior).

5. **Button order:** Record button is ABOVE the details expander, so one-tap recording of systolic/diastolic is possible without scrolling past detail fields.

6. **Keep unchanged:** Header, RecentEntriesList, EditBloodPressureDialog, all edit-related hooks and state.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/Personal/intake-tracker && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "showDetails" src/components/blood-pressure-card.tsx returns >= 3 (state, setter, condition)
    - grep -c "showAdvanced" src/components/blood-pressure-card.tsx returns 0 (removed)
    - grep "More options" src/components/blood-pressure-card.tsx returns a match
    - The Record button JSX appears BEFORE the "More options" button in the file (button before details expander)
    - grep -c "Dialog," src/components/blood-pressure-card.tsx only matches EditBloodPressureDialog
    - grep -c "position.*sitting" src/components/blood-pressure-card.tsx returns >= 1 (default still sent)
  </acceptance_criteria>
  <done>BP card shows systolic/diastolic inputs and Record button prominently, with HR/position/arm/irregular/time override tucked into a single expandable "More options" section. No popup dialog for new entries.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- Weight card renders with +/- buttons and large centered value
- BP card renders with primary sys/dia inputs and collapsible detail section
- No Dialog import from "@/components/ui/dialog" exists in either card file for new entry creation
- Edit dialogs (EditWeightDialog, EditBloodPressureDialog) still render and function for editing existing records
</verification>

<success_criteria>
- Weight card: minus button, centered value display (text-4xl), plus button, pre-filled with latest weight, configurable increment from settings
- BP card: sys/dia inputs + record button always visible; HR, position, arm, irregular, time override in collapsible section
- Both cards: zero popup dialogs for new entries, edit dialogs preserved for existing records
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-dashboard-input-redesign/06.1-01-SUMMARY.md`
</output>
