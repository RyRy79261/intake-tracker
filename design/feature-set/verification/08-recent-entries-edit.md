# Verification — 08-recent-entries-edit

**Verdict:** minor-gaps  ·  checked 96 claims, verified 89.

Verified against actual source in `src/components/recent-entries-list.tsx`, the six `edit-*-dialog.tsx` files, `src/hooks/use-edit-record.ts`, `use-record-adapters.ts`, `use-delete-with-toast.ts`, `use-undo-delete-mutation.ts`, all five consumer cards, `history-drawer.tsx`, `analytics/records-tab.tsx`, plus supporting `lib/constants.ts`, `lib/card-themes.ts`, `medications/undo-toast.tsx`, `lib/db.ts`, `lib/date-utils.ts`, `lib/alcohol-units.ts`, `lib/intake-service.ts`, `lib/history-types.ts`, query hooks, and the regression test `recent-entries-list.dom.test.tsx`.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | "Note handling: trimmed; blank becomes `undefined`" stated as a blanket rule (line 213) | True for intake/eating/urination/defecation adapters and for ALL inline `useEditRecord` paths, but the **weight** and **bp** dialog adapters use `fields.note \|\| undefined` (NOT trimmed). The analytics records-tab weight/BP handlers also use `editNote \|\| undefined` (untrimmed). | `src/hooks/use-record-adapters.ts:177` (weight), `:203` (bp); `src/components/analytics/records-tab.tsx:269,286` |
| low | "Cards typically fetch last `5` … or use water query default (Liquids)" (line 135); "cards fetch the last 5 records" (line 27) | Urination/Eating cards pass `5` explicitly (hook defaults are 10), Weight/BP default to 5. Liquids uses `useRecentIntakeRecords` → `getRecentRecords` whose default `limit` is **3** (not 5). The "water query default" wording is correct but its value is 3, and the blanket "last 5" generalization (line 27) does not hold for Liquids. | `src/lib/intake-service.ts:113` (`limit = 3`); `src/components/urination-card.tsx:45`, `food-salt/food-section.tsx:101` (pass 5); `use-health-queries.ts:68,118` (default 5); `use-eating-queries.ts:27`, `use-urination-queries.ts:31` (default 10) |
| low | BP `irregularHeartbeat` is a "checkbox / Yes/No toggle" for "inline form **+ dialog**" (line 162) | Editable inline (BP card uses a Checkbox at `blood-pressure-card.tsx:487`). In the **dialog/adapter** path it is NOT editable: `FieldMap.bp` omits `irregularHeartbeat`, the bp adapter never writes it, and `EditBloodPressureDialog`'s Yes/No Select is gated on `onIrregularHeartbeatChange` — which neither history-drawer nor records-tab passes. So the dialog path silently drops it. | `src/hooks/use-record-adapters.ts:36-44` (no field); `edit-blood-pressure-dialog.tsx:163` (gated); `history-drawer.tsx:318-337` / `records-tab.tsx:480-499` (not wired) |
| low | EditEatingDialog "edits an eating record (time + 'what I ate' note + optional grams)" and grams field only when `onGramsChange` provided (lines 64,123) | Correct, but worth flagging: the grams path is dead in both current hosts — neither history-drawer (`:338`) nor records-tab (`:500`) passes `grams`/`onGramsChange`, so the grams field never renders in the modal editor. The inline FoodSection editor handles grams via a separate `editGrams` field, not this dialog prop. | `edit-eating-dialog.tsx:72`; `history-drawer.tsx:338-347`; `records-tab.tsx:500-509` |
| low | Undo toast "title (e.g. 'Record deleted' / domain message)" (line 128) | All four consumers (intake/urination/defecation/eating) call `useUndoDeleteMutation` with no custom `title`, so the title is always the default "Record deleted". No "domain message" title is actually passed anywhere. | `use-intake-queries.ts:107`, `use-urination-queries.ts:65`, `use-defecation-queries.ts:65`, `use-eating-queries.ts:61` (no 3rd arg) |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| medium | `EditWeightDialog` and `EditBloodPressureDialog` are entirely absent from the "Files covered" list (lines 3-16) and the "Sub-components / variants" list (lines 226-241), yet they are core to the dialog-editor surface — rendered by both history-drawer and records-tab for the weight/bp types. | `src/components/edit-weight-dialog.tsx`, `edit-blood-pressure-dialog.tsx`; rendered at `history-drawer.tsx:306,318`, `records-tab.tsx:468,480` |
| low | `useDeleteWithToast` success toast title is the fixed string **"Entry deleted"**; the caller-supplied message is the `description`, not the title. Doc (line 56) says "shows a success toast (caller-supplied message)" without noting the fixed title. | `src/hooks/use-delete-with-toast.ts:27-29` |
| low | History-drawer and records-tab delete (the dialog hosts, separate from card `useDeleteWithToast`) show a hardcoded toast `{title:"Entry deleted", description:"Record removed"}` with NO undo — only the card path gets the 5s Undo toast. Doc's delete/undo section implies undo is universal. | `history-drawer.tsx:119`, `records-tab.tsx:188` |
| low | `useEditRecord` on mutation failure shows a destructive toast `{title:"Error", description:"Could not update the entry"}`. The doc's inline-form "Success/Validation" states (lines 116-117) omit this generic update-failure toast. | `src/hooks/use-edit-record.ts:112-118` |
| low | Caffeine `EditSubstanceDialog` amount input is rendered for caffeine too (`min={0}`, step `1`), not hidden — only the **Volume** field is alcohol-only. Doc line 123 says "caffeine hides volume" (correct) but the amount field framing in §"Conditional fields" could read as if more is hidden; volume is the sole conditional block. | `edit-substance-dialog.tsx:87-117` |
| low | `EditableType` discriminated union and `FieldMap`/`RecordOf`/`EditingState` typed shapes are described, but the `parseTimestamp` helper that converts the thrown `Error` into a `ValidationError("Invalid date/time")` is the actual mechanism behind the "re-raised as ValidationError" claim (line 211) — confirmed but worth pinning. | `use-record-adapters.ts:134-142` |
| low | Records-tab substance edit: when `editSubstanceAmount` is blank, NO amount field is written (`hasAmount` false → empty `amountField`), so timestamp+description can be edited without touching amount. Doc's substance-edit description (line 198) implies amount is always part of the write. | `records-tab.tsx:341,359-373` |
| low | Records-tab is the ONLY host that renders `EditSubstanceDialog`; history-drawer's `openEdit` early-returns for caffeine/alcohol (doc states this at line 219, correct) — but the doc's §"In the modal dialogs" (line 86) phrasing "caffeine/alcohol open the substance dialog" is only true in analytics, never in history-drawer. | `history-drawer.tsx:142`, `records-tab.tsx:222-242,534` |

## Spot-confirmed

- `RecentEntriesList` returns `null` when `records` undefined/empty; `maxEntries` default **3**; slices `records.slice(0, maxEntries)`; "Recent" header; per-row ghost trash; `role="button"`/`tabIndex=0`/keyboard guard `e.target !== e.currentTarget`; trash `e.stopPropagation()`; `Loader2` spinner + `disabled` while `deletingId === record.id`; hover `hover:bg-black/5 dark:hover:bg-white/5` active `active:bg-black/10 dark:active:bg-white/10`; editing row replaced on `bg-muted/30 rounded-lg`. `recent-entries-list.tsx:105,110,112,116,118-145,155-171,135,124`.
- `InlineEditFormShell` renders children → datetime-local → note → Save/Cancel; labeled mode shows "Date and time" + "Note (optional)"; unlabeled fallback uses `aria-label` + placeholder "Note (optional)"; `idPrefix` default `"edit"`; per-instance `useId()` suffix; Save accepts `buttonClassName`. `recent-entries-list.tsx:27-28,43-45,49-72`. Collision regression test present. `recent-entries-list.dom.test.tsx:41-79`.
- `useEditRecord`: tracks `editingRecord`/`editTimestamp`/`editNote`; `openEdit` populates common + `onOpen`; `dateTimeLocalToTimestamp` throws → caught → "Invalid date/time" toast; note `.trim() || undefined`; `buildUpdates` null aborts; success toast "Entry updated"; `optionsRef` ref for latest callbacks. `use-edit-record.ts:75-121`.
- `useRecordAdapters`/`initEditingState`: `EditableType = intake|weight|bp|eating|urination|defecation`; `ValidationError extends Error`; intake amount `parseInt > 0` else "Invalid amount"; weight `parseFloat > 0` else "Invalid weight"; bp systolic+diastolic `> 0` else "Invalid values"; `parseTimestamp` re-raises as ValidationError "Invalid date/time". `use-record-adapters.ts:25-31,63,157,174,196-198,141`.
- `useDeleteWithToast`: `deletingId` state, success/error toasts, `finally` clears spinner. `use-delete-with-toast.ts:21-43`.
- `useUndoDeleteMutation`: `unwrap(deleteFn)`, onSuccess `showUndoToast`, default title "Record deleted"; used by intake/urination/defecation/eating. `use-undo-delete-mutation.ts:14-25`.
- `showUndoToast`: `duration: 5000`, `ToastAction` "Undo". `medications/undo-toast.tsx:23-28`.
- `EditIntakeDialog`: water→Water/ml/`bg-sky-600 hover:bg-sky-700`, sugar→Sugar/g/`bg-pink-600 hover:bg-pink-700`, salt→Sodium/mg/`bg-amber-600 hover:bg-amber-700`; amount `type=number min=1 step=1`; note `maxLength=200`. `edit-intake-dialog.tsx:43-52,63-67,99-122`.
- `EditSubstanceDialog`: caffeine "Caffeine (mg)" step `1` placeholder "e.g. Flat white" theme `caffeine`; alcohol "% ABV" step `0.1` placeholder "e.g. Glass of red wine" + Volume(ml) + "Standard drinks are calculated from ABV % and volume." hint; `min={0}`. `edit-substance-dialog.tsx:48-52,84,100-117`. CARD_THEMES.caffeine `bg-yellow-700 hover:bg-yellow-800`, alcohol `bg-fuchsia-600 hover:bg-fuchsia-700`. `card-themes.ts:234,255`.
- `EditEstimateEntryDialog`: `NONE_VALUE="__none__"` ↔ `""` when `allowNoEstimate`; Urination wrapper `bg-violet-600 hover:bg-violet-700` Small/Medium/Large; Defecation wrapper `bg-stone-600 hover:bg-stone-700` + `allowNoEstimate`. `edit-estimate-entry-dialog.tsx:24,100-109`; `edit-urination-dialog.tsx:31-36`; `edit-defecation-dialog.tsx:23-31`.
- `URINATION_AMOUNT_OPTIONS`/`DEFECATION_AMOUNT_OPTIONS` = small/medium/large with labels Small/Medium/Large. `constants.ts:80-92`. SodiumSource multipliers sodium 1.0 / salt 0.39 / msg 0.12. `food-section.tsx:54-58`.
- BP enums: `position: "sitting"|"standing"`, `arm: "left"|"right"` (DB declares `position:"standing"|"sitting"`, `arm:"left"|"right"`). `db.ts:86-87`. `getBPCategory` thresholds: ≥180/≥110 Grade 3, ≥160/≥100 Grade 2, ≥140/≥90 Grade 1, ≥130/≥85 High normal, ≥120/≥80 Normal, else Optimal. `constants.ts:57-71`.
- History drawer `FILTER_TABS`: All/Water/Salt/Weight/BP/Eating/Urination/Defecation; `PAGE_SIZE=30`; Load More. `history-drawer.tsx:47,188-197`. Records-tab `FilterType` full 12-value enum incl. caffeine/alcohol; sugar/potassium tabs gated by tracker. `history-types.ts:14`; `records-tab.tsx:72-89,111-116`.
- Liquid composable: beverage name persisted as `source = "beverage:<name>"`; `syncLiquidSubstancesMutation` clears via blank→0 soft-delete; `openTokenRef` discards stale `fetchEntryGroup`; legacy ABV back-derived via `abvFromStandardDrinks`. `liquids-card.tsx:82,180-205,124-161`.
- Eating composable: back-converts sodium-mg via `SODIUM_MULTIPLIERS`; disabled trackers omitted (`...(sugarEnabled && …)`); `openTokenRef` guard. `food-section.tsx:162-207,148-180`.
- Alcohol math: ABV `0 < amt ≤ 100` AND volume > 0 else toasts "ABV must be between 0 and 100" / "Enter a volume greater than 0"; `standardDrinksFromAbv(amt,vol).toFixed(2)`. `records-tab.tsx:343-356,368`.
- Substance description required ("Description required"). `records-tab.tsx:340`. SubstanceRecord interface fields and source enum `'water_intake'|'eating'|'standalone'`. `db.ts:304-324`.
- Soft-delete + undo via `ServiceResult`/`unwrap`. `service-result.ts:1-13`; `use-undo-delete-mutation.ts:20`.
- Quick-add time cap `max={getCurrentDateTimeLocal()}` (urination direct; weight/BP via `CollapsibleTimeInputControlled` default max). `urination-card.tsx:199`; `collapsible-time-input.tsx:61,116`.

## Low-confidence / could-not-verify

- "≈5s undo window" / "5000 ms" — verified literally (`duration: 5000`); the "≈" framing is harmless. No autotest exercises the actual dismissal timing.
- The doc's "keyboard-aware scroll so the field stays visible above the mobile keyboard" (line 90) — `onFocus={scrollOnFocus}` is wired in both dialog hosts via `useKeyboardAwareScroll`; behavioral correctness of the scroll itself not exercised here (out of scope, code presence confirmed). `history-drawer.tsx:56,304`; `records-tab.tsx:107,466`.
- "BP categories … plus higher grades" (line 163) — doc lists 120/130/140/160 systolic & 80/85/90/100 diastolic and labels through Grade 2, deferring Grade 3 (180/110) to "higher grades". Technically accurate but the explicit Grade 3 thresholds (≥180/≥110) are not stated; treated as acceptable summarization, not flagged.
