# ===========================================
# Privy Authentication
# Get credentials from dashboard.privy.io
# ===========================================
NEXT_PUBLIC_PRIVY_APP_ID=your-app-id
NEXT_PUBLIC_PRIVY_CLIENT_ID=your-client-id
PRIVY_APP_SECRET=your-app-secret

# Whitelist (comma-separated emails/wallets allowed to use the app)
ALLOWED_EMAILS=you@gmail.com
# ALLOWED_WALLETS=0x123...,0x456...

# ===========================================
# Perplexity AI (for food/drink parsing)
# Get API key from perplexity.ai/settings/api
# ===========================================
PERPLEXITY_API_KEY=pplx-xxx

# ===========================================
# Neon Postgres (for server storage mode)
# Get credentials from Vercel Marketplace or neon.tech
# Only DATABASE_URL is required for the app
# ===========================================

# Recommended for most uses (pooled connection)
DATABASE_URL=postgresql://neondb_owner:.../neondb?sslmode=require

# For uses requiring a connection without pgbouncer
DATABASE_URL_UNPOOLED=postgresql://neondb_owner:.../neondb?sslmode=require

# Parameters for constructing your own connection string
PGHOST=...
PGHOST_UNPOOLED=...
PGUSER=neondb_owner
PGDATABASE=neondb
PGPASSWORD=...

# Parameters for Vercel Postgres Templates (auto-populated by Vercel)
POSTGRES_URL=postgresql://neondb_owner:.../neondb?sslmode=require
POSTGRES_URL_NON_POOLING=postgresql://neondb_owner:.../neondb?sslmode=require
POSTGRES_USER=neondb_owner
POSTGRES_HOST=...
POSTGRES_PASSWORD=...
POSTGRES_DATABASE=neondb
POSTGRES_URL_NO_SSL=postgresql://neondb_owner:.../neondb
POSTGRES_PRISMA_URL=postgresql://neondb_owner:.../neondb?connect_timeout=15&sslmode=require
